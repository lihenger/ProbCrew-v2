# ============================================================================
# ProbCrew v2 · 忽略规则
# ----------------------------------------------------------------------------
# 三条原则，按重要性排序：
#   1. L2 敏感数据在**结构上不可能**入库（不是"小心别提交"）—— 靠的是
#      "运行时目录整目录忽略" + "L0 资产放在别处"，不是靠后缀匹配
#   2. L1 教材语料与模型权重体积大、可随时重抓，不入仓才能保住 cite_id 的复核口径，绝不入库
#   3. 生成物**必须入库**（否则 clone 后不跑脚本就打不开），因此不在此列
# ============================================================================

# ===== 密钥与本地配置 =====
.env
.env.local
*.local

# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# ===== ★ L1 + L2（+ 日志）：`backend/data/` 整个目录默认忽略 =====
# 该目录从此是【纯运行时目录】：只装 L1（教材语料、**抓取到的开放许可资源缓存**、
# 模型权重）与 L2（学生库）。自制内容资产不放这里 —— 它们属 L0，在
# **顶层 `content/`**（见 docs/02 §1.3）。
#
# 为什么是"整目录 deny-by-default"而不是"精确匹配库文件后缀"：
# 旧写法的洞是 glob 的 `*` **不跨目录** —— `backend/data/*.sqlite` 匹配不到
# `backend/data/sub/learning.sqlite`，也匹配不到改了扩展名的库文件。
# 而"结构上不可能入库"这句话，靠的正是这条规则本身。
#
# ★ 外传是【流向属性，不是目录属性】—— 本文件管不到它；去向由 docs/02 的出网清单与 R-K/R-L 守。
# ★ 另有一条与此相关但**不受本文件管辖**的风险：用户凭据（N14）
#   只存浏览器、随请求携带 ⇒ 它**不进仓库也不进库**，但**可能进日志**。
#   那条防线在 docs/02 §1.4 的 R-J，不在这里。
backend/data/**
!backend/data/.gitkeep

# ===== ★ L0 内容资产：必须入库（此处显式声明，防止被上面的规则或将来
#        新增的全局规则误伤）=====
# 五类资产 + content/MANIFEST.json 都要入库 —— "已核验的标准答案"是批改
# 闭环的依据，要能随代码分发、可 diff、可 review（docs/02 §3.3）。
# 注意：这里放的是**自编内容**；真实考试题与教材原文属 L1，不入库。
!content/
!content/**

# ===== ★ 盲测冻结正式集（N13）—— 结构上不可能入库 =====
# 口径（docs/02 §9）：冻结正式集 = **注入集 20 道 + 冻结验证集 + 期望判据表**，
#   **不入主仓**，由非实现者保管、放在主仓之外（执行时用 `--frozen-dir <dir>` 指过去）。
#   入仓的**只有** content/eval/sample/（样例集）—— 下面几条**不碰它**。
#
# ★ 为什么要把这件事从"靠人"改成"靠结构"：
#   `.gitignore` 对 content/ 是**显式反转放行**（上面的 !content/ 与 !content/**），
#   所以任何新建的 content/eval/<任意名>/ 都会被正常跟踪；而"冻结集不入仓"
#   在此之前**完全靠人记得**。★ 远端仓库是 **public**（2026-09-18 实测）——
#   误提交一次，历史与 fork 里都删不干净，**盲测当场作废且不可逆**。
#   原则 1（"L2 在结构上不可能入库"）在 backend/data/ 上成立，在这里此前不成立。
#
# ★ 不带前导斜杠 ⇒ **任意层级**都匹配（同时挡住仓根的 ./frozen/ 与
#   content/eval/frozen/ 两种落法）。★ 这几条针对的是一个**不该存在**的落点，
#   因此它**不出现在 docs/03 §2 的骨架里** —— 骨架列的是**存在的东西**（§2 规则 3）。
# ★ 2026-09-18 已逐个核过既有的 32 个入仓文件与第一轮 21 个计划产物，无一命中本段。
frozen/
frozen/**
frozen_cases.json
frozen_judgement.json

# ===== 检索模型权重（BGE-M3 等，数 GB）=====
# ★ 2026-09-16 已裁定【不使用】嵌入 / 重排权重（docs/02 §11.2-13 · 决策 1A）：
#   检索退化为纯词法，因此 `backend/data/models/` 不存在。
#   下列规则【保留】仅为防止将来误引入数 GB 权重 —— 那不是"多一条忽略规则"，
#   而是"补第三个出网口"，必须走 ADR。
models/
*.safetensors
*.onnx

# ===== 前端 =====
node_modules/
dist/
build/

# 说明：frontend/vendor/katex/fonts/ 约 1.5MB，**保留**（保证零依赖公式渲染（与网络无关））。

# ===== 编辑器 / 系统 =====
.vscode/*
!.vscode/settings.json.example
.idea/
.DS_Store
Thumbs.db

# ===== 大体积素材（教材原始文件，注意版权）=====
*.pdf
!docs/**/*.pdf

# ===== 临时文件 =====
*.tmp
.git-commit-msg.tmp
.draft-*.md

# ===== 会话交接文档（不属于项目文档）=====
docs/*交接*.md
docs/*handover*.md
