# 02 · 系统设计

> | | |
> |---|---|
> | 性质 | **系统设计的唯一权威**。领域模型 · 数据模型 · 主链路 · 状态机 · 错误语义 · 失败模式 |
> | 回答什么 | **系统由什么构成、数据长什么样、一次请求怎么走完、出错时怎么办** |
> | **不**回答什么 | 为什么要有这些需求 → [`01-需求分析`](01-需求分析.md) · 怎么分层与依赖 → [`03-架构总览`](03-架构总览.md) · 字段的机器可读定义 → [`contracts/`](../contracts/) · 谁做 → [`05-分工与协作规范`](05-分工与协作规范.md) |
> | 上游输入 | `docs/01` 的硬要求：**F7 快照与版本号 · F15 免登录例外与越权语义 · F19 知识点标注与 DAG · F20 五类内容资产 · N11 可寻址 · N13 评测资产盲测** · 附录 A 的两条对照规则 · §8 承诺与规则 R-A/R-B/R-C |
> | 下游产出 | `contracts/*.schema.json`（A3）· `docs/03` 的分层与依赖（**§2.4 / §7.2 已于 2026-09-16 移入**）· **7 个页面 + 1 个未排期 demo 页**的实现依据 |
> | 快照 | ProbCrew v2 · 阶段 A2 |

**本文是上一个仓库从来没有过的那一篇。** 旧仓的需求、任务书、页面规格都写得很细，唯独没有一份文档回答"数据长什么样、一次请求怎么走完"—— 于是字段由界面反推（6 处需求缺口就是这么漏的），错误体每个接口各写各的形状，最热的问答接口是唯一没有契约的接口。本文的存在就是为了这两件事不再发生。

---

## 1. 设计约束（先立边界，再谈设计）

### 1.1 继承自 A1 的硬要求

| 来源 | 要求 | 本篇落点 |
|---|---|---|
| F7 | 作答记录含**标准答案快照 + 资产版本号**；"用时"为自陈值 | §3.3 · §3.2 的 `asset_version` 字段 |
| F15 | 全部学习类接口要求登录，**不设免登录例外**；学生只见自己 | §4.1 · §6.3 |
| F19 | 作答记录知识点标注**非空**；知识点 **DAG 无环** | §2.2 · §3.2 · §3.4 |
| F20 | **五类**内容资产，固定格式、可版本化、条目关联知识点 ID、题库带 `gradable` | §3.3 |
| N11 | URL 可表达界面状态，返回键有效 | §7.1 |
| N13 | 评测资产**盲测**：样例集与运行器入仓、纳入门禁；冻结正式集不入仓 | §9 · §9.1 |
| **`docs/01 §8`** | **R-A**：字符串字段必须有上界；判定正则禁嵌套量词 | §6.1 · §8 · **§1.4** |
| **`docs/01 §8`** | **R-B**：写库必须显式提交；测试必须读回 | §3.6 · §8 · **§1.4** |
| **`docs/01 §8`** | **R-C**：F20 与编排层各须有责任人 | `docs/05`（不属本篇） |
| 附录 A | 每个字段指得出需求 ID；每条 MUST 有字段或接口支撑 | §3.4 · §10 |

### 1.2 技术约束（不可在本篇推翻）

| 约束 | 来源 | 设计后果 |
|---|---|---|
| **免构建前端**：无 npm、无打包器、原生 ESM、**零依赖** | ADR-0001 | 前端不做 SSR、不做 TS、不做 CSS-in-JS；vendor 本地化。★ **注意：原文写的"离线可跑"已删（2026-09-16）** —— 完整链路依赖网络（见下两行），"免构建"的正当性现在锚在**零依赖 + 多人并行 + 交付简单**上 |
| **只做主站**，探针后置 | ADR-0003 | 上下文来源必须可替换（N12）；`page-context` 契约不进 v2 |
| **保留 LangGraph** | ADR-0003 | 编排以图运行时承载；但**业务逻辑不得写进节点函数**（`docs/03` 强制） |
| **stdlib `sqlite3`**，无 ORM | ADR-0003 | 表结构集中在一处建表入口；查询手写 SQL；迁移手写脚本 |
| ★ **用户自持凭据** | **N14** | 凭据**只存浏览器本地**、**随请求携带**、**服务端不代为持有**；**硬约束：凭据不得落日志、不得进审计 `detail`、不得进错误信息、不得进 L2 表**。**无凭据 ⇒ 答疑不可用**（判定 / 错题本 / 知识图谱仍可用） |
| ★ **学习资源联网可达，但只取开放许可** | **F10** | **抓取在预抓阶段完成，运行期只读缓存** —— 不做运行期实时抓取（否则网络抖动会表现成"答疑不稳定"，且版权与引用不可控）。缓存属 L1，不入仓 |
| ~~数据不出校~~ | ~~N3~~ | **已废弃（2026-09-16）**：学生提问会发给第三方模型服务。**残余影响见 §1.3 与 `docs/01` F16 的删除权边界** |

### 1.3 数据分级（★ 2026-09-16 重构：本表**只管"入仓与否"**这一个维度）

**重构原因**：原表把三个**正交维度**塞进一张表 —— **入仓与否** · **传输去向** · **删除与保留**，于是「外传」被写成与 L0/L1/L2 并列的**一级**。但 `student_qa_log` **同时是 L2 且会外传** —— 一张表表达不了"一物两属性"。现在按维度拆开：**本表管入仓**（§1.3）· **去向与删除见 §1.4** · **出网面见 §1.5**。

★ **二次修正（2026-09-16）：本表只承担"分级与入仓"，另两个维度降为索引列。**「保留与删除」给出该级的**处置口径**、「含外传字段？」给出该级**有没有外传面** —— 它们是**概览**，逐项的分工、去向与出网路径在 **§1.4 / §1.5**，**冲突以那两节为准**。原文写的是"本表只管入仓这一个维度"，却仍留着另两列 —— 那是**重构只做了一半**；而把这两列删掉反而会让 §1.3 无法自证"这一级会不会被删、会不会外传"。

| 级 | 内容 | 存放 | 入仓？ | 保留与删除 | 含外传字段？ |
|---|---|---|---|---|---|
| **L0** | 代码、契约、文档、**自制内容资产** | `content/`（顶层，与 `contracts/` 并列）+ 代码目录 | ✅ | git 历史 | ❌ |
| **L1** | **开放许可资源缓存**（预抓产物） | `backend/data/corpus/` | ❌ | 可随时重抓 / 删除 | ❌ |
| **L2** | **学生数据**：账号 · 令牌 · 审计 · 问答流水 · 提示流水 · 作答 · 掌握度 | `backend/data/*.sqlite` | ❌ **结构上不可能入仓** | **四类处置见 §1.4** | ★ **`qa_log` 含提问原文 ⇒ 是**（见 §1.5） |

> ★ **路径是"结构上不可能入仓"这句话的一半**：五类自制资产放**顶层 `content/`**，`backend/data/` 因此成为**纯运行时目录**（只装 L1 与 L2）—— 于是 `.gitignore` 可以对整个目录 **deny-by-default**，而不是靠"精确匹配库文件后缀"（旧仓的写法有个洞：`backend/data/*.sqlite` 里的 `*` **不跨目录**，库文件一旦挪进子目录就悄悄不被忽略）。
> ★ **"模型权重"已从 L1 删除（2026-09-16）**：原表把它与语料并列，与三处冲突 —— **N16** 明写"我们**不内置、不打包模型权重**"；**§1.5③** 的出网清单只有两个口，**没有任何口下载权重**；数 GB 权重也不可能"可随时重抓"。`ADR-0005` 的"被否决方案"里"内置 / 打包本地模型权重"同样已被否决。用户自备的本机模型是**用户自己的运行环境**，不是本项目的存放对象。
> ★ **已裁定（2026-09-16，负责人 · 决策 1A / §11.1-3）：v2 检索不使用嵌入 / 重排权重。** 旧仓的"混合检索"依赖 **BGE-M3 + bge-reranker-v2-m3** 的本地权重（数 GB），与三处硬约束冲突：**N16** 的"不内置、不打包模型权重" · **§1.5③** 的"**恰好两个出网口**"（没有任何口下载权重）· 本行对 L1 的收窄。**裁定 = 检索退化为纯词法**（BM25 + 中文 bigram），因此 **L1 只剩 `backend/data/corpus/` 一项**，`backend/data/models/` **不存在**。**代价（明账）**：F10 的检索质量下降（F10 是 SHOULD，其验收只要求"命中带出处 / 无命中如实说明"，**不承诺召回率**）。**若要恢复向量检索**，须走 ADR，并**为权重下载补一条出网口**（或把它并入 `scripts/fetch_corpus.py` 的预抓职责）。
> **L2 表的机械识别**：全部使用 `student_` 共同前缀，一条 `LIKE 'student_%'` 即可排除（沿用旧仓数据分级约束）。
> **前缀是数据分级口径，目录是代码归属口径** —— 两者严格分开，因此把存储层拆到 `identity/` 与 `learning/` **不会**破坏这条安全属性（ADR-0003 §5）。

**L1 语料的形状（必须约定，否则 F10 不可断言）**：
"语料不入仓"**不等于**"语料没有形状"。最小约定：

```
backend/data/corpus/<教材>/ch<nn>/sec<nn>.md     ← 路径即章节位置
    文件前置元数据（YAML front matter）：
        cite_id:    <教材>-<nn>-<nn>    ← ★ 出处的机器可读标识（F10 的验收对象）
        title:      第 3 章 条件概率
        chapter:    3
        license:    CC-BY-4.0           ← ★ 必填：只收开放许可
        source_url: https://...         ← ★ 必填：来源可追溯
        fetched_at: 2026-09-16          ← ★ 必填：抓取时间
```

- **`cite_id` 是 F10 的验收对象**：检索响应必须带 `cite_id`，而不是一段自由文本的"出处" —— 否则"检索命中带出处"无法断言、也无法核对。
- ★ **只取开放许可资源**（MIT OCW / OpenStax / 可汗学院等），`license` / `source_url` / `fetched_at` **缺一即为资产错误 ⇒ 启动报错退出**（与 §8-14 同一模式）。
- **预抓 + 运行期只读缓存**：抓取在预抓阶段完成，运行期不联网取语料 —— 这样 `cite_id` 长期可复核（网页会变，缓存不会）。
- 缓存缺失时检索**如实说明"出处暂不可用"**（F10），**不得编造章节号**；并计入 §8 的失败模式。

### 1.4 数据存放分工表（★ 2026-09-16 新增 —— "什么存在哪里"的权威表）

**先定义参照物**（"本地"有三个意思，必须分开；此前**从未定义参照物**）：

| 名称 | 指什么 |
|---|---|
| **浏览器本地** | 学生**自己的设备**（`localStorage` / 内存 / HTTP 缓存） |
| **部署主机** | 跑 FastAPI + SQLite 的那台机器。**单机形态 = 教师 / 答辩本人的机器；远程托管形态 = 我们运营的服务器** |
| **第三方** | 用户配置的**远程**模型服务 |

> ★ **必须写死的一句话**：**"不外传"的准确含义是"不发往第三方模型服务"，不等于"数据不出学生设备"。**
> 单机形态下两者重合；**远程托管形态下两者不重合** —— 学生数据**存在我们的服务器上**（这正是 **N17 受控托管**存在的理由）。这是答辩现场最容易被追问的一处。

| 数据 | 存在哪 | 具体位置 / 字段 | 清除动作 | 需求 |
|---|---|---|---|---|
| 会话令牌（明文） | 浏览器本地 | `localStorage`；库里只存 sha256 | 登出 / 401 / 改密 / 禁用 / 删除 ⇒ 清 | F15 · N6 |
| **模型凭据（明文）** | 浏览器本地 | `localStorage`，固定键名，与账号绑定；**服务端不落任何介质** | 登出 / **切换账号** / 删除账号 / 改密 / 手动 ⇒ 清 | **N14** |
| **端点配置**（provider · model · base_url · egress） | 浏览器本地 | `localStorage`，与凭据同生命周期 | 同凭据 | **N16** |
| 界面位置（题目 / 提示层级 / 筛选 / 翻页） | URL | `#/practice?kc=&item=&hint=` | 不适用 | N11 |
| 跨页面临时态（当前 run、未提交作答） | 浏览器内存 | `core/store` | 刷新即丢（可接受） | — |
| 静态资源（katex 等） | 浏览器 HTTP 缓存 | 部署主机静态托管 | 浏览器自清 | N4 |
| 账号 · 令牌哈希 · 审计 · 问答流水（含提问原文）· 提示流水 · 作答 · 掌握度 | **部署主机 · SQLite** | `backend/data/*.sqlite`（七张 `student_` 表） | **四类处置**：四张数据表**硬删** / `token` **级联删** / `user` **脱敏** / `audit` **保留（只记 ID 与动作）** | F16 · F7 · F15 |
| **告知已读标记** | **部署主机 · SQLite** | `student_user.notice_ack_at`（存服务端的理由：**换设备仍须告知**） | 随账号脱敏清空 | **N15** |
| 开放许可资源缓存（预抓产物） | 部署主机 · 文件系统 | `backend/data/corpus/`（不入仓） | 可随时重抓 / 删除 | F10 |
| ★ **备份产物** | **部署主机 · 文件系统** | `backend/data/backups/` —— **同一台主机的另一个卷 / 目录**，仍在 `backend/data/**` 的 deny-by-default 内；**不含日志**（R-O） | 按保留策略轮转覆盖 | **N19** |
| **运行日志** | **部署主机 · 文件系统** | **`backend/data/logs/`**，轮转 10MB × 5；**内容受 R-O 约束** | 轮转覆盖 | N14 · N16 |
| 契约 · 五类内容资产 · 代码 · 文档 | 仓库 | `contracts/` · `content/` · `backend/` · `docs/` | git 历史 | F20 |
| **提问原文 + 上下文**（仅 P2 远程端点） | **第三方**（不在我们控制内） | 随请求发往远程端点；`qa_log` 只记 `provider` / `model` / `egress` | **删不掉** —— 须与 N15 一并告知 | **N15 · N16** |

**表后三条规则**：

1. ★ **R-O · 日志与审计只记 ID 与动作，不记内容**：不得出现提问原文、学生答案、教材片段、凭据。**理由**：F16 靠"读回 0 行"验收，而**日志文件不在读回清单里** —— 内容一旦进日志，"删除"就只删了数据库那一份。这是原设计里**唯一一个"验收通过但数据仍在"的洞**。
2. **日志目录定死为 `backend/data/logs/`**（此前全仓无日志位置），落在 `backend/data/` 的 deny-by-default 之内，`.gitignore` 既有规则即可覆盖。
3. **凭据与端点配置同生命周期**；**凭据不随令牌撤销而失效**（它是用户的额度，不是我们的会话），但**登出必须清除**（保护"下一个人"）。
4. ★ **备份不构成第三个出网口（N19）**：备份**只落部署主机本地**（另一个卷 / 目录），**不写对象存储、不写异地** —— 否则 §1.5③ 的"恰好两个口"就不成立。**异地 / 多副本备份列为不做**，放开必须走 ADR。
5. ★ **凭据的"五条清除动作"以本表为准**（登出 / 切换账号 / 改密 / 禁用 / 删除 / 手动）—— 前端的键名与清除动作表见 §7.4，两处必须同源。

### 1.5 运行形态、配置与出网面（N14 · N15 · N16 · N17）

**① 部署形态两档 × 运行配置三档**（★ 端点**由用户提供**，**不存在部署级共享端点**。★ 2026-09-16 补 `egress` 与告知文案两列 —— 这是 **"形态"与"去向"两个维度**的**四象限**视图）：

| 部署形态 | 配置档 | `egress` | 是否外传 | 断网可否 | 需要什么 | N15 告知文案 |
|---|---|---|---|---|---|---|
| **单机**（答辩 / 教师自用） | **P1 本机端点** | `loopback` | ❌ | ✅ **完整演示（含答疑）** | 用户自备一个本机 OpenAI 兼容端点 | **"数据不发往第三方"** |
| 单机 | **P2 远程端点 + 凭据** | `remote` | ✅ | ❌ | 网络 + 用户凭据 | **"提问会发送到第三方模型服务"** + 供应商名 + 载荷范围 |
| 单机 / 远程托管 | **P3 无凭据** | —（**不写 `qa_log`**） | ❌ | ✅（**不含答疑**） | — | **"数据不发往第三方"** |
| **远程托管**（正式服务） | **P2 远程端点 + 凭据** | `remote` | ✅ | ❌ | 网络 + 用户凭据 | **"提问会发送到第三方模型服务"** + 供应商名 + 载荷范围 |
| 远程托管 | **P3 无凭据** | —（**不写 `qa_log`**） | ❌ | ✅（**不含答疑**） | — | **"数据不发往第三方"** |
| ~~远程托管~~ | ~~P1 本机端点~~ | — | — | — | ★ **一律拒绝**：学生填的 loopback 是**服务器**的 loopback，既无正当性也是 SSRF 面 | — |

> ★ **这一列是本表存在的理由（2026-09-16）**：**N15 的判据是 `egress`，不是部署形态。** 只按"单机 / 远程托管"分档会让**第 2 行**（单机 + P2 远程端点）的学生看到"数据不发往第三方"，而提问正文**正在**发往第三方 —— **单机形态下两列不重合**。这句话是 N15 与 N2 的接缝，必须写在这里而不是留给人推断。
> **P1 的启用方式（`ADR-0005 §4`）**：loopback 例外**仅在单机形态**，且必须由部署者**显式勾选**才生效（与 N18 的"单机允许 http"**共用同一开关**）；默认关闭。

**② 能力 × 依赖矩阵**：

| 能力 | 需网络 | 需凭据 | 需 L1 缓存 |
|---|---|---|---|
| 答疑（F1–F3 · F13） | ✅（P2）· ❌（P1） | ✅ | ❌ |
| 判定 / 错因（F4 · F5） | ❌ | ❌ | ❌（吃 L0 题库 / 错因库） |
| 错题本 / 进度 / 掌握度（F8 · F11 · F14） | ❌ | ❌ | ❌ |
| 知识图谱 / DAG（F9 · F17–F19） | ❌ | ❌ | ❌ |
| 公式 / 定理检索（F18） | ❌ | ❌ | ❌ |
| 教材出处（F10） | ❌（**运行期只读 L1 缓存** —— 见 §1.3"预抓 + 运行期只读缓存"） | ❌ | ✅ |
| 门禁评测（N13） | ❌ | ❌ | ❌ |

> **这张矩阵是"降级演示"的验收依据**：把"需网络 / 需凭据"两列全为 ❌ 的能力连起来，就是"**无凭据也能演示**"的清单。

**③ 出网清单（egress manifest —— 恰好两个口）**：

| 口 | 位置 | 何时 | 目的地 |
|---|---|---|---|
| **(A) 运行期模型调用** | `backend/app/tools/llm.py`（**唯一出网口**） | 服务运行期 | 用户配置的端点 |
| **(B) 预抓脚本** | `scripts/fetch_corpus.py` | **离线运行，不在服务运行期** | 开放许可资源站点 |

**④ 载荷白名单**：

- **正向允许**：`query` · `context.text` / `context.refs` · 系统提示 · ★ **自制内容资产片段**（`content/` 的题型 / 公式 / 错因解释 —— **原文写的是「L0 资产片段」，而 L0 含代码、契约、文档，出网的只有这一部分**，2026-09-16 改口径）。
- ★ **反向禁止**：`user_id` · `username` · 令牌 · `attempt` · `mastery` · `qa_log` 历史 · 审计记录。
- **凭据作为认证头发给端点是 N14 的固有面**（由 **R-M 的"七面"**兜底，见 §1.6），**与载荷白名单分开陈述** —— 混谈会让"白名单"看起来包含凭据。
- ★ **本清单由 R-L 守**：**新增任何出网字段，必须同时改这份清单与 R-L 的断言** —— 否则"白名单"就退化成一句注释。

> ★ **这一节不含 `GET /api/health`（2026-09-16）**：health 只报告**本地事实**（DB · L1 缓存 · 资产 `rev`），**不做任何出网调用** —— 否则它就成了第三个口，且与 §8-7 的"压测时 health < 100ms"冲突。原措辞写它"报告 DB / **模型** / 检索状态"，而**模型状态需要一次出网调用**，且 N14 下**不存在部署级端点**，health 无从知道任何用户的模型。
> ★ **这一节也不含备份（N19）**：备份只落部署主机本地（§1.4 规则 4）。
> **为什么给出网口一个名字**：在此之前"联网"只是散落的实现细节，因此 R-J 与 N15 **都只能靠自觉**。收口到 `tools/llm.py` 之后才有可 grep 的执行载体（R-K），`§4.2.1` 的端点信任边界也才有唯一的施加点。

### 1.6 检查规则总表（"谁守"必须落在一处）

A1 的 §8 定了 R-A/R-B/R-C 三条规则，本篇的 §7.3 / §7.4 / §8 又散着若干条"可被脚本守住"的断言。**散着写就等于没有** —— 下表把它们收在一处，并明确每条是**可自动**还是**仅 review**。

| 规则 | 内容 | 检查方式 | 谁执行 |
|---|---|---|---|
| **R-A** | 请求体字符串字段必须有上界；判定正则禁嵌套量词 | **可自动**：`scripts/verify.sh` 静态扫描 + 长度/长数字串用例 | 门禁 |
| **R-B** | 写库必须显式 `commit()`；测试必须读回断言 | **可自动**：扫描 + 读回用例 | 门禁 |
| **R-C** | F20 与编排层各须有责任人 | **仅 review**（脚本查不了） | `docs/05` 责任人表 |
| **R-D** | 样式**禁止硬编码颜色/圆角/阴影**，只能来自 `tokens.css` | **可自动**：grep `#rrggbb` / `rgb(` / `rgba(` / `hsl(` / `oklch(`，**扫描范围排除 `styles/tokens.css`**（唯一允许出现色值的文件，见 §7.3）。★ **颜色关键字（`red` 等）绕得过 grep，靠 review 兜底** | 门禁（+ review 兜底） |
| **R-E** | 前端**不得用 `innerHTML` 拼用户内容** | **可自动**：grep `innerHTML` 赋值（§7.4，XSS 的唯一防线） | 门禁 |
| **R-F** | 可访问性**三项分开**：点击目标 ≥ 32px（可自动）· 键盘可达 + 语义标签（可自动）· **对比度 ≥ 4.5:1（只对"声明的色对"可自动；渲染结果仅 review）** | **可自动**：①② 前端冒烟断言；③ 色对清单 + 纯 Python WCAG 计算（§7.3） | 门禁 |
| **R-G** | URL 必须可寻址，且 `hashchange` / `popstate` 有监听 | **可自动**：路由冒烟（N11） | 门禁 |
| **R-H** | 演示类页面总行数 ≤ 产品功能页总行数 | **可自动**：按 feature 分组统计行数（`docs/01` 承诺 10） | 门禁（review 级） |
| **R-I** | 契约中标记 **`must_render: true`** 的字段，前端**必须实际渲染** | **可自动**：前端冒烟断言该字段出现在 DOM 中 | 门禁 |
| **R-J** | **凭据不得出现在日志 / 审计 / 错误信息 / L2** | **可自动**：grep 日志与审计写入点 + 一条"带凭据请求不产生含凭据日志"的用例 | 门禁 |
| **R-K** | **出网收口**：全仓**只有一个运行期出网口** `backend/app/tools/llm.py`；预抓脚本 `scripts/fetch_corpus.py` **只能离线跑**，不在服务运行期 | **可自动**：grep 出网调用（`httpx` / `requests` / `urllib` / `socket`）出现在 `tools/llm.py` 之外即红灯 | 门禁 |
| **R-L** | **载荷白名单**：出网载荷只允许 §1.5④ 的**正向清单**（★ **正向项以 §1.5④ 的"自制内容资产片段"为准，不是"L0 资产"**）；**反向禁止**字段（`user_id` · `username` · 令牌 · `attempt` · `mastery` · `qa_log` 历史 · 审计）一律不出现 | **可自动**：**假 provider 抓载荷** + 断言不含禁止字段（N16 验收③） | 门禁 |
| **R-M** | ★ **凭据七面不落**（R-J 的四面 → **七面**）：**① 日志 · ② 审计 `detail` · ③ 错误信息 · ④ L2 表 · ⑤ URL / query 串 · ⑥ 前端持久化以外的地方（内存快照 / 埋点 / 导出） · ⑦ 出网载荷本体**（作为 `Authorization` / `X-Model-Key` 请求头是 **N14 的固有面**，**但不得出现在 body 或 query 里**） | **可自动**：①②③④⑦ 由 grep + 假 provider 抓载荷断言；⑤ 由路由冒烟断言 URL 不含凭据；**⑥ 仅 review**（断言"无埋点、无导出路径"） | 门禁 + review |
| **R-N** | **"去向可查"不是一句声明**：设置页展示的去向必须与 `SELECT DISTINCT egress FROM student_qa_log` **一致**。★ **前置（2026-09-16，决策 5A / §11.1-7）：该账号至少完成过一次答疑** —— 干净账号（P3）该查询为空，断言**既非真也非假**；P3 下设置页展示的是"当前无凭据、未发生外传" | **可自动**：接口用例（N16 验收②） | 门禁 |
| **R-Q** | ★ **工具调用边界**（2026-09-16 新增，**施加点在 `docs/03 §7②` 与 `tools/`**）：① **工具的允许集合不得为通配** —— 调用方声明的 `allowed` 必须是非空集合，**不得出现 `allowed=None` 这类"任何 Agent 都能调任何工具"的写法**；② **工具结果的 `ok` 必须来自真实执行结果**，**禁止硬编码 `true`**；③ 工具失败**必须进入事件流**（不得静默吞掉） | **可自动**：grep `allowed=None` / 硬编码的成功标记（阶段 B 随 `scripts/verify.sh` 落地）；失败可见性由用例断言 | 门禁 |
| **R-P** | ★ **文档自洽门禁**（**R-P1 计数由表体导出** · **R-P2 相对引用解析** · **R-P3 落点反向闭合**，三条明细见本节末） | **可自动**：`scripts/verify.sh`（阶段 B）—— 从表体算计数并断言正文数字、解析全部 `§N.M` 引用、对 §10 每行的落点 grep 关键词 | 门禁 |
| **R-O** | ★ **日志与审计只记 ID 与动作，不记内容**：不得出现提问原文、学生答案、教材片段、凭据。日志目录定死 `backend/data/logs/`（轮转 10MB × 5） | **可自动**：grep 日志与审计写入点 + 一条"跑完一次答疑后日志文件**不含提问原文**"的用例 | 门禁 |

> **R-I 的由来**：`docs/01` F3 的验收原文是"前端渲染该标签（**后端发了前端不看等于没发**）" —— 这句话此前**没有载体**。R-I 给它装上了：契约里标 `must_render`，前端冒烟就必须断言它出现在 DOM 里。★ **R-I 只对契约里标了 `must_render` 的字段成立**（2026-09-17 修正）：当前落点是 **F3 的级别标签**（`verification.level` + `levelLabel`）与 **N15 的告知文案**（`user.noticeStatus.message`）。★ **F14 / F18 不纳入** —— 它们在 `docs/01` 里是 SHOULD 且**没有渲染要求**（F14 要求"存在自陈视图"、F18 要求"返回唯一条目 + 适用条件"），原句写"同理覆盖 F14 / F18 的渲染口径"与契约实际标记不符。
> **R-J 是本轮新增风险（N14 连带）的防线**：凭据只存浏览器、随请求传，因此它**会出现在请求头里** —— 任何一处"顺手把整个请求打日志"的代码都会泄露用户额度。
> ★ **R-Q 是旧仓两条技术债的堵法（2026-09-16）**：旧仓 `01-架构总览 §8` 记着"**工具权限门控形同虚设**"（`RunContext.use()` 写死 `allowed=None`，任何 Agent 都能调它没声明的工具）与"**工具失败不可见**"（`tool.result.ok` 被硬编码为 `true`）—— v2 保留 `tools/` 层与图编排，**这两条照旧适用**，因此按"**必须继承并修正**"处理（不是"重新写就好"），由 **R-Q** 守。落点见 `docs/03 §7②` 与 `§9`。

> **R-K…R-O（2026-09-16 新增）** 是"数据走向"重构的执行载体：**R-K** 把出网收口到 `tools/llm.py`（唯一口）· **R-L** 守住载荷白名单 · **R-M** 把 R-J 从四面扩到**七面** · **R-N** 保证"去向可查"不是一句声明 · ★ **R-O 是那个"验收通过但数据仍在"的洞的堵法** —— F16 靠"读回 0 行"验收，而**日志文件不在读回清单里**：内容一旦进日志，"删除"就只删了数据库那一份。

> **R-E 的定位要说清**：它是**唯一**的 XSS 防线，而且**是纪律不是机制**（没有 CSP、没有 DOMPurify —— ★ 理由是"**免构建 + 零依赖**"，而**不是**"离线"；"离线"已不是 v2 的需求口径）。grep 能挡住"顺手写了 `innerHTML`"，挡不住"用 `insertAdjacentHTML` 绕过" —— 这一条仍需要在 review 里看。
> **R-C 是唯一查不了的一条**，正因如此它才必须写进 `docs/05` 的责任人表（`docs/01 §8` 的 R-C 原文）。
> ★ **R-K…R-O 此前只在一段引文里被描述，没有表行** —— 表头写着"下表把它们收在一处，并明确每条是可自动还是仅 review"，而正文只有 R-A…R-J 十行。本节的原则是"**散着写就等于没有**"，它此前**被自己违反了**（2026-09-16 补行，同时把 **R-M 的"七面"逐条列出** —— 原文只说"从四面扩到七面"，七面是什么从未写明，而 §1.5④ 与 §10 的 N14 行都以它为依据）。

> ★ **R-P · 文档自洽门禁（2026-09-16 新增，可自动）** —— 本轮评审查出的每一处硬伤，**都是在正文里能机械发现的**。不进门禁，下一轮一定重演：
>
> | 检查 | 内容 | 本轮它抓到的是 |
> |---|---|---|
> | **R-P1** | **计数由表体导出**：`docs/01 §1.1` 的行数与 S1–S4 计数、附录 B.1 的 MUST 条数、本篇 §10 的落点数 —— **三者由脚本从各自表体计算**，并断言与正文写的数字一致 | §1.1 曾**同时**写着"37 条"与"35 行"；B.1 标题写 25、同节却写"22 → 23" |
> | **R-P2** | **相对引用解析**：★ **三种引用形式都要解析** —— 本篇 `§N.M`（须命中本篇标题）· 跨篇 `docs/0X §N.M`（须命中**那一篇**的标题）· `ADR-00NN` 的 `§N`（须命中该 ADR 的小节）；**裸 `§N.M` 一律按"本篇"判**，指别篇时必须写全前缀 —— ★ **例外（2026-09-16 补）**：同一行（或同一表格单元格）里**跟在 `docs/0X §N.M` 之后的并列引用**可省略前缀，但**必须先在该行出现一次带前缀的引用，且它们同属那一篇**（如「`docs/02` 的 §1.2 · §1.4 · §1.6」合法；整行没有任何前缀的裸引用一律按本篇判）。链接目标若属**未产出**文档，须在 `docs/07` 的"未产出清单"登记 | `§4.4` 被引用却不存在；`GET /api/runs/{id}` 被断言却不在路由表；"`mp_<slug>`，见 §2.2"而 §2.2 没有它 |
> | **R-P3** | **落点反向闭合**：本篇 §10 每一行指出的落点，必须能在**被指章节**里 grep 到该需求 ID 或其关键词（如 N15 → `notice_ack_at`、N16 → `egress`）；`docs/01` 附录 A 每项须在 `contracts/` **或 `docs/03`** 找到落点（**数据侧 → 契约 · 传输与运维侧 → 架构篇**，A3 后生效） | §10 的 N15 行指向 §7.1，而 §7.1 里没有 `notice_ack_at` 的任何内容 |
>
> **接入点**：`scripts/verify.sh`（阶段 B）。**`docs/06`（A4）必须把 R-P 列为门禁项** —— 那是"文档不会自己漂回去"的唯一保证。

---

## 2. 领域模型

### 2.1 名词与职责

| 名词 | 是什么 | 归属层 | 谁产出 |
|---|---|---|---|
| **知识点**（KnowledgePoint） | 依赖图的一个节点，如"条件概率" | `domain`（纯数据） | **内容资产**（F20） |
| **依赖 DAG** | 知识点之间的有向无环关系（前置 → 后继） | `domain` | **内容资产**（F19 要求无环） |
| **题型**（ProblemType） | 一类题的解法套路，如"贝叶斯公式正向代入" | `domain` | **内容资产**（F20，F17 的"适用模型"吃它） |
| **题目**（Problem） | 题干 + 标准答案 + 知识点标注 + 题型 + 难度 | **内容资产** | F20 |
| **错因模式**（MistakePattern） | 一种可枚举的错误，如"方向颠倒" | **内容资产** | F20（F5 吃它） |
| **公式条目**（FormulaEntry） | 定理 / 公式 + **适用条件** + 反例 | **内容资产** | F20（F18 吃它） |
| **作答**（Attempt） | 一次提交：答案、对错、知识点、用时、提示次数 | `learning` | 学生提交 → 判定 |
| **掌握度**（Mastery） | `(user, 知识点)` 的聚合：作答数 / 正确数 | `learning` | 由作答累加 |
| **验证报告**（VerificationReport） | 这次解答验证到什么程度（级别 + 逐项检查 + 置信度） | `graph`（验证节点） | 每条解答必带（F3） |
| **提示**（Hint） | 分级提示的一级（方向 / 步骤 / 解析） | `domain` + `learning` | 分级给出 → 计入 `hint_used` |
| **内容版本**（content_rev） | 五类资产各自的 rev 组合出的**单一版本号** | `domain` | 生成物 `MANIFEST.json` |

### 2.2 知识点 DAG（F19 的核心）

```
            ┌────────────┐
            │ 样本空间与 │
            │  事件运算   │
            └─────┬──────┘
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
  ┌──────────┐        ┌──────────┐
  │ 条件概率  │        │ 独立性   │
  └────┬─────┘        └────┬─────┘
       │                   │
       ├─────────┬─────────┘
       ▼         ▼
  ┌─────────┐ ┌──────────┐
  │ 全概率   │ │ 贝叶斯公式│
  └────┬────┘ └─────┬────┘
       └──────┬─────┘
              ▼
        ┌──────────┐        ┌──────────┐
        │ 随机变量  │───────▶│ 期望     │
        └──────────┘        └────┬─────┘
                                 ▼
                            ┌──────────┐
                            │ 方差     │
                            └──────────┘
```

（示意图；**规模与真实节点以内容资产为准**，首版目标见 F20 的 DAG 要求。）

**四条设计规则**：

1. **ID 稳定且可引用、不得复用** —— 被历史记录长期引用的 ID 一律适用此规则，**不只知识点**：
   - `kc_<slug>`（知识点，如 `kc_bayes`）
   - `prob_<slug>`（题目，被 `attempt.item_id` 引用）
   - `pt_<slug>`（题型，被题目与 F17 的 `model_id` 引用）
   - ★ **`mp_<slug>`**（**错因条目**，被 `attempt.mistake_id` 引用 —— 见 §3.2）
   - ★ **`fm_<slug>`**（**公式 / 定理条目**，被 F18 的检索响应引用）
   改含义只能**新增 ID** + 把旧的标 `deprecated`，否则历史作答的标注会指向**错误含义**（而且无法事后区分哪些是错的）。
   ★ **两个前缀是 2026-09-16 补的**：§3.2 的 `mistake_id` 早已写"`mp_<slug>`，**见 §2.2**"，而 §2.2 只定义了三个前缀 —— 那是一个**指向不存在内容的引用**；`fm_` 则从未定义，而 F18 要"按名称返回**唯一条目**"，没有稳定 ID 就只能靠名称字符串。
   ★ **适用范围已扩（2026-09-16）**：本规则原文限定为"被**历史记录**引用的 ID"，实际更宽 —— **凡被契约、资产或历史记录引用的 ID 一律适用**（`mp_` 被 `attempt` 引用、`fm_` 被检索响应引用，两者都不属于"历史记录"）。
2. **无环是硬约束**（F19 验收）：建图时做拓扑排序，有环即拒绝加载（不是警告）。加载失败必须**报错退出**，不得静默丢边。
3. **边有含义**：`depends_on`（前置）单向。上溯（F9）沿 `depends_on` 反向走。★ **上溯的门槛已裁定（2026-09-16，决策 6A / §11.1-8）：不设门槛 —— 全量返回 + 计数。** 输出 = 从该知识点的错误作答出发、沿 `depends_on` **反向可达**的前置知识点集合（**同一条路径不得重复计数**）；界面**全量显示**并给出计数（"沿依赖上溯到 N 个前置知识点"），**不按错题数或层数截断** —— 门槛属学习者建模（`docs/01 §9` 不做），而"只显示前 2 层"这类截断会让 F9 的"上溯到根源"**失去判据**。集合为空时显式说"未定位到前置薄弱点"。
4. **题目与题型都挂知识点**，但两者独立：一道题可以考多个知识点；一个题型可以服务多个知识点。

### 2.3 关系图

```
   内容资产（L0，入仓，可版本化）
   ┌──────────┬──────────┬──────────┬──────────┬─────────────┐
   │ 题库      │ 错因库    │ 知识点DAG │ 公式库    │ 题型与解法套路│
   └────┬─────┴────┬─────┴────┬─────┴────┬─────┴──────┬──────┘
        │          │          │          │            │
        │   kc_ids │  kc_ids  │  node    │  kc_ids    │  kc_ids
        ▼          ▼          ▼          ▼            ▼
   ┌─────────────────────────────────────────────────────────┐
   │                   知识点（KnowledgePoint）                │
   └───────────────────────────┬─────────────────────────────┘
                               │ kc_ids[]
                               ▼
   学生 ──提交──▶ 判定（确定性，不依赖 LLM） ──▶ 作答记录（Attempt）
                               │                      │
                               │ hint_used            │ kc_ids
                               ▼                      ▼
                        提示阶梯状态            掌握度（Mastery）
                               │                (user, kc) 聚合
                               ▼                      │
                       错因诊断（吃错因库）             ▼
                                             进度 / 薄弱视图（F11）
```

### 2.4 领域层的边界 → **已移入 [`03-架构总览`](03-架构总览.md)**

`domain/` 是**纯函数层**：不碰数据库、不碰 FastAPI、不碰 LangGraph、不发网络请求。**它的边界表（属于 / 不属于 `domain`）与理由在 `docs/03 §4`。**

★ **本节 2026-09-16 移出（一主题一篇）**：**分层与依赖是 `docs/03` 的主题**（`ADR-0003` 决策 6 与 `docs/02` 头部都把"怎么分层与依赖"指派给它）。原写法让"`domain` 里有什么"与"层与层怎么依赖"**分居两篇**，读者要跳两篇才能拼出完整分层 —— 正是"同一主题两处记载"的形态。

> **本篇保留的关联义务**：§3.4 的字段-需求对照表与 §10 的落点表，仍以"`domain` 是纯函数层"为前提（判定可复算、注入集可函数级执行）。

---

## 3. 数据模型

### 3.1 L2 七张表（全部 `student_` 前缀；其中六张 per-user，审计表天然跨用户）

| 表 | 装什么 | 谁写 | 谁读 | 删除权（F16） |
|---|---|---|---|---|
| `student_user` | 账号（bcrypt 哈希 / 角色 / 状态 / 锁定） | `identity` | `identity` · 管理页 | **脱敏**（不可登录），不物理删 |
| `student_token` | 会话令牌（**只存 sha256**） | `identity` | `identity` | **级联删除** |
| `student_audit` | 审计：谁在何时看了 / 改了谁的数据。**`actor_id` / `target_id` 天然跨用户**，因此不是 per-user 表 | `identity` | 管理页 | **保留**（已裁定，见下） |
| `student_qa_log` | 问答流水（**含提问原文**，L2 里最敏感）。★ **字段增量（2026-09-16）**：`provider` · `model` · **`egress`**（枚举 **`loopback`** / **`remote`**）。三者是 **N16 验收②**（设置页展示的去向必须与 `SELECT DISTINCT egress FROM student_qa_log` 一致）与 **N16 验收①**（`qa_log.egress='loopback'`）**唯一的读对象** —— 此前它们只在 §1.4 的分工表里出现过一次，本表、§3.4、§3.5 都没有。★ **P3（无凭据）不写 `qa_log`**：没发起就没有提问流水，否则"先落再答"会记下**从未发出**的提问，"问了就算数"这句话会变成假账 | `graph` 落库 | 管理页 · 统计 | 硬删 |
| **`student_hint_log`** | **提示流水**：谁在哪道题上取了第几级提示（`hint_id` PK · `user_id` · `item_id` · `session_id` · `level`(1/2/3) · `served_at` · ★ **`attempt_id`（可空）**）。★ **`hint_used` 的唯一权威来源**；★ **`attempt_id` 是"这一级提示是不是在本次作答之前取的"的唯一对账依据**（F6 的伦理约束要判的是"先要提示、后提交"）。★ **`hint_used_total` 不落列** —— 它按 `(user_id, item_id)` 计数得到（见 §3.2） | `learning` | 错题本 · 掌握度 · 自陈视图 | 硬删 |
| `student_attempt` | 作答记录 | `learning` | 错题本 · 进度 · 管理页 | 硬删 |
| `student_mastery` | 知识点掌握度聚合 | `learning` | 进度 / 薄弱视图 | 硬删 |

> **为什么提示要单独一张表**：`docs/01` F6 要求"每次提示使 `hint_used` **精确** +1"，§5 要求"用了 3 次提示才答对**照实**记"。如果这个计数由客户端在提交时上报，那两条就只是**前端的一句声明** —— 改一下 URL 或直接构造请求即可绕过。**这张表是伦理约束的执行载体，不是便利设施。**

> ✅ **已裁定（2026-09-16，负责人）· `student_audit` 在删除用户时保留**
> **设计**：`student_audit` **不随删除而清除**，理由：它是"管理员看过谁的数据"的合规证据，删掉它等于删掉监督记录；且账号本身是**化名**（N6 不采集学号/姓名），脱敏后 `user_id` 已无法回溯到具体的人。审计表本身只存**化名 ID 与动作**（R-O）。
> ★ **前提（N17 口径，2026-09-16 补充）**：本裁定依赖"**`user_id` ↔ 身份 的映射不落在我们控制的服务器之外**" —— 单机形态下即"不落在这台机器之外"，远程托管形态下即"不落在**我们运营的服务器**之外"。N17 声称已补上这句前提，而正文此前没有它；**"保留审计"的正当性完全建立在这句话上**（脱敏能成立，是因为映射不在外部）。
> **同时要求**：删除动作本身必须写一条审计（谁删的、删了几行）。
> **已考虑并否决的替代方案**：① 删除时一并清除 `target_id` 指向该用户的行 —— 否决，因为"该生数据曾被反复查看"这件事会变得不可查，而它恰恰是监督的价值所在；② 对 `target_id` 做盐化 —— 否决，为一条**落在我们控制范围内**的审计记录引入一个密钥管理面不划算。
> **代价（记账）**：删除后仍存在"某个匿名 ID 曾被查看过"的记录。这是**有意选择合规证据优先于彻底清除**，若日后合规要求变化，改法是上面被否决的方案 ①。

### 3.2 关键表：字段级设计

#### `student_attempt` —— 本仓的**核心表**（F7 / F19 的落点）

| 字段 | 类型 | 本仓处置 | 支撑需求 |
|---|---|---|---|
| `attempt_id` | TEXT PK | 保留 | F7 |
| `user_id` | TEXT | 保留（身份归属） | F7 · F15 |
| ~~`student_id`~~ | ~~TEXT~~ | ★ **已删除（2026-09-16）**：它是旧仓搬来的遗留列，原理由是"历史兼容" —— 但**v2 是新库、不迁旧数据**（§3.2 下文与 `ADR-0003` 的可逆性一节均如此说），新库里**没有历史**。保留一个指向不存在的历史的列，只会让接手者以为有迁移负担，并且它让"写入时与 `user_id` 同值"这条纪律永远悬在那里。**若日后决定迁入旧库数据，须先补一条需求再加回该列** | —（**无需求支撑 ⇒ 按附录 A 规则 1 删除**） |
| `item_id` | TEXT | 保留（指向题库条目） | F7 · F8 |
| `session_id` | TEXT | 保留（同一浏览器会话，用于「本会话用了 N 次提示」F14） | F14 |
| `kc_ids` | TEXT(JSON) | **非空强制**（见下） | **F19** |
| `correct` | INTEGER | **改为可空** —— `NULL` 表示"**未判定**"（见 A5：不可判定题目） | F7 · F11 |
| `score` | REAL | 保留（应用题部分分；未判定时为 NULL） | F7 |
| `answer_raw` | TEXT | 保留（学生原文） | F7 · F5 |
| `answer_normalized` | TEXT | 保留（归一化后，便于复算） | F4 |
| `expected` | TEXT | **标准答案快照**（F7 明写） | **F7** |
| `hint_used` | INTEGER | 保留（**伦理约束的落地点**，§5）。**值由服务端从 `student_hint_log` 计数得出，不接受客户端上报**。★ **作用域 = 本次会话**（`user,item,session`） | F6 · F14 |
| ~~`hint_used_total`~~ | **—（★ 不是列）** | ★ **不落列（2026-09-16 写死）：跨会话累计**（`user,item`）由 `SELECT COUNT(*) FROM student_hint_log WHERE user_id=? AND item_id=?` **派生**，供错题本显示"这道题一共提示了 N 次"，同样由**服务端**算 —— 它落的是"每次取提示"的明细，而 `attempt` 侧只存 `hint_used`（会话域）。★ **原行把类型写成 `INTEGER`，与同一格里的"不落列"自相矛盾**：写 DDL 的人**无法判断该不该建这一列**，而 `§3.4` 与 `§11.2` 都按"不落列"记载 —— 按 `student_id` 那一行的既有做法标为**非列**（它仍是**字段级**需求，落点见 §3.4）。原设计只有一个字段，而"重做时重新计"（会话域）与"历史不清零"（题目域）**各自预设了不同的作用域** | F8 · F11 · F14（**派生字段，非列**） |
| `duration_ms` | INTEGER | 保留 —— **客户端自陈值，不参与任何验收**（F7 已记账；要可信用时需另建"作答开始"记录，v2 不做） | F7 |
| `attempt_no` | INTEGER | 保留（第几次做这道题）。**由服务端按 `(user_id, item_id)` 历史计数 +1 得出**，不接受客户端上报 | F8 · F11 |
| `source` | TEXT | 保留（`practice` / `wrong_review` / …） | F8 |
| `grader` | TEXT | 保留；**新增取值 `not_gradable`** 表示"该题不可判定"（与 `correct=NULL` 同时出现） | F4 · N2 |
| `created_at` | REAL | 保留 —— ★ **本表的时间字段**；`§3.5` 的索引与 `§6.4` 的 cursor 分页都以它为排序键 | F7 · F8 · F11 |
| **`asset_version`** | TEXT | ★ **新增字段** | **F7** |
| **`mistake_id`** | TEXT | ★ **新增（P0-1）**：这次错误命中的**错因条目 ID**（`mp_<slug>`，见 §2.2）。**可空**。语义写死：**`NULL` = 判定为正确、或未判定** | **F5** · F8 · F11 |
| **`mistake_matched`** | INTEGER | ★ **新增（P0-1）**：`1` = 识别了错因（此时 `mistake_id` 非空）· `0` = **如实未识别**（`mistake_id` 为 NULL）。区分"未识别"与"没匹配上"，对齐 F5 的"不得编造错因" | **F5** |

> **`deleted`（软删）字段已删除**。它原是旧仓搬来的字段，但实测**没有任何代码把它置 1**，而所有读查询都过滤 `deleted=0` —— 是个"**有读无写的死字段**"：既支撑不了 F16（F16 是硬删 + 读回 0 行），也没有任何需求要求它。按附录 A 规则 1，只有"删"或"补需求"两种状态，**本仓选择删**（v2 是新库，无迁移负担）。删后读查询不再需要 `deleted=0` 过滤。若将来要做"撤回误删"，须先补一条需求再加字段。

**新增 `asset_version` 的理由与合规性**：
F7 要求"**标准答案快照 + 资产版本号**"。`expected` 已提供快照，但快照离了版本号**无法解释**——题库改版后，"当时的标准答案"为什么是这个值、知识点标注依据的是哪一版 DAG，都查不出来。
**这是"只增不改"允许的增量变更**：新字段可空、有默认值、不改变任何既有字段的含义，旧数据读出来是 NULL 而非错误。

**`kc_ids` 非空的实现路径（F19）**：
不是"在写入时校验一下"这么弱 —— 而是**从源头保证**：F20 要求**题库条目必须关联知识点 ID**，而 `attempt.kc_ids` 由题目继承。因此：

```
题库条目 kc_ids 必填（F20）
        └─▶ 作答时继承 kc_ids
                └─▶ attempt.kc_ids 天然非空（F19）
```

三层都要有断言：① 题库加载时校验条目 `kc_ids` 非空；② 契约里 `kc_ids` 标 `minItems: 1`；③ 写入函数拒绝空数组并抛错（**不得静默写 `[]`**）。
> 为什么强调第三层：旧仓该列是 `NOT NULL DEFAULT '[]'` —— **"非空"在数据库层面看起来被满足了，实际存的是空数组**。这正是 F19 要防的那种"表面合规"。

#### `student_mastery` —— 聚合口径

| 字段 | 说明 |
|---|---|
| `user_id` + `topic`（PK） | `topic` **就是知识点 ID**（与 `attempt.kc_ids` 同口径，不再另设一套命名） |
| `attempts` / `correct` | 聚合计数 |
| `updated_at` | 最后更新时刻 |

**更新时机**：**每次作答后累加**（同事务）；**`correct` 为 NULL 的作答（不可判定）不累加**。
**不做**"重算全表"——重算会在删除时产生漂移。

**★ 多知识点聚合口径（必须写死，否则数字会虚高）**：一道题可以挂多个 `kc_ids`，一次作答会对**所有** topic 各累加一次 `attempts` / `correct`。因此：

- **进度视图的"总作答数"必须按 attempt 去重**（`COUNT(DISTINCT attempt_id)`），**不得对 `mastery.attempts` 求和** —— 否则同一份作答被重复计入，总量虚高；
- **"首次正确率"的单位是 `(user, item)` 的第一次 attempt**，不是 `(user, topic)` 的第一次 —— 后者在一题多知识点时无定义。

**已知限制（诚实）**：当前口径是**计数**，不含"提示次数对掌握度的扣减"。F6 要求如实记录，但**掌握度公式本身不在 v2 范围内**（属学习者建模，`docs/01 §9` 不做）。数据齐备，公式后补 —— 这正是"现在开始记"的意义。

#### 其余几张表
`student_user` / `student_token` / `student_qa_log` / `student_hint_log` 的字段沿用已落地实现（`student_hint_log` 为 v2 新增，字段见 §3.1；`student_qa_log` 的增量见上表）；`student_audit` 同理。**字段级机器可读定义在 `contracts/`（A3）**，本篇只记录**本仓的增量**（`asset_version` · `correct` 可空 · `grader` 新增取值 · **`qa_log.provider`/`model`/`egress`** · **`user.must_change_pw`** · **`user.notice_ack_at`**）与口径说明。

★ **`student_user.must_change_pw`（2026-09-16 补位）**：由 §6.1 的"管理员重置密码"引入 —— 置位后**只放行 `POST /api/auth/password`**，其余学习类接口一律 **403 `must_change_pw`**（**不复用 `forbidden`**：那个 code 的语义是"角色不足"，混用会让前端把"去改密码"显示成"无权访问"）。字段级定义在 A3 契约；**它此前只在 §6.1 出现一句，既不在本节的增量清单、也不在 §3.4 对照表、也没有状态机条目**（见 §5.3）。

### 3.3 内容资产：五类、格式与版本化（F20）

| 资产 | 最小规模（首版**下界**） | 必须关联 | 主要消费方 |
|---|---|---|---|
| 题库 | **≥ 30 道**（长期目标 ≥ 100 道，与评测资产共用） | 知识点 ID · 题型 ID · **`gradable` 标记** · ★ **`answer_kind` 标记**（枚举 **`value`** / **`text`**，与 `gradable` 并列；**任一未标记视为资产错误 ⇒ 启动报错退出**，与 §8-14 同一模式）。★ **两者关系写死（2026-09-16）**：`gradable=false` ⇒ **不进判定链**（`correct=NULL` · `grader=not_gradable`，见 §4.3②），`answer_kind` 不参与；**`docs/01 §6` 的"判定准确率"与"判定延迟"只对 `gradable=true` 的题目统计**，"数值题 / 应用题"= `answer_kind` 的 `value` / `text` | 刷题 · 评测 |
| 错因库 | **≥ 40 条** | 知识点 ID | F5 错因诊断 |
| 知识点 DAG | **≥ 40 节点且覆盖 ≥ 5 章**，拓扑无环 | —（节点即 ID） | F9 · F19 · 上溯 |
| 公式库 | ★ **≥ 30 条**（2026-09-16 补下界；原为"—"，使 F18 可被一个**空库**平凡满足） | 知识点 ID · **含摊布下限**（每条 ≥ 1 个知识点，且单一知识点占比 ≤ 30%，口径同 F20） · ★ **名称唯一**（F18 要"按名称返回唯一条目"，重名即资产错误 ⇒ 启动报错退出） | F18 精准检索 |
| 题型与解法套路 | **≥ 15 类** | 知识点 ID | **F17 的"适用模型"** |

★ **`answer_kind` 为什么必须在这里写出来（2026-09-16）**：`docs/01` 的 **F20**、**§6 的"判定准确率"与"判定延迟"两条验收**、以及**附录 C** 都引用了它（`answer_kind=value` / `answer_kind=text`），而**本篇此前全篇没有这个词** —— 于是两条验收读不到自己声明的分类字段，F20 的"题库条目必须带 `answer_kind`"也**没有落点**。

**五条规模下界都是下界，只许上调**（与地板阈值同一精神；公式库 ≥ 30 条为 2026-09-16 补入）。

**存放路径（唯一）**：`content/<asset>.json` —— **顶层目录，与 `contracts/` 并列**。理由：五类资产是**项目级真相**（自制、入仓、可 diff、可 review），而 `backend/data/` 从此是**纯运行时目录**（只装 L1 语料与 L2 库）。把"必须入仓"的东西放进"绝不能入仓"的目录里，会让 §1.3 那句"结构上不可能入仓"变得难以一眼自证。

**统一版本化规则**：

1. 每类资产一个文件（或一个目录 + 索引），**文件头带 `rev`**：`<asset>-<YYYY.MM.DD>.<n>`。
2. 生成物 **`content/MANIFEST.json`（入仓）** 记录**五类资产各自的 rev**，并派生出**组合版本号** `content_rev`。★ **算法已定死（2026-09-16，决策 4B / §11.1-6）**：五类资产**按 ASCII 键名升序**排列，逐个拼成 `key=rev`，以 `;` 连接，取 `sha256(...).hexdigest()[:12]` —— 即 `content_rev = sha256("dag=<rev>;formula=<rev>;mistakes=<rev>;problems=<rev>;types=<rev>")[:12]`。★ **本式同时把五类资产的键名固定下来**：`content/problems.json` · `content/mistakes.json` · `content/dag.json` · `content/formula.json` · `content/types.json`（键名进哈希，**不能随手改**）。★ **两个设计点各有一个理由**：**顺序写死**是因为"按**中文名**排序"会依赖 locale —— 同一份资产在不同机器上可能算出**不同的 `content_rev`**，于是 F7 的版本号不可复现；**用哈希而不是直接拼接**是因为 `attempt.asset_version` 要长期存（F7），12 位十六进制比一串 rev 更稳定地短。★ **反解链不受影响**（见第 3 条）：哈希不可逆，因此反解是"**在 `git log` 里找计算出的 `content_rev` 相同的那一版 MANIFEST**"，再落回五类 rev —— 这一步**可算、可核**，所以第 3 条的"反解链依赖 MANIFEST 入仓 + git 历史"依然成立。
3. `attempt.asset_version` 存**当时的 `content_rev`**；**反解链**：`content_rev` → 同提交的 `content/MANIFEST.json` → 五类资产各自的 `rev` → **`git log` 里升该 rev 的那次提交**（内容与解释一并可查）。
   > 反解**依赖 MANIFEST 入仓 + git 历史**这两件事同时成立。若 MANIFEST 不入仓，旧 `content_rev` 就是一个无法解释的数字 —— 那正是 F7 要防的事。
4. **`rev` 变更即视为内容变更**：改一个错因解释也要升 `rev`。**不允许原地改内容不升版本** —— 否则历史记录的解释链断裂。
5. **一致性由启动校验守住**：资产文件头 `rev` 与 MANIFEST 不一致 ⇒ **启动报错退出**（与 §8 第 14 条同一模式），防止"手改了资产忘了重生成 MANIFEST"静默产出错版记录。

> 资产**入仓**（L0，自制、可版本化、可 diff、可 review）；**教材语料不入仓**（L1，见 §1.3）。
> 这与 N3 不冲突：入仓的是我们自编的题目、错因、DAG、公式、题型；从教材摘录的原文属 L1。

### 3.4 ★ 字段-需求对照表（附录 A 的两条规则）

**规则 1：每个字段指得出它支撑哪个需求 ID —— 说不出的删掉。**

| 字段 | 支撑的需求 | 说不出的？ |
|---|---|---|
| `attempt.attempt_id` | **F7**（记录的主键，F16 的删除读回与 §6.4 的 cursor 分页都按它） | — |
| `attempt.user_id` | **F7** · **F15**（身份归属；越权隔离的判据） | — |
| `attempt.item_id` | F7 · F8（指向题库条目） | — |
| `attempt.expected` · `attempt.asset_version` | **F7** | — |
| `attempt.kc_ids` | **F19** · F8 · F9 · F11 | — |
| `attempt.answer_raw` | F7 · **F5**（错因匹配的输入） | — |
| `attempt.hint_used` | **F6** · F14（值来自 `hint_log`，服务端派生） | — |
| `attempt.hint_used_total` | **F8 · F11 · F14**（跨会话累计，由 `hint_log` 按 `(user,item)` 计数，**不落列**） | — |
| `attempt.mistake_id` · `attempt.mistake_matched` | **F5**（错因落库；`NULL` / `0` = 如实未识别）· F8 · F11 | — |
| `attempt.duration_ms` | F7（**自陈值，不参与验收**） | — |
| `attempt.attempt_no` | F8 · F11（服务端派生） | — |
| `attempt.correct` | F7 · F11（**NULL = 未判定**） | — |
| `attempt.grader` | F4 · N2（`not_gradable` 表示不可判定） | — |
| `attempt.answer_normalized` | F4（判定可复算） | — |
| `attempt.source` | F8（区分练习 / 错题重做） | — |
| `attempt.score` | F7（应用题部分分） | — |
| `attempt.session_id` | **F14**（本会话用了多少次提示） | — |
| `attempt.created_at` | F7 · F8 · F11 + **§6.4 cursor 分页的排序键** | — |
| `hint_log.hint_id`（PK） · `hint_log.user_id` · `hint_log.item_id` | **F6**（服务端提示计数的归属键） | — |
| `hint_log.level` · `hint_log.served_at` | **F6**（服务端提示计数的唯一依据） | — |
| `hint_log.attempt_id` | **F6**（"本级提示是否在本次作答之前取的"的唯一对账依据） | — |
| `mastery.user_id` + `mastery.topic`（PK） | **F11**（**与知识点 ID 同口径**，per-user 聚合） | — |
| `mastery.attempts` · `mastery.correct` | **F11** | — |
| `mastery.updated_at` | **F11**（进度视图的"最后更新"） | — |
| `qa_log.user_id` · `qa_log.created_at` | **F15**（越权隔离）· **N16**（去向查询的分组键）· §6.4 的 cursor 排序键 | — |
| `qa_log.query` · `qa_log.answer` | F7 的问答侧 · F11 的辅助数据源 | — |
| `qa_log.provider` · `qa_log.model` · `qa_log.egress` | **N16**（去向可查；`egress` 枚举 `loopback` / `remote`）· **N15**（告知文案的判据） | — |
| `user.notice_ack_at` | **N15**（每账号一次的**服务端**告知标记 —— 只存浏览器则换台机器就不再告知） | — |
| `user.must_change_pw` | **F15**（管理员重置密码后强制改密，见 §5.3） | — |
| `content/*.answer_kind`（资产字段，非 L2 表） | **F4**（"判定准确率"与"判定延迟"两条验收的分类依据）· **F20** | — |
| `user.password_hash` | **N6**（只存哈希） | — |
| `user.status` · `failed_count` · `locked_until` | **F15**（禁用 / 锁定） | — |
| `token.token_hash` | N6（只存 sha256） | — |
| `audit.*` | **—（§11.1 裁定项，无需求 ID）** —— F15 只要求**可见范围与越权**，不要求留痕。审计是裁定保留下来的合规手段，如实标注，**不硬挂在 F15 上** | — |

> **已删除的字段：`attempt.deleted`。** 它是"**有读无写的死字段**" —— 旧仓里没有任何代码把它置 1，但所有读查询都过滤 `deleted=0`。原表把它记为"F16 误删撤回"，但 F16 是**硬删 + 读回 0 行**，两者不成立。按附录 A 规则 1 只有"删"或"补需求"，本仓选删（见 §3.2）。

**该表已列出的字段全部指得出用途，且没有一个字段是为"页面要显示什么"而加的** —— 附录 A 的规则在第一次执行时就是干净的，因为数据模型是从需求推出来的，不是从界面反推的。
> ★ **口径修正（2026-09-16）**：原写"**这张表里**没有'说不出用途的字段'"，而本表此前**只列了部分字段**（`attempt_id` · `attempt.user_id` · `attempt.item_id` · `attempt.answer_raw` · `attempt.mistake_id` · `attempt.mistake_matched` · `attempt.hint_used_total` · `hint_log.hint_id/user_id/item_id/attempt_id` · `mastery.user_id` · `mastery.updated_at` · `qa_log.user_id/created_at` 都不在表里）—— 那是一句**范围被夸大**的断言，现已按 §3.1 / §3.2 的字段清单补齐，并把断言收窄到"已列出的字段"。**字段全集是 §3.1 的七张表 + §3.2 的字段级设计**。

**规则 2：每条 MUST 需求至少被一个字段或一个接口支撑 —— 说不出的补上。**
逐条核对见 **§10**（不是全部 MUST 都需要新字段：有的靠接口、有的靠规则，但**每一条都必须能指出落点**）。

### 3.5 索引与查询

| 索引 | 服务的查询 |
|---|---|
| `user(role, status)` | 管理页按角色 / 状态筛选 |
| `token(user_id, revoked)` | 撤销某用户全部令牌 |
| `audit(actor_id, created_at)` · `audit(target_id, created_at)` | 审计按人 / 按被查看者 |
| `qa_log(user_id, created_at)` | 我的问答历史（**cursor 分页的主键序**） |
| `attempt(user_id, created_at DESC, attempt_id)` | 错题本 / 进度 + **cursor 分页**（三者齐全才能保证同秒记录不乱序） |
| `attempt(user_id, item_id)` | 同一题的第几次（`attempt_no` 由它派生） |
| ★ **`UNIQUE(user_id, item_id, attempt_no)`** | **是约束不是索引（2026-09-16 补）**：`attempt_no` 由 `COUNT(...)+1` 派生（§3.2），**并发下两个请求会派生出同一个值**，而没有这条约束就会静默写出两条同为"第 3 次"的记录 —— 而 F8 / F11 与 §6.4 的分页都靠它排序。**冲突时重派生一次，仍冲突则返回 409 `conflict`（不得静默覆盖）** |
| `hint_log(user_id, item_id, session_id)` | **`hint_used` 的服务端计数**（F6，会话域） |
| ★ **`hint_log(user_id, item_id)`** | ★ **`hint_used_total` 的跨会话累计**（F8/F11/F14）—— 只按 `(user,item)` 计数时前一条索引用不上前缀之后的部分，须单独建 |
| `mastery(user_id, topic)`（PK） | 薄弱点定位 |

### 3.6 并发与事务（R-B 的落地）

| 规则 | 做法 | 为什么 |
|---|---|---|
| 并发写 | 开启 **WAL** + `busy_timeout=5000` | 读写不互相阻塞；短时争用自动重试而非立即报错 |
| 建表 | **只有一个建表入口**，幂等 | 分两处建表必出"某个入口漏建了表"，症状是运行时的 `no such table` |
| 写事务 | 每次写**显式 `commit()`**，并集中在一个 `with closing(conn)` 里 | 旧仓真实 bug：写函数漏一次提交，作答记录被**静默回滚** |
| 复合写 | `作答 + 掌握度 + 提示流水的计数读回` 的联动写在**同一事务**内（★ **口径修正（2026-09-16）**：`attempt.hint_used` 是**派生值**，原文"提示计数读取"读起来像在更新一个存量计数） | 否则会出现"记了作答但掌握度没动"或"用了 3 次提示但记成 2 次"的半成品状态 |
| 测试 | **必须读回断言**，不得只断言"HTTP 200" | 同上的 bug 当时没被发现，就是因为测试只看了状态码 |
| ★ **备份取快照**（N19 的机制，2026-09-16 补） | 开了 **WAL** 之后，备份**不得用文件级 `cp`**（会漏掉未检查点的 WAL 页 = "备份成功但少了最近的作答"）—— 必须用 **`sqlite3.Connection.backup()`**（Python 3.7+，与"stdlib `sqlite3`、无 ORM"一致）或 `VACUUM INTO` 产出**一致快照** | F16 靠"读回 0 行"验收、N19 靠"逐表读回"验收；**文件级拷贝会让 N19 平凡通过**（文件在、内容缺），与"写库漏提交"是同一类事故 |

---

## 4. 三条主链路

### 4.1 登录与令牌链路（F15 · N6）

```
浏览器                        后端                         SQLite(L2)
  │                            │                              │
  │ POST /api/auth/login       │                              │
  │ {username, password}       │                              │
  ├───────────────────────────▶│                              │
  │                            │ ① 查 user                    │
  │                            ├─────────────────────────────▶│
  │                            │ ② 不存在 / 已删除 / 空哈希？  │
  │                            │    ⇒ burn_time() + 401       │
  │                            │ ③ bcrypt.checkpw             │
  │                            │    失败 → failed_count+1     │
  │                            │    达上限 → 置 locked_until  │
  │                            │    且**校验失败也烧时间**     │
  │                            │    ⇒ 401（或 423，见下）      │
  │                            │ ★ ④ 锁定中？→ 423            │
  │                            │ ★ ⑤ status=disabled？→ 403   │
  │                            │ ⑥ secrets.token_urlsafe(32)  │
  │                            │    库里只存 sha256(明文)      │
  │                            ├─────────────────────────────▶│
  │ {token, expires_at, user}  │◀─────────────────────────────┤
  │◀───────────────────────────┤                              │
  │                            │                              │
  │ 后续请求：Authorization: Bearer <token>                    │
  ├───────────────────────────▶│ resolve_token(sha256)        │
  │                            │ → revoked/过期/账号禁用 ⇒ 401/403
```

> ★ **③④⑤ 的次序是刻意的**：**锁定与禁用的判定都在密码验证之后**。
> 反例（旧仓实测的次序）：把"锁定中 → 423"放在验证**之前**，则"不存在的用户名"给 `401`、而"已锁定账号"给 `423` —— **无需密码即可区分**，构成账号存在性枚举面。旧仓的禁用检查本就在验证之后（这点是对的），是本篇先前把图画错了，已按实现改正。

**七条口径（每条都对应一个具体的坏结果）**：

| # | 口径 | 不做会怎样 |
|---|---|---|
| 1 | **服务端会话令牌，不是 JWT** | JWT 无状态 → 无法满足"停用账号**立刻**生效" |
| 2 | 库里**只存 sha256**，明文只在登录响应出现一次 | 库被拿走等于所有会话被劫持 |
| 3 | 令牌 **7 天**有效，可即时撤销（`revoked`） | 无法响应"这孩子手机丢了" |
| 4 | **改密码 / 禁用 / 删除 ⇒ 撤销该用户全部令牌** | 改完密码旧令牌还能用，等于没改 |
| 5 | 失败 **5 次锁 60 秒**；**校验失败也烧固定时间** | ① 可无限爆破 ② 通过响应时间可枚举用户名是否存在 |
| 6 | 除 `health` / `login` / 静态资源外，**全部端点要求登录** | **A1 已裁决：不设免登录例外**（理由见 `docs/01` §1） |
| 7 | 管理员账号**只能由 CLI 创建**，接口不可创建管理员 | 否则拿到一个管理员令牌就能自我复制 |

> ★ **信息量不变量（可断言）**：**在密码通过之前，一切失败都返回同一个 `401 bad_credentials`，且都烧等长的时间。**
> 因此"账号已被禁用（403）""尝试次数过多（423）"这两个可区分的状态，**只可能在密码验证通过之后出现**。这条不变量是 §6.3 两个 `code` 拆分成立的前提，也是它唯一的正当性依据 —— 测试直接断言它。

### 4.2 答疑链路（SSE · F1 F2 F3 F10 F13）

```
POST /api/chat  { query, context }        ← 必须登录（F15）
   │
   ├─ ① 校验：query 长度上界（R-A）· context 可缺省但显式降级（F1）
   ├─ ② 落 qa_log（提问原文，L2）—— 先落再答，保证"问了就算数"
   │
   ▼  graph（LangGraph 图运行时）
   ├─ 路由 → 意图（solve / explain / lookup / visualize）
   ├─ 检索（本地语料）→ 命中带出处；**无命中如实说明**（F10 · N2）
   ├─ 求解 → 逐步过程，每步 {说明, 式子}（F2 结构断言，**仅 solve 类**）
   ├─ 验证 → **验证报告**（级别 A–D + 逐项检查 + 置信度）—— 每条解答必带（F3）
   │      ├─ 通过 → 正常输出
   │      └─ ★ **不通过** → 重算一次（**上限 1 次**）
   │             ├─ 重算后通过 → 正常输出
   │             └─ 仍不通过 → **标 `uncertain`** + 级别降 **D** + 事件流**显式告知**
   │                             （附"哪一项检查没通过"）—— **不静默输出，也不假装通过**
   ├─ 需要人协同？→ hitl.request（挂起，等前端确认）
   └─ 汇总 → 事件流
   │
   ▼ SSE 帧（`events.schema.json`）
   │   frame: { seq, type, ts, payload }   seq 单调递增
   ▼
前端消费：seq 去重 · 按类型渲染 · **断流后重新发起（见硬性设计 1）** · 不重放已渲染帧
```

**八条硬性设计**：

1. ★ **`seq` 只用于去重，不承担"断流续传"（2026-09-16 修正）** —— 服务端**不做重放缓冲**，断流语义为"**重新发起（会重跑一次图）**"。**已记账：因此不能续传**。理由：内存环形缓冲在单 worker 下能工作，但要为"断流"引入一套会话态生命周期，而 v2 的 SSE 是**短连接**（一次答疑一次流），收益与复杂度不成比例。若将来改做缓冲，须走 ADR。
2. **客户端断开 ⇒ 图必须取消**。否则一次刷新页面会留下一个还在跑的后台图，把单 worker 拖死。
3. **验证报告缺失即不出结果**（F3："无标签不出结果"）—— 这是 N2 的机制，不是前端的事。
4. ★ **验证不通过有明确出口**（本轮补齐）：**重算一次（上限 1 次）→ 仍不通过就标 `uncertain` 并降级到 D ＋ 显式告知**。
   这是 **N1 的机制所在** —— N1 的验收是"注入的已知错解被拦住或标注"，而"拦住"就发生在这一步。没有这一条，N1 只是一句指标，没有对应设计。
   ★ **"重算"的语义已写死**：**换一条策略重新求解**（多路求解的第二次尝试），**不是**同一次调用的重试采样 —— 同一次调用的多个样本会共享同一个错误前提。上限为 1 次：无限重算会把"验证"退化成"直到算对为止"。**"哪些检查项的什么结果算不通过"见 §4.4 的判定表。**
5. **`hitl.request` / `hitl.resolved` 必须成对**，且前端必须有可见的挂起态 —— 否则用户面对一个永远不动的进度条。
6. **上下文来源可替换**（N12）：`context` 是一个**显式可选参数**，不是"从浏览器读屏"。将来探针接入时，它只是换一个来源，**契约不变**。
7. ★ **F2 的结构断言有明确落点**：解答步骤以 **`steps[]`** 出现在两处 —— ① SSE 的解答事件（`events.schema.json` 里承载步骤数组的那个字段）② **`GET /api/runs/{id}` 的响应**（`steps: [{说明, 式子}]`，★ 该端点此前**不在 §6.1 的路由清单里**，2026-09-16 补入）。**断言的对象是这两处**，不是"事件流里大概有步骤"。仅约束 **solve 类**解答 —— 路由枚举的四个意图（`solve` / `explain` / `lookup` / **`visualize`**）里**只有 `solve` 是解答**，其余三个不受"步骤数 ≥ 3"约束（`visualize` 原未列入排除项，2026-09-16 补）。
8. ★ **答疑链路的同步 CPU 工作一律 `to_thread`（2026-09-16 补，N20 连带）**：判定链早有这条（§4.3-1 · §8-7），**答疑链没有** —— 而 N20 承诺的是 **10 路并发答疑**。图运行时里的同步部分（求解 / 符号计算 / 语料检索）若占住事件循环，10 路并发会退化成串行，`/api/health` 也会跟着变慢（§8-7 的断言会当场失败）。**判定 / 答疑两条链路的同步工作都必须丢线程池**，这是 N20 ④ 的前提。

### 4.2.1 答疑的前置条件（N14 —— 无凭据则不可用）

```
有凭据 + 端点在信任范围内 → 走上面的链路
无凭据                   → ★ 503 `model_key_missing`   （引导去设置页填凭据）
端点不可达 / 超时 / 被拒绝 → ★ 503 `model_unreachable`   （**可重试**，**不得引导填凭据**）
凭据无效 / 被撤销         → ★ 400 `model_key_invalid`   （客户端可修的输入问题）
额度不足 / 欠费           → ★ 402 `model_quota_exceeded`
                          → 四者一律**不发起、不降级、不产出假答案**
                          → 判定 / 错题本 / 进度 / 知识图谱**全部照常可用**（它们不依赖模型）
```

> **为什么连 mock 都不留**：旧仓的 `llm_provider = "auto"`（无 key 走 mock，保证零配置可跑）是**产品模式**，与 **N2 诚实**直接冲突 —— mock 产出的是**假答案**。v2 只在**测试**里注入假 provider，**不提供产品级 mock**。这条要写死，否则会有人把 mock 重新加回来。
> ★ **为什么"未配置"与"不可达"必须分成两个 code（2026-09-16）**：`ADR-0005 §5` 已裁定拆分，而本节与 §6.3 此前仍是合并的 `model_unavailable`，前端行为被写成"显示'未配置模型凭据'并提供 `#/settings?tab=model` 入口" —— 于是**断网的学生被推去填 key**（他会反复重填一个本来就正确的凭据），而真正该做的是"重试"。这两个状态的**用户动作完全相反**，因此不能共用一个 `code`。
> **凭据怎么传**：请求头 `X-Model-Key`（**不放 body**，避免进 body 日志）；因此答疑**必须用 `POST + fetch 流`** —— `EventSource` **不能带自定义头**。若将来改用 `EventSource`，凭据必须改走 cookie 或 body（已列为失败模式，见 §8）。
> ★ **P1（本机端点）下的空凭据（2026-09-16 补）**：本机端点**没有真实凭据**，因此 `X-Model-Key` **允许为空串**（仅 P1 单机形态）；此时 `egress='loopback'`，N15 文案为"数据不发往第三方"。**空串同样受 R-M 约束**（不得落日志 / 审计 / 错误信息 / URL / 载荷本体）—— 这一条是 N16 验收① 断网演示的前提，此前无落点。

#### 端点信任边界（N16 · `ADR-0005 §4` —— 2026-09-16 落进正文）

**端点由用户提供（N14），因此服务端会向一个用户可控的 URL 发起请求 —— 这是一个真实的 SSRF 面。** 六条规则，**`/api/model/ping` 与 `/api/chat` 必须共用同一个校验函数**（否则"用 ping 绕过校验再直接用 chat"就是一条现成的旁路）：

| # | 规则 | 为什么 |
|---|---|---|
| 1 | **预置供应商白名单**（DeepSeek / Qwen / GLM + 自定义 OpenAI 兼容），自定义时仍须过第 2–4 条 | 只靠"用户填什么就是什么"等于把出网权交出去 |
| 2 | ★ **loopback 例外仅在单机形态**，且**必须由部署者显式勾选**才生效（与 N18 的"单机允许 http"**共用同一开关**） | 远程托管下学生填的 loopback 是**服务器**的 loopback：他既无正当性使用它，它又是一条通往服务器内部服务的路 |
| 3 | ★ **强制 `https`**（单机 loopback 例外）；★ **禁 RFC1918 / 169.254.169.254 等内网与云元数据地址** | 只挡 loopback 不够：`10.0.0.0/8`、`192.168.0.0/16`、**云元数据端点**都是同一类目标，而元数据端点能直接读出托管环境的凭据 |
| 4 | **禁跟重定向** | 否则第 2、3 条可以被一个 302 绕过去（校验的是第一个 URL，请求最终落在另一个地址） |
| 5 | **超时与响应体上界**（连接 / 读取超时 + 最大响应字节） | 用户可控的端点可以让服务端一直挂着 —— 单 worker 下这就是一次拒绝服务 |
| 6 | **校验失败一律 503 `model_unreachable`**（不回显解析到的 IP、不回显内部错误） | 把"这个地址被拒绝"与"这个地址不可达"**对外合并**，避免把内网探测结果回吐给用户 |

> ★ **这六条此前只在 `ADR-0005` 里有，本篇零落点**（2026-09-16 补）。ADR 是决策记录，**执行面必须在正文**，否则实现只看本篇就得到"只挡 loopback"这一条。

### 4.3 刷题判分链路（F4 F5 F6 F7 F8 · N1）

```
GET  /api/problems?kc=...&limit=&cursor=      ← 题目来自内容资产（F20）
     ★ **响应绝不包含 `expected` / 解析 / 错因答案**（见下"暴露面")
POST /api/practice/hint                        ← ★ 提示阶梯的唯一推进口（F6）
     { item_id, session_id }
     → { level, content, hint_used }           ← hint_used 由服务端计数后回传
POST /api/practice/grade
     { item_id, answer, duration_ms, session_id }
     ★ 请求体里**没有** hint_used / attempt_no —— 两者都由服务端派生
   │
   ├─ ① 参数校验：answer **max_length=200**（R-A）· 数值上界
   ├─ ② 读 `gradable`（F20）：为假 ⇒ **显式降级**
   │      → 200 { graded: false, reason: "not_gradable", message: "该题需人工判定" }
   │      → 仍写 attempt，但 correct=NULL、grader='not_gradable'、**mastery 不累加**
   ├─ ③ 归一化（domain 纯函数）：去空白 / 统一符号 / 精度口径
   ├─ ④ 判定（domain 纯函数，**不依赖 LLM 往返**，F4）
   │      ├─ 正确 → correct=1
   │      └─ 错误 → 错因匹配（吃错因库，F5）
   │            命中 → 输出对应错因解释
   │            未命中 → **如实说"未识别错因"**，不得编造
   ├─ ⑤ 定级：本判定为**确定性**，验证级别按手段封顶（附录 C）
   ├─ ⑥ 服务端派生（★ 权威值，不读请求体）：
   │      hint_used  = COUNT(student_hint_log WHERE user,item,session)   ← F6
   │      attempt_no = COUNT(student_attempt WHERE user,item) + 1        ← F8 · F11
   │
   ├─ ⑦ **同一事务**写入：
   │      attempt（含 expected 快照 + asset_version + created_at）  ← F7
   │      mastery（(user_id, topic) 累加；correct 为 NULL 时跳过）    ← F11
   │      显式 commit()                                             ← R-B
   ▼
GET  /api/me/wrong?kc=...&limit=&cursor=      ← 跨会话读回（F8）
GET  /api/me/progress                          ← 进度 / 薄弱（F11，数据源=attempt）
```

**六条硬性设计**：

| # | 设计 | 对应 |
|---|---|---|
| 1 | 判定**全部走确定性代码**，且是**同步 CPU 工作 ⇒ 必须丢线程池** | F4 · **旧仓 P0 事故**：`/api/practice/grade` 曾冻结整个事件循环 |
| 2 | 判定正则**禁止嵌套量词**；对长数字串有专门测试 | R-A · 旧仓实测：100k 位数字耗时约 2 分钟 |
| 3 | 提示分级由**独立接口**推进，**每次调用写一条 `hint_log`**；`hint_used` 由服务端计数；**未提交前不返回完整解析**（完整解析只在 `grade` 之后可取） | F6 · §5 伦理约束 |
| 4 | 判定接口**只读题目、不写题库**；内容资产是只读的 | F20（资产一致性） |
| 5 | ★ **列表接口的答案暴露面必须封死**：`GET /api/problems`（以及任何列表 / 详情接口）**不得返回** `expected`、解析步骤、错因解释或任何能反推答案的字段 | **§5 伦理约束在 UI 之下同样是敞开的** —— 只要列表接口带标准答案，前端不显示就只是"没做"，谁都能直接读接口 |
| 6 | ★ **`hint` / `answer` / `attempt_no` 的权威都在服务端**：URL 里的 `hint` 层级只用于渲染；改 URL 不能让服务端认为已取过提示；请求体里传 `hint_used` 会被**忽略**（不是报错，避免旧客户端崩），但**不采信** | F6 · §5 |

★ **`POST /api/practice/grade` 的响应里必须有 `solution`（2026-09-16 补位）**：§4.3-3 与 §5.2 都写死"**完整解析只在 `grade` 之后可取**"，但此前**全篇没有说明解析从哪来** —— 提示阶梯的最后一格因此没有出口。响应形状（字段级定义在 A3）：

```
{ graded, correct, score, mistake_id, mistake_matched,
  hint_used, attempt_no,
  solution: { steps: [{说明, 式子}], cite_id: <string|null>, explanation } | null }
```

**`solution` 只在本次请求确实写入了一条 `attempt` 之后才非空** —— 不可判定题目（§8-18）、参数校验失败、未登录时为 `null`。这条把"未提交前不返回完整解析"从**前端纪律**变成**接口事实**。

---

### 4.4 验证判定表（★ 2026-09-16 新增 —— `§4.2-4` 指向的就是本表，也是 N1 的机制所在）

`§4.2-4` 写"验证不通过 → 重算一次 → 仍不通过就标 `uncertain` 并降 D"，但**"什么算不通过"全篇没有定义** —— 于是 **N1**（唯一的技术差异化，MUST）的"拦住"判据没有载体，`docs/01 §6` 的"诚实性"与"错误拦截率"两条验收同时悬空。本表把它写死。

**① `level` 的取值与 `cap(level)`** —— 置信度上限表，同时也是 `docs/01` 附录 A 的待落地项（**B 级在这里补齐**）：

| level | 含义 | 可达条件（**手段封顶**） | `cap(level)` |
|---|---|---|---|
| **A** | 纯数值，可完全验证 | `checks` 里**必须出现** `sympy_recompute` | **1.0** |
| **B** | 符号可强验证 | `checks` 里**必须出现** `symbolic_equivalence` | **0.90** ★ |
| **C** | 应用题，可交叉验证 | `checks` 里**必须出现** `cross_check` 或 `unit_check` | **0.85** |
| **D** | 证明题 / 开放题，**不做承诺** | **无条件可达**（兜底级） | **0.4** |

- ★ **B = 0.90 是 2026-09-16 补的**：旧仓 schema 只定义了 A / C / D，**B 没有上限** ⇒ `docs/01 §6` 的 `conf ≤ cap(level)` **全量断言无法执行**（一条无法执行的 MUST 断言）。取值理由：它**必须低于 A 的 1.0**（符号等价性判定本身有实现风险，弱于数值重算），且**高于 C 的 0.85**（符号等价是**强验证**，不是交叉印证）—— 0.90 使三档严格单调。
- **两条断言都执行**：`level ≤ 手段允许的上限`（挡"一个手段都没跑却报高级别"）**且** `conf ≤ cap(level)`（挡"高级别配低置信"）。

**② 逐项检查 × 结果 → 判定**（`checks[]` 的每一项都必须落到下表的某一行）：

| 检查项 | 结果 | 判定 |
|---|---|---|
| `sympy_recompute` | 与解答不一致 | **不通过** → 触发重算 |
| `symbolic_equivalence` | 不等价 | **不通过** → 触发重算 |
| `cross_check` | 与第二条路径不一致 | **不通过** → 触发重算 |
| `unit_check` | 量纲不符 | **不通过** → 触发重算 |
| **任意检查项** | **未执行**（手段缺失） | ★ **不算"不通过"，但也绝不计入"通过"** ⇒ 该报告的 `level` 按 ① 的手段封顶**下压**，且**必须在验证报告里显式列出缺哪一项**（N2："不得笼统说已校验"） |

**③ 出口（与 `§4.2-4` 一一对应）**：

| 情形 | 行为 | 对 N1 注入集的含义 |
|---|---|---|
| 全部检查通过 | 正常输出，`level` = 手段可达的最高级 | — |
| **任一项不通过** | **重算一次**（换一条策略重新求解，**上限 1 次**） | 注入的**系统错解**在这一步被拦住 |
| 重算后通过 | 正常输出，但验证报告里**如实记"重算过一次"** | 计为"被拦住" |
| 重算后仍不通过 | **标 `uncertain` + 级别降 `D` + 事件流显式告知（附未通过的检查项）** | 计为"被标注" |
| **手段缺失**（既无 `sympy_recompute` 也无 `symbolic_equivalence`） | 照常输出，但 `level` 只能到 **C 或 D**，且报告里显式列出缺失项 | ★ **这是"走了流程却一步真验证都没做"的可见化** —— 旧仓级别恒为 D，正是这个形态被写成了"验证通过" |

> ★ **无限重算被明确拒绝**：上限 1 次的理由是"重算"= **换一条策略**，而不是同一次调用的重试采样（多个样本共享同一个错误前提）。上限放开会把"验证"退化成"直到算对为止"，而 **N1 的验收恰好是"允许标注，不要求修好"**。
> ★ **本表是 `docs/01 §6`"诚实性"行的执行载体**，也是 **§9.1 注入集**断言的对象：注入的系统错解，必须落在 ③ 的"被拦住"或"被标注"两行之一。

---

## 5. 状态机

### 5.1 编排图状态机

```
idle ──▶ routing ──▶ running ──┬──▶ verifying ──┬──▶ done
                      ▲         │                │
                      │         │                └──▶ needs_human ──▶ hitl
                      │         │                                    │
                      └─────────┴──◀──────── resolved ◀──────────────┘
                                │
                                └──▶ failed（错误体见 §6.2）
```

**约束**：`done` / `failed` 为终态；`needs_human` **必须有超时**（否则图永远挂着）。
**已知限制（诚实）**：图检查点当前为**内存态** ⇒ **只允许单 worker**。见 §8 的失败模式表。

### 5.2 提示阶梯状态机（F6 —— 伦理约束的落地点）

```
        未作答
          │ 请求提示 1
          ▼
    hint_used=1 ──「方向」**不给完整思路**
          │ 请求提示 2
          ▼
    hint_used=2 ──「步骤骨架」**不给最终答案**
          │ 提交作答
          ▼
        已提交 ──▶ 此时才允许返回**完整解析**
          │ 错题重做（source=wrong_review）
          ▼
        重新作答（attempt_no+1，hint_used **重新计**）
```

**三条不可违反**：① 未提交前**不返回完整解析**；② 每次提示**精确 +1**（这是"如实记入掌握度"的依据）；③ 重做时 `hint_used` 重新计，但**历史不清零**（否则"我第三次才做对"的证据就没了）。

### 5.3 账号状态机（F15）

```
          创建(CLI)              禁用(管理员)
   (无) ──────────▶ active ◀───────────────▶ disabled
                     │  ▲                        │
        删除(本人/管理员) │  │ 启用(管理员)          │
                     ▼  │                        │
                  deleted ◀───────────────────────┘

   任一状态下的失败计数：failed_count≥5 ⇒ locked_until 置未来时刻（60s 后自动解除）
```

**`deleted` 是脱敏态，不是物理删除**：`username` 改为 `deleted_<8hex>`、`password_hash` 置空串（**任何密码都登不进去**）、令牌全撤；**四张学习表**（问答流水 / 提示流水 / 作答 / 掌握度）的数据**硬删**。

★ **`must_change_pw` 的自环（2026-09-16 补）**：管理员重置密码后置位 ⇒ **登录成功，但只放行 `POST /api/auth/password`**，其余学习类接口一律 **403 `must_change_pw`**（**不复用 `forbidden`** —— 它的语义是"角色不足"，混用会让前端把"去改密码"显示成"无权访问"；这与 §6.3 把"禁用"和"越权"分两个 code 是**同一条原则**）。改密成功后清位，并按 §4.1 的第 4 条**撤销该用户全部令牌**。

```
        active ──(管理员重置密码)──▶ must_change_pw=1
           ▲                                  │
           │                    （只放行 POST /api/auth/password）
           └──────────(改密成功 ⇒ 清位 + 撤销全部令牌)──────────┘
```

> **为什么不能只靠前端跳转**：管理员重置密码的**唯一意义**是"学生必须自己换一个密码"。若这个位只在登录响应里回一次、由前端决定跳不跳，那么**直接调任意学习类接口就绕过去了** —— 于是管理员**始终知道学生的密码**，重置密码反而成了一个降低安全性的功能。`must_change_pw` 必须由服务端在每个请求上判定，**这是它的全部价值**。

> **两个 `deleted` 不再同名易混**：`student_user.status = 'deleted'` 是**账号状态**（保留，脱敏态）；`student_attempt` 的软删字段**已删除**（见 §3.2 —— 它是"有读无写的死字段"）。
>
> **username 复用规则**：脱敏后原 username **可以再次被注册**（不引入"退休用户名表"），因为 `user_id` 不复用；**教师视角靠 `user_id` 后 8 位区分同名**（管理页必须显示它）。若将来发现"同名不同人"造成实际混淆，再改为禁止复用并走 ADR。

### 5.4 令牌状态机

```
issued ──▶ active ──┬──▶ expired（到期）
                    ├──▶ revoked（改密 / 禁用 / 删除 / 主动登出）
                    └──▶ orphan（账号被删除后，行被级联清理）
```

---

## 6. 接口与错误语义

### 6.1 路由清单（按模块）

| 模块 | 端点 | 认证 | 备注 |
|---|---|---|---|
| 系统 | `GET /api/health` | **否** | 唯一无需登录的 API；★ **只报告本地事实**：DB · **L1 缓存** · **资产 `rev`**。★ **不报"模型状态"（2026-09-16 修正）** —— 那需要一次出网调用，会与 §1.5③ 的"恰好两个口"、§8-7 的"health < 100ms"**同时冲突**；且 N14 下**不存在部署级端点**，health 无从知道任何用户的模型 |
| 身份 | `POST /api/auth/login` | 否 | 唯一另一个例外 |
| 身份 | `POST /api/auth/logout` · `POST /api/auth/password` | 是 | 改密后撤销全部令牌 |
| 身份 | `GET /api/me` · `GET /api/me/wrong` · `GET /api/me/progress` · `DELETE /api/me/data` | 是 | **学生只见自己**（F15） |
| 身份 | ★ **`GET /api/me/qa`**（**我的问答历史**） | 是 | ★ **2026-09-17 补入**：`qa_log` 此前**没有任何读取路径**（`docs/04 §9` 缺口 1）⇒ **契约 C9 悬空**（违反承诺 12）。负责人裁定：**学生自己可见**（与错题本并列）。走 **C2 分页 + C9 流水条目**；读取用 §3.5 的 `qa_log(user_id, created_at)` 索引。它是 §3.4 所说「F7 的**问答侧**」的真实出口 |
| 身份 | ★ **`GET /api/me/notice` · `POST /api/me/notice`** | 是 | ★ **N15 的读写口（2026-09-16 补位）**：`GET` 返回"这次要不要告知"（**判据是去向 `egress`**，见 §1.5①）；`POST` 写 `student_user.notice_ack_at`。**此前 N15 只有字段、没有接口** —— §10 甚至把它的落点指向了 §7.1 |
| 答疑 | `POST /api/chat`（**`fetch` 流，非 EventSource**；请求头带 `X-Model-Key`） | 是 | 事件 `seq` 单调（**只用于去重，不承担断流续传** —— §4.2-1）；**无凭据 ⇒ 503 `model_key_missing`**（§6.3） |
| ★ 运行 | **`GET /api/runs/{id}`** · ★ **`POST /api/runs/{id}/hitl`** | 是 | ★ **2026-09-16 补位**：前者是 **F2 结构断言的第二处落点**（`steps: [{说明, 式子}]`，§4.2-7）；后者是 §4.2-5 要求**成对**的 `hitl.request` / `hitl.resolved` 的**应答口**。**两者此前都被正文引用，却都不在本表里。** ★ **run 生命周期已裁定（2026-09-16，决策 2A / §11.1-4）**：run **只存在于进程内存**（§5.1 的图检查点），**进程重启后 `GET /api/runs/{id}` 返回 404 `not_found`**，**不做持久化** —— 与 §4.2-1 的"服务端不做重放缓冲、断流即重新发起"自洽：客户端本来就不能靠 run 续传，因此"重启后 run 消失"是**可接受语义，不是缺陷** |
| ★ 模型 | **`POST /api/model/ping`** · `GET /api/model/providers` | 是 | `ping` 校验用户凭据（避免"填错 key 后每次答疑都失败"）；`providers` 返回预置供应商（DeepSeek / Qwen / GLM + 自定义 OpenAI 兼容） |
| 刷题 | `GET /api/problems` · **`POST /api/practice/hint`** · `POST /api/practice/grade` | 是 | 判定确定性 + 线程池；**`problems` 不返回答案**；`hint` 是提示阶梯唯一入口（F6） |
| 知识 | `GET /api/knowledge/tree` · `GET /api/knowledge/formula` | 是 | 吃 DAG / 公式库（F18）；**`formula` 按名称返回唯一条目**（名称唯一性由资产校验保证） |
| 管理 | `GET /api/admin/students` · `GET /api/admin/users` · `GET /api/admin/audit` · `PATCH/DELETE /api/admin/users/{id}` · `POST /api/admin/users/import` | 是（**管理员**） | 一律写审计；`PATCH` 含 **重置密码**（见下） |

**四处端点差异必须写清，否则契约会各写各的**：

- `GET /api/admin/students` = **数据视图**，是 §5"教师可见"的落点；`GET /api/admin/users` = **账号管理**（角色 / 状态 / 锁定 / 令牌）。两者的人**是同一批**，但关注点不同，**不要合并成一个接口**。
  ★ **"数据视图"必须写成可判定的形状（2026-09-16 修正）**：管理员可**逐条查看**本机学生的**作答记录与掌握度计数**（允许排序与筛选）；**不产出任何跨学生的派生指标** —— 界面上**不得出现平均分、排名、分布、热力图**。原措辞写的是"班级统计"，而 `docs/01 §9` 把"跨用户聚合"列为**排除项** ⇒ **同一个词在两侧意思相反，边界就没有执行载体**；改为上面这句后，可以**对着界面点数**。
- `POST /api/admin/users/import` **只能建 `student` 角色，且不接受学号 / 姓名字段** —— 否则一个便利功能就能破掉 N6（`docs/01` 已记账，ADR-0002 §v2 增量 6）。
- ★ **`PATCH /api/admin/users/{id}` 含"重置密码"（2026-09-16 补）**：**强制下次登录改密**（`must_change_pw`）+ **撤销该用户全部令牌** + **写审计**；新密码由管理员经**单独渠道**告知学生。此前这一项在 ADR-0002 决策 3 里有、在 v2 接口清单里**悬空**。

**R-A 强制的字段**（所有请求体字符串字段必须有上界）★ **本表已补齐（2026-09-16）** —— 原文只有五行，而规则声称"**所有**请求体字符串字段"，**规则的范围 ⊃ 检查表**：

| 字段 | 上界 | 理由 |
|---|---|---|
| `username` | 32 | 契约正则 `^[A-Za-z0-9_.-]{3,32}$` |
| `password` | 72 字节 | bcrypt 输入上限 |
| `answer` | **200** | 旧仓 P0：无上界 ⇒ 判定被喂 100k 位数字 |
| `query` | **2000** | 答疑提问；超长直接 400 |
| `display_name` | 64 | — |
| ★ `context.text`（F1 的上下文，可含教材片段） | **4000** + **嵌套深度 ≤ 3** | ★ **比 `query` 更危险**：它是**常用路径**（F1 明确要求无上下文时显式降级），也是**最容易被塞进大对象**的参数（`{"text": "<1MB>"}`） |
| ★ `session_id` | 64 | 出现在 `hint` 与 `grade` 两处 |
| ★ `item_id` | 64 | 同上 |
| ★ `cursor` | 256，**且必须校验可解码** | 不校验就是一个**注入面** |
| ★ **`base_url`**（N16 的端点） | **2048**，且 **`http(s)` 白名单 + 主机校验**：必须 `https`（**仅单机形态的 loopback 例外**）· **禁 RFC1918 / 169.254.169.254 等内网与元数据地址** · **禁跟重定向** | ★ **这是 SSRF 的输入面**：§4.2.1 的六条信任边界要施加的**就是它**，而它此前**不在这张表里**（表声称覆盖"所有请求体字符串字段"） |
| ★ **`provider`** | 32，**枚举**（预置供应商 + `custom`） | 非枚举值会让"预置白名单"这条规则形同不存在 |
| ★ **`model`** | 128，**字符集受限**（不得含控制字符 / 路径分隔符） | 它会拼进上游请求的 body，不做字符集约束就是一个注入面 |

> ★ **"静态扫描"的实现方式必须写明**（否则门禁本身不可 review —— 这与 §9 对运行器"必须可 review"是同一条标准）：扫描**请求体模型的字段声明**（Pydantic 层），断言**每个 `str` 字段都带 `max_length`**；对正则，断言判定类正则**不含嵌套量词**（`(...)+` / `(...)*` 内部再有量词）。

### 6.2 统一错误体（`error.schema.json` —— 旧仓的缺口之一）

```json
{
  "error": {
    "code": "unauthenticated",
    "message": "未登录或登录已过期，请重新登录",
    "detail": { "field": "password", "reason": "too_short" }
  }
}
```

| 字段 | 含义 | 约束 |
|---|---|---|
| `code` | **机器读的稳定标识**（枚举，见 §6.3） | 只增不改；新增须走契约变更 |
| `message` | **给人看的中文说明** | 不含内部路径 / SQL / 堆栈 |
| `detail` | **可选**结构化补充（哪个字段错了、缺什么） | 不得放敏感值 |

**为什么要有它**：旧仓每个接口各写各的形状（有时是字符串，有时是对象，有时干脆是 500），前端只能按形状猜。**统一错误体是前端能写出一套错误处理的前提**。

### 6.3 状态码与 `code` 语义表

| HTTP | `code` | 触发条件 | 前端行为 |
|---|---|---|---|
| 400 | `invalid_argument` | 参数校验失败（含超长、格式） | 高亮字段 + 显示 `message` |
| 401 | `unauthenticated` | 无令牌 / 令牌无效 / 已过期 / 已撤销 | **清本地令牌 + 跳登录** |
| 401 | `bad_credentials` | 登录时用户名或密码不正确 | 显示提示，**不清空用户名** |
| 403 | `forbidden` | **非管理员访问 `/api/admin/*`**（角色不足） | 显示"无权访问"，**不跳登录** |
| ★ 403 | **`must_change_pw`** | **管理员重置密码后、改密前访问任何学习类接口**（F15 · §5.3） | **强制跳设置页改密**；★ **不清本地令牌**（令牌本身有效） |
| 403 | `account_disabled` | 账号被禁用（**仅在密码验证通过之后可能出现**） | 显示"账号已停用，请联系老师" |
| 404 | `not_found` | 资源不存在，**含"存在但不属于我"的资源**（故意不区分） | 友好提示 |
| 409 | `conflict` | 用户名已存在 | 提示换一个 |
| 423 | `locked` | 连续失败过多，短时锁定（**仅在密码验证通过之后可能出现**） | 显示剩余秒数 |
| 500 | `internal_error` | 未预期错误 | 显示"服务出错"，**附 trace id** |
| 503 | `dependency_unavailable` | ★ **口径已改（2026-09-16）**：**L0 内容资产加载失败等启动后不可恢复的依赖**（与 §8-14 同一模式）。★ **不含语料缓存** —— 缓存缺失**有降级路径**（§8-13b：如实返回"出处暂不可用"），原措辞把两者混在一起，于是这个 `code` 变成了**没有任何触发条件的死枚举** | 显示"服务处于降级模式" |
| ★ 503 | **`server_busy`** | ★ **新增（2026-09-16）**：**并发答疑超过承诺上限**（**第 11 路起**，N20 · §8-13g）。★ **此前没有任何"容量超限"的 `code`**，只能复用 `dependency_unavailable` ⇒ 前端会显示"服务处于降级模式"，而真实含义是"**同时答题的人太多，稍后重试**" —— 与"断网被推去填 key"**同型的误导**（用户动作完全相反）。★ **命名把责任划在我们这一侧**：叫 `capacity_exceeded` / `too_many_requests` 会被读成**限流或配额**，而 `model_quota_exceeded` 是**用户自己的模型额度**，两个名字相近会让前端分支与排障分类都混 | 显示"**当前同时答题的同学较多，请稍后重试**"；**可重试**；**不清本地令牌、不引导填凭据、不显示"降级模式"** |
| ★ 503 | **`model_key_missing`** | **未配置凭据**（N14） | 显示"**未配置模型凭据**"并提供 `#/settings?tab=model` 入口；**判定 / 错题本 / 进度 / 知识图谱照常可用** |
| ★ 503 | **`model_unreachable`** | **端点不可达 / 超时 / 被信任边界拒绝**（N14 · N16 · §4.2.1） | 显示"**网络或模型服务不可达，可重试**" —— ★ **不得引导去填凭据**；★ **不得清本地登录令牌** |
| ★ 400 | **`model_key_invalid`** | 用户凭据无效 / 被撤销 | 提示到设置页重填；**不得清本地登录令牌** |
| ★ 402 | **`model_quota_exceeded`** | 用户凭据额度不足 / 欠费 | 提示"你的模型额度不足"，并说明**费用由凭据持有人承担** |

> ★ **凭据类错误绝不能复用 401（2026-09-16 新增，本轮最容易踩的坑）**：前端收到 `401` 的既定行为是"**清本地令牌 + 跳登录**" —— 若把"模型 key 填错"也标成 401，用户会**被登出**，而真正的问题是设置页里那串 key。因此这些 `code` **必须是独立值**（503 `model_key_missing` / 503 `model_unreachable` / 400 `model_key_invalid` / 402 `model_quota_exceeded`），且 `model_key_invalid` 用 **400** 而非 401：它是**客户端可修的输入问题**，不是身份问题。

> ★ **`model_unavailable` 已按 `ADR-0005 §5` 拆为两个 `code`（2026-09-16）**：原表的触发条件写"未配置凭据、**供应商不可达**"、前端行为写"显示'未配置模型凭据'并提供设置页入口" —— 于是**断网的学生被推去填 key**，而两者要的用户动作**完全相反**（一个去填、一个去重试）。ADR 已裁定，本篇此前是合并的，现已落齐（§4.2.1 · §6.1 · §8-13 同源）。

> **越权只走一条路，不再两处定义**（本轮收口）：**读他人数据 → 404**（对外看起来"不存在"，避免用状态码探测他人 ID 是否存在；**日志里记 403** 便于排查）；**403 `forbidden` 只保留"角色不足"**，即非管理员访问 `/api/admin/*`。
> ✅ **已裁定（2026-09-16，负责人）**：旧仓把"账号被禁用"与"权限不足"都归 403 且用同一段文案；**本仓分成两个 `code`**，理由是用户需要知道"这是账号问题，去找老师启用"而不是"我权限不够"。
> **已考虑并否决的替代方案**：退回单一 `forbidden`（理由是避免泄露"该账号存在且被停用"）。
> **否决依据已从"经验判断"改成一条可断言的不变量**（评审 A3 指出原表述不成立，已修）：**在密码通过之前，一切失败都返回同一个 `401 bad_credentials`，且都烧等长的时间**（`§4.1`）。因此 `403 account_disabled` 与 `423 locked` **只可能在密码验证之后出现** —— 攻击者拿不到密码就看不到这两个状态，**不构成额外的账号枚举面**。这条不变量由登录用例直接断言（见 §8）。

### 6.4 分页口径（旧仓的第二个缺口）

**统一为 cursor 分页**，不用 `offset`：

```
请求：?limit=20&cursor=<opaque>
响应：{ "items": [...], "next_cursor": "<opaque>|null" }
```

| 规则 | 值 |
|---|---|
| `limit` 默认 / 上界 | 20 / **100**（超界即 400，不静默截断） |
| `cursor` | 不透明字符串（内部是 `created_at` + `id` 的编码），**客户端不得解析** |
| 排序 | 一律 `created_at DESC, id DESC`（稳定序，避免同秒记录乱序） |

**为什么不用 offset**：问答流水与作答记录是**追加写**的表，翻页时新记录插入会让 offset 漂移 ⇒ **同一条记录被读到两次、或某条被整页跳过**。这对"错题本复习"是真实伤害。

### 6.5 SSE 帧

帧格式与 14 类事件由 `events.schema.json` 定义（A3）。本仓对**帧的两条增量要求**：

1. **每帧带 `seq`（单调递增整数）** —— ★ **只用于去重，不承担"断流续传"**（服务端**不做重放缓冲**，断流语义是**重新发起**，见 §4.2-1）。**原写"断流续传与去重的唯一依据"，与 §4.2-1 直接冲突，2026-09-16 统一**。
2. **终帧必须显式**（`done` 或 `error`）—— 让前端能区分"跑完了"与"网断了"，否则只能靠超时猜。

---

## 7. 前端系统设计

### 7.1 URL 可寻址设计（N11 —— 旧仓实测返回键失效）

**规则**：**凡是有意义的界面状态，必须由 URL 表达**。顺序是"URL 是状态源，组件是它的投影"，不是反过来。

| 路由 | 表达的状态 | 为什么必须可寻址 |
|---|---|---|
| `#/login` | 登录页 | — |
| `#/home` | 首页 | — |
| `#/practice?kc=kc_bayes&item=prob_0012&hint=2` | **具体题目的具体提示层级** | "刷到第 3 步提示"要能刷新、能分享、能回退 |
| `#/wrong?kc=kc_bayes&cursor=<opaque>` | 错题本 + 知识点筛选 + 翻页位置 | 复习到一半刷新不能回到起点 |
| `#/knowledge?kc=kc_bayes&view=graph` | 知识点定位 + 视图模式 | 分享"就是这个知识点不会" |
| `#/admin?tab=students&user=usr_xxx` | 管理页标签 + 选中学生 | 管理员排查时要能把链接发给别人 |
| ★ `#/settings?tab=model` · `#/settings?tab=notice` | **模型端点与凭据**（N14 · N16）· **外传告知**（N15） | ★ **2026-09-16 补位**：§4.2.1 / §6.3 / §8-13 **三处都指向 `#/settings?tab=model`**，而本表**此前没有这一行**；设置页是 **N14 / N15 / N16 三联的承载页**，缺它则 **N11 的"可寻址完备清单"不可能通过** |

**三条实现要求**：
① 监听 `hashchange` **与** `popstate`（旧仓只写 hash、无人监听 ⇒ 返回键只变 URL 不变视图）；
② 视图状态从 URL **单向派生**，组件不得各存一份；
③ ★ **URL 是渲染状态的来源，不是权限或计数的来源** —— `hint=2` 只决定"界面显示到第几级"，**提示次数一律以服务端 `hint_log` 为准**（F6）。手工把 `hint` 改成 2 不会让服务端认为已取过提示，也不会让解析提前出现。

### 7.2 外壳、模块图与页面注册 → **已移入 [`03-架构总览`](03-架构总览.md)**

**外壳职责表 · `features/<x>/feature.json`（含 `class`）· 页面清单 · 模块依赖方向，全部在 `docs/03 §5`。**

★ **本节 2026-09-16 移出（一主题一篇）**：模块图与依赖方向属于**分层**，而分层是 `docs/03` 的主题 —— 与 §2.4 同一个理由。

> ★ **不要顺手把 §7.3 / §7.4 也搬走**：`§7.3 样式分层（单一令牌源）`是 **R-D 的施加面**、`§7.4 状态管理的边界`是 **`§1.4` 存放分工表的一族**（`R-M` 的"⑤ URL / ⑥ 前端持久化以外的地方"都引它）—— 它们是"某个专题的规则与验收口径"，不是"层与层之间的依赖"。**这条边界写在 `docs/03 §11` 的单一权威对照表里。**

### 7.3 样式分层（**单一令牌源**）

```
styles/tokens.css       ← ★ 唯一变量源（颜色/圆角/阴影/间距/过渡）
styles/app.css          ← 外壳骨架（布局、侧栏、顶栏）
styles/components.css   ← 通用组件
styles/pages.css        ← 页面模板层（页面头/工具栏/表格/空态/骨架）
features/<x>/styles.css ← 只写该页独有样式，**禁止硬编码颜色**
```

**规则**：任何颜色 / 圆角 / 阴影**只能来自 `tokens.css`**。旧仓 `widget.css` 有 **77 处硬编码色值**（其中 13 处与 token 同值）—— 那是双形态遗留；v2 单形态下这条规则可被脚本扫描守住。

### 7.4 状态管理的边界

| 状态 | 存哪 | 为什么 |
|---|---|---|
| 令牌（键名 `dsh.token`） | `localStorage` | 免构建、无后端渲染；XSS 风险由"不用 `innerHTML` 拼用户内容"控制（**R-E 是唯一防线，且它是纪律不是机制**） |
| 界面位置（知识点 / 题目 / 提示层级 / 翻页） | **URL** | N11：可刷新、可分享、可回退 |
| 跨页面临时态（当前 run、未提交的作答） | `core/store`（内存） | 刷新即丢是**可接受的** —— 它们本来就不该是持久状态 |
| ★ **模型凭据（N14）** | `localStorage`，键名 **`dsh.model_key`** | 只存浏览器、随请求携带（`X-Model-Key` 头）；**服务端不落任何介质**。★ 它**不进 §7.4 之外任何一处的"存哪"清单**，其清除动作由 §1.4 的第 5 条统一定义 |
| ★ **端点配置（N16）** | `localStorage`，键名 **`dsh.endpoint`**（`provider` · `model` · `base_url`） | 与凭据**同生命周期**；`base_url` 同时是 §4.2.1 六条信任边界的输入面 |
| 持久学习数据 | **只在服务端** | 单一真相；避免前后端两套数据打架 |

---

## 8. 失败模式与降级（一开仓就定死，不留给日后踩）

| # | 场景 | 设计行为 | 谁守（测试） |
|---|---|---|---|
| 1 | **未登录 / 令牌过期** | 401 `unauthenticated`；前端清令牌并跳登录 | 接口测试 |
| 2 | **令牌被撤销**（改密 / 禁用 / 删除） | 立即 401（**不是等 7 天**） | 接口测试 |
| 3 | **越权读他人数据** | 404（对外）· 403（日志） | 越权用例 |
| 4 | **账号禁用 / 锁定** | 403 `account_disabled` / 423 `locked`；两者**只能出现在密码验证通过之后** | 接口测试 |
| 4b | ★ **登录失败的信息量不变量** | **密码通过之前一切失败都是同一个 401 `bad_credentials`，且响应耗时同量级** —— 断言"不存在的用户"与"密码错误"返回**同一状态码 + 同一 code**，且"已禁用/已锁定账号 + 错误密码"**也不泄露**其存在 | 登录用例（枚举面回归） |
| 5 | **超长输入** | 400（有上界）；**绝不进入判定** | R-A 扫描 + 长度用例 |
| 6 | **正则灾难性回溯** | 禁嵌套量词；数字长串专项用例 | 用例（100k 数字须 < 1s） |
| 7 | **判定阻塞事件循环** | 同步 CPU 工作全部 `to_thread` | 并发健康检查（**判定压测时 `/api/health` 必须仍 < 100ms**） |
| 8 | **写库静默回滚** | 显式 `commit()`；测试读回断言 | R-B 扫描 + 读回用例 |
| 9 | **SQLite 并发写** | WAL + `busy_timeout=5000` | 并发写用例 |
| 10 | **SSE 客户端断开** | **取消图**，释放资源 | 断流用例（断开后无残留任务） |
| 11 | **SSE 网络中断** | 前端**重新发起**（会重跑一次图 —— 服务端不做重放缓冲，见 §4.2-1）；按 `seq` **去重**，不重复渲染。★ 原写"按 `seq` 续传"，与 §4.2-1 冲突，2026-09-16 统一 | 前端用例（注入重连） |
| 12 | **图挂起无人应答（HITL）** | 挂起**必须有超时**，超时后按降级路径收尾 | 超时用例 |
| 13 | ★ **未配置凭据 / 凭据失效 / 端点不可达 / 额度不足**（N14 · N16） | **答疑不可用**，显式返回 503 `model_key_missing` / 503 `model_unreachable` / 400 `model_key_invalid` / 402 `model_quota_exceeded`（§6.3）。★ **四者是四条不同的前端动作**：只有 `model_key_missing` 引导设置页；`model_unreachable` **明确可重试**、**不得引导填凭据**、**不得清本地令牌**（原写法把"没配 key"与"网络不通"合成一个 `model_unavailable`，会把断网的学生推去填 key —— `ADR-0005 §5` 已裁定拆分）。**不降级、不产出假答案、不留 mock**。**判定 / 错题本 / 进度 / 知识图谱 / DAG 浏览全部照常可用**（它们不依赖模型 —— F4 早就要求判定不依赖 LLM，这条此刻成了降级演示的基石） | 降级演示用例（不配凭据跑完全部非答疑功能）+ 四条 `code` 的分支用例 |
| 13b | ★ **语料缓存缺失**（预抓未跑 / 缓存被删） | 检索**如实返回"出处暂不可用"**（F10），**不得编造章节号**；答疑其余部分仍可进行 | 缓存缺失用例 |
| 13c | ★ **凭据进入日志 / 审计 / 错误信息 / URL / 出网载荷本体** | **红线**：一律不得出现。★ **口径已由 R-J 的四面扩为 R-M 的七面（2026-09-16）**（日志 · 审计 `detail` · 错误信息 · L2 表 · URL/query · 前端持久化以外的地方 · 出网载荷本体） | R-M 扫描 + "带凭据请求不产生含凭据日志"用例 + 假 provider 抓载荷 |
| 13d | ★ **前端改用 `EventSource`** | `EventSource` **不能带自定义头** ⇒ 凭据将静默失效。若必须改，凭据要改走 cookie 或 body（须走 ADR） | review（已写入 §4.2.1） |
| 13e | ★ **明文 HTTP 下传输凭据与令牌**（N18 连带） | **拒绝**：远程托管形态下 HTTP 请求被拒（重定向或 400 并说明）；`Strict-Transport-Security` 必须存在 | N18 验收①④ 用例 |
| 13f | ★ **备份失败 / 磁盘满**（N19 连带） | ① 备份失败**必须可观测**（写一条**只含 ID 与动作**的日志，符合 R-O）；② **磁盘满时不得静默丢写** —— 写库失败必须冒泡成 500 `internal_error`，**不能吞掉**（旧仓"写函数漏提交、记录被静默回滚"是同一类事故） | 备份失败用例 + 磁盘满注入用例 |
| 13g | ★ **并发超过承诺上限**（N20 连带） | 超过 **10 路并发答疑**时**如实返回 503 并说明**（不做排队队列）。**不得**降级成假答案、**不得**静默串行超时 | 并发压测（11 路，断言第 11 路是显式 503） |
| 13h | ★ **端点校验被旁路**（N16 连带） | `/api/model/ping` 与 `/api/chat` **必须共用同一个校验函数**；**只挡 loopback 不挡 RFC1918 / 169.254.169.254** 视为未实现（§4.2.1 六条） | 校验用例（私网地址 / 元数据地址 / 重定向 / 非 https 四类负样本，两个端点**各跑一遍**） |
| 13i | ★ **`must_change_pw` 被绕过** | 置位后**除 `POST /api/auth/password` 外的一切学习类接口都返回 403 `must_change_pw`** —— 直接构造请求也不放行（与 §8-19 的"伪造 `hint_used`"是同一类：**服务端权威，前端只做渲染**） | 接口用例（置位后逐个接口断言） |
| 14 | **内容资产加载失败**（★ 清单已补全为七条，2026-09-16：DAG 有环 / 条目缺 `kc_ids` / **缺 `gradable`** / **缺 `answer_kind`** / **公式库名称重复** / **L1 语料缺 `license` · `source_url` · `fetched_at`** / **文件头 `rev` 与 MANIFEST 不一致**） | **启动即报错退出**，不得静默丢边、写空标注或用错版内容 | 资产校验脚本 |
| 15 | **单 worker 约束被违反** | 启动时检测并发度；>1 即**拒绝启动并说明原因** | 启动用例 |
| 16 | **删除用户后仍能登入** | 不可登录（空哈希）；令牌已撤 | 接口测试 |
| 17 | ★ **解答验证不通过** | 重算一次（上限 1 次）→ 仍不通过 ⇒ 标 `uncertain` + 级别降 D + **事件流显式告知**（附未通过的检查项）；**不静默输出，也不假装通过** | 验证用例（注入已知错解，见 §9） |
| 18 | ★ **提交不可判定题目的作答** | 200 + `{graded:false, reason:"not_gradable"}`；写 attempt 但 `correct=NULL`、`mastery` 不累加；界面提示"该题需人工判定。**不得**用模型兜底判定（违反 F4），也不得静默给一个错判定（违反 N2） | 接口用例 |
| 19 | ★ **客户端伪造 `hint_used` / `attempt_no`** | **忽略请求体里的这两个字段**，一律用服务端派生值；只改 URL 的 `hint` 不产生任何服务端效果 | 接口用例（构造伪造请求） |

> **第 14 条为什么必须"报错退出"而不是"警告后继续"**：F19 要求 DAG 无环、F20 要求条目挂知识点。资产坏了而系统照跑，产生的是**带错误知识点标注的作答记录** —— 那些记录会污染掌握度与错题本，且事后无法区分哪些是坏的。**宁可起不来。**

> ★ **`§8-14` 是"启动校验清单"的唯一权威（2026-09-16 写明）**：正文另有四处只**引用**它 —— `§1.3` 的语料许可三字段 · `§3.3` 的题库 `answer_kind` · `§3.3` 的公式库名称唯一 · `§3.3` 的 `rev` 一致性，都写着"**启动报错退出（与 §8-14 同一模式）**"。
> **因此新增任何"启动报错退出"的触发条件，必须同步本节清单。** 此前本清单只有 **4** 条，而正文另有 **3** 条引用它 —— **权威处没跟上，引用就变成悬空**：资产校验脚本会少实现这三项（缺 `answer_kind` ⇒ `docs/01 §6` 的两条判定类验收失去分类字段；公式重名 ⇒ F18 的"唯一条目"运行时不可断言；语料缺许可字段 ⇒ 引用合规失去拦截），**而且失败是完全静默的**。
> **第 15 条为什么必须是启动检测**：旧仓把"只允许单 worker"写在文档里，**代码不检测** —— 于是部署时多开一个 worker 就出现"随机丢 run / 事件串台"，而症状看起来像前端 bug。

> ★ **本表的编号规则（2026-09-16 写明）**：**新增失败模式只能续号**（下一个是 `13j`、然后是 `14` 之后顺延），**不得插入既有编号之间** —— 与 `docs/01` 附录 B 的"只增不改、不重编号"同一原则。表中的 `4b` / `13b`…`13i` 是**已发布的锚点**，`docs/06`、测试用例名与提交信息都会引用它们。

---

## 9. 评测资产的存放与执行（N13 · 盲测）

A1 已定（本轮回补时同步细化）：**样例集与运行器入仓并纳入门禁（防退化）· 冻结正式集由非实现者保管、不入主仓（能力验收）**。本篇给出可执行形式：

> ★ **理由已换锚（2026-09-16）**：原文写的是"**离线、无密钥基础设施的前提下（ADR-0001），门禁无法从仓库外取题**" —— 那个前提**已不成立**（v2 联网、且用户自持凭据，理论上可以联网取题）。**结论不变，理由改成**：为一件**用一次**的验收，引入**外部托管位置 + 一套取题凭据**，成本高于收益；而"**正式集不进仓**"这条隔离要求**与能不能联网无关**。

| 资产 | 存在哪 | 谁写 | 谁执行 | 自动化 |
|---|---|---|---|---|
| **样例集**（3–5 题 + 期望结论） | **`content/eval/sample/`**（**入仓**） | 非实现者 | 本地门禁（接远端后即 CI） | ✅ 自动红灯 |
| **运行器** | `scripts/eval_verify.py --frozen-dir <dir>`（**入仓，不含任何答案**） | 任意人 | 非实现者 | — |
| **冻结正式集**（注入集 20 道 + 冻结验证集） | 主仓**之外**（非实现者本机） | 非实现者 | 非实现者 | ❌ 人工门禁 |
| **期望判据表** | 与正式集同处，**不入主仓** | 非实现者 | — | — |

**为什么把"评分脚本"拆成"运行器 + 期望判据表"**：运行器本身**必须可 review**（否则没法确认它真的在数命中，而不是在数"没报错"）。把**答案**移出仓库、把**机制**留在仓库，两个目的同时达成。（该细化已同步回 `docs/01` 的 N13 与附录 B.2。）

★ **执行交接（2026-09-16 补，否则这条门禁没有可执行的路径）**：**执行者在自己的仓库副本上运行**，冻结集**始终留在主仓之外**：

```
非实现者：把冻结集与判据表放在自己机器的任意目录（不入仓、不进 PR）
执行者：  git clone <本仓> → scripts/eval_verify.py --frozen-dir <该目录>
            → 输出 日期 / 执行人 / content_rev / 逐类命中数 / 结论
本仓：    只回填结论行到 docs/06（**不回填题目，不提交冻结集**）
```

> ★ **"函数级、确定性、可批量"描述的是运行器，不是门禁**：它可以一次跑完 20 道，但**结论必须由非实现者签署** —— 自动化与盲测是两件事，混着写会让下一轮以为"既然可批量，那就进 CI 吧"。

### 9.1 ★ 注入集口径（本轮定死 —— 否则这个门禁会平凡通过）

| 项 | 定义 | 为什么必须写死 |
|---|---|---|
| **注入物** | **系统错解**：一道题 + **一个已知错误的解答** | **不是学生错解**。学生答错本来就该被判错，"拦住"便无从体现 |
| **注入点** | **验证链的输入**（**函数级**、确定性、可批量） | 走 `/practice/grade` 是错的：那条链路上任何错答案都会被判为错，**门禁平凡通过** —— 正是 N1 自己警告的"可以被满足而毫无效果" |
| **断言** | 该解答**被标注 `uncertain` / 存疑，或级别被降**（`§4.2-4` 与 **§4.4** 的降级出口 —— §4.4 定义了"什么算不通过"及 `cap(level)`） | 与 §4.2 / §4.4 的验证分支一一对应，无歧义 |
| **分层** | **函数级 20 道**（盲测、人工门禁）+ **端到端 3–5 道**（样例集、进门禁） | 函数级保证覆盖率与确定性；端到端保证"这条链真的接上了" |
| ★ **解答级注入集的构成（2026-09-16 补）** | ★ **20 道中至少 8 道必须落在 A / B 级可达的类别上**（即 `sympy_recompute` / `symbolic_equivalence` **可施加**），且这些道次的拦截方式**必须是"重算不一致"**（`§4.4③` 的第 2、3 行），**不得全部靠"手段缺失 ⇒ 降级到 D"** | 不加这条，N1 的 **≥18/20** 地板就**只约束了"有没有一个降级开关"** —— 全文最贵的一条指标可以被一个 `if` 分支平凡满足。★ **口径与 `docs/01` N1 的构成要求同源**（原文只写在需求层，本节是对应的落地口径） |
| ★ **答案级注入集的构成（2026-09-16 补）** | ★ **20 道 = 错因库一级分类 4 类 × 5 道**（与 F5 的"每类 ≥ 4/5"同一分法；类的定义见 `docs/01` F20 / F5）；★ **另附 ≥ 2 道负样本**（确实无对应错因的错误答案，期望 = "未识别错因"） | 不加这条，F5 的"每类 ≥ 4/5"**在文档里找不到对应的分法**；负样本是"不得编造错因"的唯一载体（**编造即红灯**） |

**执行时机**：**阶段门 C 的出口条件之一**。执行记录（日期 / 执行人 / `content_rev` / 逐类命中数 / 结论）留档，**结论写进 `docs/06`**。

---

## 10. 与 A1 的对照（覆盖面自检）

**附录 A 规则 2 的逐条核对** —— 每条 MUST 至少被一个字段或一个接口支撑：

| 需求 | 落点 | 需求 | 落点 |
|---|---|---|---|
| F1 | `POST /api/chat` 的 `context`（§4.2） | N1 | 判定纯函数层（**`docs/03 §4`**，2026-09-16 由 §2.4 改指）+ 注入集（§9） |
| F2 | 解答步骤结构（§4.2） | N2 | 验证报告必发 + 降级显式（§4.2 · §8-13） |
| F3 | 验证报告字段（§4.2） | ~~N3~~ → **N14** | 见下方 N14 行（N3 已于 2026-09-16 废弃，其行已删除） |
| F4 | `POST /api/practice/grade`（§4.3） | N4 | 免构建外壳（**`docs/03 §5`**，2026-09-16 由 §7.2 改指） |
| F5 | 错因匹配吃错因库（§4.3） | N5 | 无注入面（`docs/01` §9） |
| F6 | 提示状态机 + `hint_used`（§5.2） | N6 | `password_hash` / `token_hash`（§3.2） |
| F7 | `attempt` 全字段 + `asset_version`（§3.2） | N8 | 页面模板层 + 前端断言（§7.3） |
| F8 | `GET /api/me/wrong`（§4.3） | N11 | URL 设计（§7.1） |
| F15 | 令牌链路 + 越权语义（§4.1 · §6.3） | N12 | `context` 可替换（§4.2-5） |
| F16 | 删除范围（§3.1 · §5.3） | N13 | 评测资产存放（§9） |
| F19 | `kc_ids` 非空 + DAG 无环（§2.2 · §3.2） | **N15** | ★ **落点已改（2026-09-16）**：`notice_ack_at` 字段（**§1.4**）+ 读写接口（**§6.1**）+ 设置页路由（**§7.1**）+ 文案判据按**去向**（**§1.5①**）+ 渲染（**R-I：`contracts/user.schema.json` 的 `noticeStatus.message` 标 `must_render`**，前端冒烟断言该文本出现在 DOM 里）。**原文指向 §7.1，而 §7.1 里没有 `notice_ack_at` 的任何内容** —— 与已删掉的 `N3` 死行**同型的空落点** |
| F20 | 五类内容资产 + 版本化 + `gradable`（§3.3） | **N16** | 部署形态两档 + `egress` 字段 + 端点信任边界（§1.5 · §4.2.1） |
| **N14** | 凭据七面不落 + `X-Model-Key`（§4.2.1 · **R-M**） | **N17** | 受控托管：L2 只落我方服务器（§1.3 · §1.4；**N6 / F16 / 审计裁定的口径已于 2026-09-16 落进正文**） |
| **N18** | 传输加密：HTTPS-only + HSTS + 无第三方脚本（§1.5① · §8-13e） | **N19** | 备份与恢复：`backend/data/backups/` + 恢复逐表读回（§1.4 · §8-13f） |
| **N20** | 容量与并发：10 路并发答疑 + 断流取消 + 单 `worker`（§4.2-8 · §8-7 · §8-13g） | — | — |

**28 条 MUST 全部有落点，无悬空**（N3 已废弃，不在内外两张表中；N18 / N19 / N20 于 2026-09-16 加入）。

> ★ **自检纪律（2026-09-16 新增）**：**本表的 MUST 条数必须等于 `docs/01` 附录 B.1 的条数** —— 它是"每条 MUST 都有落点"这句话的**唯一机械载体**。★ **本表的计数变化链：22 → 25（N16/N17）→ 28（N18/N19/N20）**，与 B.1 同源；它曾停在 22、并残留一条**已作废**的 `N3 | 本地检索 / 无 KEY 降级` 行，即**因为没有人核对过这条纪律**。
> ★ **同日第二次核对（2026-09-16）**：条数已随 N16/N17 → **25**、再随 N18/N19/N20 → **28** 更新；同时发现 **N15 行的落点指向 §7.1，而 §7.1 里没有 `notice_ack_at`** —— **"条数对得上"并不等于"落点是真的"**。因此把 R-P3（落点反向闭合）与 R-P1（计数由表体导出）**一并写进门禁**（§1.6）：只数条数会漏掉这一类，**必须能 grep 到被指章节里的关键词**。

**附录 A 待落地表（A3 的输入）在本篇的对应**：

| A3 待落地项 | 本篇已定的口径 |
|---|---|
| F15 账号与角色字段、越权语义 | §4.1 · §6.3（404/403 双写 + 两个禁用 code） |
| F16 删除接口形状与删除范围 | §3.1（**已裁定**：审计保留）· §5.3（脱敏而非物理删） |
| F19 知识点字段与 DAG 节点 ID 口径 | §2.2（`kc_<slug>`，不得复用）· §3.2（非空三层断言） |
| N13 评测资产格式与存放 | §9 · §9.1（运行器入仓 / 判据表不入仓 / **注入集口径**） |
| F3 · N2 置信度上限表（含 B 级） | §6 的 `code` 思路同构：**上限表进契约**，本篇不定值 |
| F3 级别标签渲染口径 | §4.2-3（无标签不出结果） |
| F6 `hint_used` 计数口径 · **`student_hint_log` 字段** | §3.1 · §4.3-6 · §5.2（服务端计数） |
| F7 快照与版本号 | §3.2 · §3.3（`content_rev` 反解链：**MANIFEST 入仓 + git 历史**） |
| F20 五类资产格式与 **`gradable`** | §3.3（含五类首版下界） |
| F10 L1 语料的 **`cite_id`** | §1.3（语料形状约定，见下） |

---

## 11. 本篇的已裁定事项与诚实缺口

### 11.1 已裁定事项（2026-09-16，负责人）—— 原两处 + 本轮九处（决策 1A–9A）

| # | 决策点 | 裁定结果 | 被否决的方案与依据 |
|---|---|---|---|
| **1** | 删除用户时**审计保留** | ✅ **保留** —— 合规证据优先；同时要求删除动作本身写审计 | 否决"一并清除"（会让"该生数据曾被反复查看"不可查）与"target_id 盐化"（为本地审计引密钥管理面不划算）。**代价已记账** |
| **2** | **禁用**与**权限不足**分成两个 `code` | ✅ **分开**（`account_disabled` / `forbidden`） | 否决"退回单一 forbidden"。**依据已按评审 A3 修正**：原表述"能走到 403 说明已通过密码验证"在旧次序下不成立（旧仓的**锁定检查在验证之前**）。现改为一条**可断言的不变量**：**密码通过之前一切失败都是同一个 401 `bad_credentials`，且烧等长的时间**（§4.1 · §8-4b）；本仓据此把锁定与禁用的判定**都放在验证之后**。不变量成立时，这两个 code 不构成额外的账号枚举面 |
| **3** | **检索是否用嵌入 / 重排权重** | ✅ **不用** —— 检索退化为**纯词法**（BM25 + 中文 bigram）；L1 只剩 `corpus/` | 否决"用权重"：要动 **N16 的"不打包权重" + 补第三个出网口**，是改 **MUST 级出网面**的大动作，不只是"加个依赖"。**代价**：F10 检索质量下降（SHOULD，只要求"命中带出处"） |
| **4** | **`GET /api/runs/{id}` 的 run 生命周期** | ✅ **404 `not_found`** —— run 只存内存，**不做持久化** | 否决 `SqliteSaver` 持久化：那要改 §5.1 的**单 worker 约束**，是改架构不是补一行。与 §4.2-1 的"不重放、断流即重新发起"自洽 |
| **5** | **HITL 超时长度的取值时机** | ✅ **阶段 C 用真实链路测定**；在那之前该值**必须来自配置**（不得硬编码），阶段 B 只留一个可配置默认值 | 否决"现在写死一个数"（没有真实链路依据）；否决"不设超时"（违反 §5.1 的硬要求） |
| **6** | **`content_rev` 的组合算法** | ✅ `sha256("dag=..;formula=..;mistakes=..;problems=..;types=..")[:12]`，**按 ASCII 键名升序**拼接 | 否决"按**中文名**排序"：排序依赖 locale ⇒ 同一份资产在不同机器上会算出**不同的版本号**；否决"直接拼接不做哈希"：它要长期存进 `attempt.asset_version`（F7） |
| **7** | **N16 验收② 在干净账号上不可执行** | ✅ 加**前置**"**该账号至少完成过一次答疑**"；P3 下展示"当前无凭据、未发生外传" | 否决"不改、测试时人工先答疑一次"：那是**靠人记住**，不是断言 |
| **8** | **F9 上溯的结果门槛** | ✅ **不设门槛：全量 + 计数**，不按错题数或层数截断 | 否决"只看错题数 ≥2 的前置"与"限前 2 层"：门槛属学习者建模（`docs/01 §9` 不做），而截断会让"上溯到根源"**失去判据** |
| **9** | **F11 阈值未定期间的占位文案** | ✅ 统一为"**数据不足，暂不显示薄弱点**" | 否决"阈值未定义"：那是**面向开发者的措辞**，不该显示给学生 |
| **10** | **运维面的"谁来运维"** | ✅ 四类（证书续期 · 备份调度 · **恢复演练频率** · 压测脚本）**各绑一个 `docs/06` 阶段门动作** | 否决"只把恢复演练进 `docs/06`"：另外三项同样会变成"**到验收那天才发现从没排过练**" |
| **11** | **五类内容资产的人力** | ✅ `docs/05` 的 **R-C 表须至少两人分担**，产出进 `docs/06` 阶段门 B | 否决"一人负责"：F5 / F17 / F18 分别吃错因库 / 题型库 / 公式库，压在一个人身上会**同时挂**；否决"缩小下界"：要改 MUST 级地板，须走 ADR |

### 11.2 诚实缺口（本篇现在缺什么）

1. ~~题库与题型的真实规模未定~~ → **已定**（F20 **五类**下界：题库 ≥ 30 道 · DAG ≥ 40 节点 ≥ 5 章 · 错因库 ≥ 40 · 题型 ≥ 15 · ★ **公式库 ≥ 30 条**（2026-09-16 补，原为无下界）；长期目标题库 ≥ 100 道另记）。**但仍缺"谁产出、何时产出"** —— 那是 `docs/05` 的 R-C 责任人表，不属本篇。★ **五类资产的人力在四人小组的排期里仍无对应条目**，且按 R-C 若压在一人身上，**F5 / F17 / F18 会同时挂** —— 建议 R-C 表里**至少两人分担**，资产产出进 `docs/06` 阶段门 B（F5 的地板要靠它）。★ **已裁定（§11.1-11）**：`docs/05` 的 R-C 表**至少两人分担**，产出进 `docs/06` 阶段门 B。
2. **`student_user` 的字段级定义不在本篇**：沿用已落地实现；机器可读定义在 A3 的契约里。本篇只记增量（`asset_version` · `correct` 可空 · `grader` 新取值 · `created_at` 补记 · ★ **`qa_log.provider` / `model` / `egress`** · ★ **`user.must_change_pw`** · ★ **`user.notice_ack_at`**）与**删除**（`deleted` · ★ **`student_id`**，2026-09-16 —— v2 是新库、不迁旧数据，"历史兼容"的列在新库里没有对象）。
3. **掌握度公式不在 v2 范围内**（属学习者建模，`docs/01 §9` 不做）：`mastery` 目前只是计数，**F11 的"薄弱"判定阈值尚未定义** —— 数据齐备，算法后补。
3b. ★ **F9 的"上溯"算法与结果门槛未定义（2026-09-16 补进本清单）**：§2.2 只定了 DAG 的装载规则（无环、`depends_on` 单向），**没有定"上溯到什么程度算定位到根源"** —— 从错题知识点出发反向可达的前置集合可能很大，"结果怎么展示、多大算够"须在写 `docs/06` 之前定，否则 F9 的"界面必须能显示该集合"没有可验收的形状。★ **已裁定（§11.1-8）**：**不设门槛，全量 + 计数**（已落进 §2.2 规则 3 与 `docs/01` F9）。
3c. ★ **F11 在阈值未定义期间的降级话术未定（2026-09-16 补进本清单）**：`docs/01` F11 已把 v2 的取值范围收窄为"总数 + 每知识点正确率"，**阈值未定义期间的占位文案**（"数据不足"还是"阈值未定义"）须写死，否则每个实现者会各写一句。★ **已裁定（§11.1-9）**：统一为"**数据不足，暂不显示薄弱点**"。
4. **HITL 的超时长度未取值**：它决定用户等多久才看到降级结果。建议阶段 C 用真实链路测完再定。★ **已裁定（§11.1-5）**：阶段 C 测定，机制已定（值须来自配置）。
5. **`content_rev` 的组合算法未定**：只定"五类 rev 的确定性拼接"，具体拼接与哈希形式在 A3 随 `MANIFEST.json` 一起定。★ **已裁定（§11.1-6）**：见 §3.3 规则 2 的写死算法。
6. ~~缓存已热没有可操作定义~~ → **已修**：判定是确定性链路，**"缓存"这个对象本就不存在**，测量条件已改为"进程已启动 + 首次请求已完成"；"缓存已热"的定义只留给真正需要它的**讲解首字（N7）**。
7. **前端 XSS 防线只有一条**（不用 `innerHTML` 拼用户内容）：没有 CSP、没有 DOMPurify。★ **理由已换锚（2026-09-16）**：原文引的是"免构建 + **离线**"，而"离线"已不是需求口径；正确的理由是"**免构建 + 零依赖**"（引一个净化库就要引构建链或增加依赖面）。**结论不变**：它是**纪律，不是机制** —— 已在 §7.4 记账，并由 §1.4 的 R-E 做 grep 兜底。
8. ★ **`duration_ms` 是自陈值**：要拿到可信用时，需要一张"作答开始"记录表。为它再加一张表不划算，故 v2 **不承诺**该值的可信性（已在 F7 与 §3.2 记账）。
9. ★ **username 允许复用**：靠 `user_id` 后 8 位区分同名。若实际出现"同名不同人"的混淆，再改为禁止复用并走 ADR。

10. ★ **运维面的三条已立项，但"谁来运维"没有落点（2026-09-16）**：N18 / N19 / N20 现在有验收动作，却没有对应的**运维脚本与演练节奏**（证书续期 · 备份调度与**恢复演练频率** · 压测脚本）。它们不属本篇（本篇只定"要什么"），但**必须进 `docs/06`** —— 否则三条新 MUST 会变成"到验收那天才发现从没排过练"。★ **已裁定（§11.1-10）**：四类各绑一个 `docs/06` 阶段门动作。
11. ★ **`GET /api/runs/{id}` 的 run 生命周期未定（2026-09-16 补端点时暴露）**：§4.2-1 已定"服务端不做重放缓冲、断流即重新发起"，且 §5.1 的图检查点**当前是内存态** —— 那么**进程重启后该端点返回什么？** 建议：404 `not_found`（run 不存在），**不做持久化**。**这一条必须在 A3 写契约之前定**。★ **已裁定（§11.1-4）**：**404 `not_found`**，不做持久化（已落进 §6.1 的运行行）。
12. ★ **N16 验收②在"干净账号"上不可执行（2026-09-16 发现）**：它要求"设置页展示的去向与 `SELECT DISTINCT egress FROM student_qa_log` 一致"，而**从未答疑过的账号**（P3）该查询为空 —— 于是这个断言在干净账号上既非真也非假。**验收条件须加前置"至少完成过一次答疑"**，且 P3 下设置页展示的是"当前无凭据、未发生外传"。★ **已裁定（§11.1-7）**：加前置"至少完成过一次答疑"（已落进 `docs/01` N16 格与 R-N）。

13. ★ **检索是否使用本地嵌入 / 重排权重 —— 已裁定：不用（2026-09-16，决策 1A）**：旧仓的"混合检索"依赖 **BGE-M3 + bge-reranker-v2-m3** 的本地权重（数 GB），而 v2 有三处与之冲突 —— **N16** 明写"不内置、不打包模型权重"（本意是 P1 的本机 LLM，但**字面覆盖全部权重**）· **§1.5③** 的"恰好两个出网口"（**没有任何口下载权重**）· **§1.3** 已把"模型权重"从 L1 删除（理由是"不可能可随时重抓"）。**默认建议（待负责人确认）**：**v2 检索退化为纯词法**（BM25 + 中文 bigram），**不使用嵌入 / 重排权重** —— 与"零依赖 + 两个出网口"一致，代价是 F10（SHOULD）的检索质量下降；**若要向量检索**，须走 ADR 并为权重下载**补一条出网口**（或明确并入 `scripts/fetch_corpus.py` 的预抓职责）。**详细处置见 `docs/03 §9.3-1`。**★ **已裁定（§11.1-3）**：**不用权重**，检索退化为纯词法（已落进 §1.3 · `docs/03 §2` 与 §10）。

★ **2026-09-16 本轮九条裁定（§11.1-3 … §11.1-11）之后，已经没有条目阻塞 `docs/06` 的编写**：第 4、5、11、12、13 条与 3b、3c **全部闭合**（第 4 条的"取值"改为**随阶段 C 测定、机制已定**），第 1 条的"人力"部分由 §11.1-11 闭合。★ **但 `docs/06` 仍要承接两条已绑定的动作**（它们不是"待裁定的问题"，而是**已裁定的义务**）：**§11.1-10 的四类运维演练** · **§11.1-11 的资产产出（阶段门 B）**。**第 3 条（掌握度公式 / 薄弱阈值）保持"不做"**（`docs/01 §9`），不因本轮裁定而变。
