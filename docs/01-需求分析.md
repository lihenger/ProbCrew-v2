# 01 · 需求分析

> | | |
> |---|---|
> | 性质 | **需求层的唯一权威**。本仓所有"应该有什么"的结论以本文为准 |
> | 回答什么 | **为谁**做 · 解决**什么**问题 · **什么算做成了** |
> | **不**回答什么 | 怎么做 → [`02-系统设计`](02-系统设计.md) · 怎么分层 → [`03-架构总览`](03-架构总览.md) · 字段长什么样 → [`contracts/`](../contracts/) · 谁做 → [`05-分工与协作规范`](05-分工与协作规范.md) · 何时做 → [`06-开发计划与阶段门`](06-开发计划与阶段门.md) |
> | 方法 | §1–§6 **脱离实现**推导（假设今天才拿到题目）；§7 映射交付形态；§8 是**建仓承诺**，把上一个仓库踩过的坑变成这里的硬性做法 |
> | 需求可追溯性 | 每条需求都带**可断言的验收方式**。写不出验收方式的不进本文 —— 那是愿望，不是需求 |
> | 快照 | ProbCrew v2 · 建仓文档（阶段 A） |

---

## 1. 用户不是一个人，是四个

学生项目最常见的失败是**把"学生"当成一个人**。实际上有四类人的成功标准**互相冲突**，一个界面同时讨好必然撕裂。

| 角色 | 真实处境 | 他要的 | 他**不**关心 |
|---|---|---|---|
| **S1 正在写作业的学生** | 有 deadline，现在就要这道题怎么算；**已注册**（或愿花一分钟注册） | 快、准、步骤能抄 | 读说明书、配环境、等半天 |
| **S2 备考的学生** | 要的是"我哪里不会"，以及反复练 | 薄弱点定位、能重做、有进度 | 单题的漂亮讲解 |
| **S3 任课教师** | 关心学生是否**真在学**、会不会被代做 | 能否被我看见、能否证明有效 | 架构多先进 |
| **S4 项目组 / 评委** | 关心这事有没有技术含量、能不能讲清 | 可验证、可复现的数字 | 界面好不好看 |

**关键推论**：**S1 与 S2 的诉求方向相反** —— S1 要"最快拿到答案"，S2 要"让我自己想出来"。

> ⚠️ **本仓必须承认的四条代价**（这是明账，不是"以后再说"）：
>
> **代价一 · 形态**：上一个仓库用**两种形态**化解 S1/S2 的冲突（探针服务 S1，主站服务 S2）。本仓决定**先只做主站**，探针后置 —— 因此 S1 得不到"就着屏幕上那段话提问"。需求 F1 成立、形态后置（见 §7），由 **N12** 保证主站不会把探针的路堵死。
>
> **代价二 · 门槛**：v2 的**全部学习类接口都要求登录**（F15），**不设免登录例外**。因此 S1 的"零摩擦"诉求**不被服务** —— v2 的 S1 实为"**已注册**学生"。
> 这不是懒，是**与 §5 伦理约束的正面冲突**：F5 的错因诊断是最接近"作业代写器"的能力，我们选择**不给它零摩擦入口**；且 ⑥⑦ 的持久化本就必须先有稳定身份（N6）。
> **v2 站在伦理一侧，并把这一取舍如实记账** —— 将来若有人要开免登录车道，必须先推翻这条理由，而不是"顺手加个开关"。
>
> **代价三 · 过程**：`§3` 环节③ 原文是"**逐步判定，指出第一处错在哪**"，但 v2 **不做中间步骤校验**（只收最终答案）。因此"**过程比答案重要**"这条课程性质（§2 性质 2）只能靠**提示阶梯**（F6）**部分**兑现 —— 系统给得出"你踩了哪个坑"，给不出"你第 2 步就错了"。
> 不做的理由是**它与 §5 的伦理约束方向相反**：允许逐步试错到对，等于把"作业代写器"从一个入口变成一个过程。
>
> **代价四 · 现场风险**：完整的答疑链路现在**依赖网络与用户自己提供的凭据**（N14）。上一个仓库的 ADR-0001 当初把"**答辩现场网络不可控**"列为主要动机之一，而 v2 主动放弃了"断网也能完整演示"这个保险。
> 缓解：§6 的可用性验收拆成**四条** —— **单机完整（P2）** · **单机离线（P1 本机端点，断网也可含答疑）** · **远程完整（P2）** · **降级（P3，不含答疑）**；但**降级演示不覆盖答疑**，这一点必须主动说，不能等评委问。★ **四条的分界是"去向"而不是"形态"**：单机形态既可能不外传（P1/P3）也可能外传（P2），见 N15 与 `docs/02 §1.5①`。

### 1.1 需求 × 角色覆盖矩阵

把"谁被服务"从散文变成**可机械核对**的表。**"v2 交付"四档的含义**：

- **完整** —— v2 范围内按 §4 验收栏完整交付。
- **部分** —— v2 只交付该需求的一部分（括号内写明缺哪部分）。
- ★ **未排期 / 后置** —— **同一档的两种写法**（**同义词，不是两档**）：`docs/06` 里一律**不计完成**（§7 的判定规则）。**未排期**用于 MAY、后置用于形态后置（探针相关）—— 区别只在**括号里那句理由**。
  > ★ **2026-09-16 补这一档的记账**：原档次说明只有"完整 / 部分 / 后置 / 使能"四档，而**表体有 2 条写成"未排期"**（F12 · F13），读者要自己推断它属于哪一档，而 **R-P1 的脚本从表体导出时也对不上正文汇总**。现把两种写法并成一档并给档位名 —— **档位名按表体写法原样列**，脚本才数得对。
- **使能** —— 不直接面向用户，其交付通过别的需求体现。

> **级别不在此重复**（级别在 §4）—— 避免同一件事两处记载。级别为 SHOULD / MAY 的需求，标"完整"指**若在 v2 实现则须完整**，**不代表排期承诺**；排期见 `docs/06`。

| 需求 | 服务角色 | v2 交付 |
|---|---|---|
| F1 带上下文提问 | S1 · S2 | **部分**（注入式读屏后置） |
| F2 逐步解题 | S1 · S2 | 完整 |
| F3 正确性声明 | S1 · S3 · S4 | 完整 |
| F4 即时判定 | S1 · S2 | 完整 |
| F5 错因诊断 | S1 · S2 · S3 | 完整 |
| F6 分级提示 | S1 · S2 · S3 | 完整 |
| F7 作答记录 | S2 · S3 | 完整 |
| F8 错题归类与重做 | S2 | 完整 |
| F9 依赖上溯 | S2 | 完整（**展示层**；数据层见 F19） |
| F10 教材出处 | S1 · S2 · S3 | **部分**（PDF 溯源定位后置） |
| F11 进度 / 薄弱 | S2 | 完整 |
| F12 可视化 | S1 · S4 | **未排期**（MAY；若实现则须完整） |
| F13 协作可见 | **S4** | **未排期**（MAY；若实现则须完整，且受承诺 10 的配额约束） |
| F14 提示透明化 | S3 · S4 | 完整 |
| F15 账号与角色 | S3 · S4 | 完整 |
| F16 删除自己的数据 | S1 · S2 | 完整 |
| F17 结构化拆解 | S1 · S2 | 完整 |
| F18 公式 / 定理检索 | S1 · S2 | 完整 |
| F19 知识点标注 + DAG | S2 · S3 | 完整 |
| F20 内容资产 | — | **使能**（题库 / 错因库 / DAG / 公式库 / 题型与解法套路） |
| N1 正确性可验证 | S1 · S2 · S4 | 完整 |
| N2 诚实 | S3 · S4 | 完整（含**外传告知**，见 N15） |
| ~~N3 数据不出校~~ | — | **已废弃**（2026-09-16，数据不出校部分由 **N14** 取代；残余的"教材语料不公开分发"并入 F20） |
| N4 无需安装即用 | S1 · S2 | 完整（**免构建、零 npm、零安装**；"离线可跑"已删） |
| N5 不篡改第三方平台 | S4 | **后置**（探针落地时生效） |
| N6 化名账号、无 PII | S3 | 完整 |
| N7 响应及时 | S1 · S2 | 完整（**判定延迟归 F4**，本项只留讲解首字） |
| N8 可访问可读 | S2 · S4 | 完整 |
| N9 可复现的效果数据 | **S4** | 完整 |
| N10 多端可用 | S2 | **后置**（§9 不做） |
| N11 可寻址与可回退 | S2 | 完整 |
| N12 形态可扩展 | — | **使能**（契约层守住） |
| N13 评测资产 | S4 | 完整（含**人工门禁**部分，见 §6） |
| **N14 用户自持凭据** | S1 · S2 · S3 | 完整 |
| **N15 外传告知** | S1 · S2 · S3 | 完整（**条件化**：★ 判据 = **去向**，而非部署形态 —— `egress=remote`（P2）显示"会外传"；`egress≠remote`（P1/P3）显示"不发往第三方"。见 N15 格子） |
| **N16 部署形态与端点可控** | S1 · S2 · S4 | 完整（**远程托管形态不含 P1**，见 N16 格子） |
| **N17 受控托管** | S3 · S4 | 完整 |
| **N18 传输加密** | S1 · S2 · S3 | 完整 |
| **N19 数据备份与恢复** | S1 · S2 · S3 | 完整 |
| **N20 容量与并发** | S1 · S2 · S4 | 完整 |

**汇总（40 条，按"服务角色"列统计）**：**S1 = 20 条**（其中 2 条"部分"）· **S2 = 26 条**（2 条"部分"）· **S3 = 15 条** · **S4 = 14 条**；使能 **2**（F20 · N12）· **未排期 2**（F12 · F13）· **后置 2**（N5 · N10）· 部分 **2**（F1 · F10）· 已废弃 **1**（N3）—— 与表体一致（**已用脚本逐行核对**：40 行 = 20 功能 + 20 非功能；档位合计 = 2 + 2 + 2 + 2 + 1 + 其余 31 条"完整"）。★ **2026-09-16 补"未排期"一档**：原写"后置 2 · 部分 2"，既漏了表体里的 2 条"未排期"（F12 · F13），也让 `docs/06` 的"未开始"口径缺一个来源；**"未排期"与"后置"在 §1.1 的档次说明里是同义词**（`docs/06` 里一律不计完成）。

> ★ **该计数必须由脚本从本表表体导出**，不得手数（本行曾因手工统计出错一次）。脚本随 `scripts/verify.sh` 在阶段 B 落地；在此之前，**表体任何增删必须同步本行**。
> **行数与需求清单一致**：20 功能 + **20 非功能** = **40 行**（N1–N20；N3 行保留为废弃标记，计入行数、不计入服务角色统计）。
> ★ **本行曾长期停留在 N16/N17 立项前的旧值（"20 功能 + 15 非功能 = 35 行"），2026-09-16 修正** —— 它与上一行的"40 条"**曾同时存在于同一节**，正是 R-P1 要挡的那类"正文数字没跟表体走"。

> **这张表解决的是"服务谁"，不解决"谁做"。** 后者只由 §8 的 **R-C** 与 `docs/05` 的责任人表解决 —— 覆盖矩阵不是它的替代品（"要求存在、没人负责"是归属问题，不是覆盖问题）。

---

## 2. 概率论这门课特殊在哪

换一门课（比如英语单词），需求会完全不同。四条性质直接决定产品形态：

| 性质 | 后果 |
|---|---|
| **答案几乎总是一个可验证的数值或表达式** | ★ **最大的机会**：正确性可以被机器证明。英语作文不行，历史论述不行，概率论行 |
| **但"过程"比"答案"重要** | 学生错的往往不是算错，而是**建模错**（把 P(A\|B) 与 P(B\|A) 搞反、漏掉完备事件组）。只给答案没用 |
| **错误高度模式化** | 贝叶斯方向颠倒、独立与互斥混淆、忽略 n 次试验的独立性……**错误种类有限且可枚举** —— 这使"错因库"成为一份**有限、可穷举、可版本化**的资产（首版规模见 §4 F20） |
| **知识点依赖关系强** | 不会条件概率就学不会贝叶斯；不会期望就学不会方差。**知识点是有向图，不是列表** |

**推论 1**：因为答案可验证，"**告诉你这个答案经过了几层验证**"能成为真卖点 —— 文科类工具做不到。
**推论 2**：因为错误模式化且有限，"**告诉你踩了哪个坑**"比"再讲一遍知识点"有效得多，而且要**从教材与教师经验整理即可，不需要任何学生数据**。
**推论 3**：因为依赖关系强，错题不能只记"这道题错了"，必须记"**这道题考了哪几个知识点**"，才能沿依赖图往上找根源。**这一条直接决定数据模型**。

---

## 3. 从"学生的一次挫败"倒推功能

不要列功能清单。跟着一次真实的挫败走一遍，功能会自己长出来。

```
场景：学生做到一道贝叶斯题，卡住了。

① 他不知道这题在问什么            → 需求 F17：题目能看懂 / 能结构化拆解
② 他隐约记得有个公式但想不起来     → 需求 F18：精准的公式/定理查找（不是泛泛讲解）
③ 他写了一个式子，不确定对不对     → 需求 F5：给出错因与下一步该检查什么
                                          ★ 措辞已改（2026-09-16）：v2 不做中间步骤校验，
                                            见 §1 代价三与下文说明
④ 他算完了，但答案是 0.99         → 需求 F4：即时批改 + 诊断"你把方向搞反了"
⑤ 他改对了，但不知道为什么对       → 需求 F10：解析 + 这一步的依据（教材出处）
⑥ 一周后他又错了同一类题           → 需求 F8：错题按知识点归类 + 能重做
⑦ 他连着错了三道贝叶斯题           → 需求 F9：沿知识依赖往上找，是不是条件概率就没懂
                                         需求 F11：给出复习路径，而不是"再练 10 道"
```

> **七个环节逐环都有需求落点**（横跨 ③④ 的 `F6 分级提示` 是伦理约束，见 §5；环节 ↔ ID 的完整性由此可被机械核对）。★ 原写"一一对应"，但 ⑦ 映射到 **F9 与 F11 两条**（8 个 ID / 7 个环节）—— 措辞已于 2026-09-16 修正。
>
> ★ **环节③ 的措辞已下调（2026-09-16）**：原文是"**逐步判定，指出第一处错在哪**"，但 v2 的判定只收**最终答案**，不做中间步骤校验 ⇒ 这句话承诺了一个系统给不出的东西。现改为"**给出错因与下一步该检查什么**"，并把"过程比答案重要只能部分兑现"写进 §1 的**代价三**。
> **为什么不做**：允许逐步试错到对，等于把"作业代写器"从一个**入口**变成一个**过程** —— 与 §5 的伦理约束方向相反。（这是取舍，不是能力不足。）

**七个环节，缺一环闭环就断。** 其中 ⑥⑦ 与 ①–⑤ 是**两种不同类型的需求**：

- ①–⑤ 是**单次会话内**的需求 → 无状态、可随时丢弃
- ⑥⑦ 是**跨会话**的需求 → **必须有持久化的作答记录**

> ★ **这是全篇最重要的推论**：任何声称"伴学"的工具，如果没有持久化作答记录，它其实只是个"答疑工具"。
> **"学习"这个词承诺的是时间维度上的变化，而没有记录就没有变化可谈。**
>
> ⚠️ **但"记下来"只完成了一半**：⑥⑦ 需要的是 **F7（记录）** 与 **F8（跨会话读回）**，两者**同为 MUST**。
> 只要求记录、不要求读回，会精确复现旧仓的失败模式 —— 契约完备、链路挂起。
> **日记了但打不开，等于没记**（见 §8 承诺 2）。

---

## 4. 需求清单

级别：**MUST**（缺了产品不成立）· **SHOULD**（缺了明显变差）· **MAY**（锦上添花）。
**"验收方式"一栏必须是可断言的动作**，不是形容词。

### 4.1 功能性需求

| ID | 需求 | 级别 | 验收方式（可断言） |
|---|---|---|---|
| **F1** | 学生能**带着上下文**提问（当前在看的题目 / 知识点 / 教材片段） | MUST | 提问请求必须携带至少一个上下文标识；无上下文时接口**显式降级**而非静默。★ **注意**：本项在 §1.1 的覆盖矩阵里标"**部分**"（注入式读屏后置），但其**级别为 MUST** —— 两者不冲突：级别说的是"这一项该不该有"，覆盖矩阵说的是"v2 交到哪一步" |
| **F2** | 系统能解题并给出**可读的逐步过程** | MUST | **结构断言**：步骤数 ≥ 3，且每步含 `{说明, 式子}` 两字段**非空**、可被脚本校验。**适用范围：只约束 solve 类解答** —— `explain` / `lookup` 意图的回答不受"步骤数 ≥ 3"约束（否则脚本会把讲解类回答当解答判）。**语义部分为人工抽样项**（非自动门禁）：2 名非作者各自复述一次，均能还原意图即通过，判读记录留档 |
| **F3** | 每条解答带**正确性声明**（这次验证到什么程度） | MUST | 每条解答都携带级别标签；**无标签不出结果**；前端渲染该标签（后端发了前端不看等于没发） |
| **F4** | 提交作答后**即时判定**，且**不依赖大模型往返** | MUST | **纯计算题**端到端 < 1s；判定链路中无 LLM 调用（以调用计数断言）。★ **术语对齐（2026-09-16）**：本文的"**纯计算题**" = §6 的"**数值题**" = 题库条目的 `answer_kind=value`，且**必须同时 `gradable=true`**（`gradable=false` 的题不进判定链，见 `docs/02 §3.3`）—— 该字段定义在 `docs/02 §3.3`，本节与 §6 不再各写一套名词 |
| **F5** | 判定错误时给出**针对性错因**，而非"答错了" | MUST | **命中路径**：在**答案级注入集**上（错因匹配的函数级入口）命中率 **≥ 90%**，**且每类 ≥ 4/5**（地板见 §6；★ **"类"的定义 = 错因库的一级分类，首版 ≥ 4 类**，随 F20 的错因库格式一并定义 —— 原写法只说"每类"，而**类的定义不存在**，该指标因此不可执行）。"命中"= **实际命中集合 ⊇ 期望集合且不含未期望条目**（见 §6）。**未命中路径**：如实说"未识别错因"，**不得编造错因**（负样本即红灯）。★ **错因标签必须落库**：`attempt` 记 `mistake_id` + `mistake_matched` —— **否则"告诉你踩了哪个坑"这条产品主张只在当次响应里成立，一周后错题本说不出"你当时是把方向搞反了"**（F8/F11 拿不到它） |
| **F6** | **分级提示**（先方向 → 再步骤 → 最后解析），且**每次提示计入记录** | MUST | 未提交前**不得**返回完整解析；每次提示使 `hint_used` 精确 +1，并能从错题本如实显示"这道题用了 2 次提示"。**`hint_used` 由服务端计数（提示流水），客户端上报值一律忽略** —— 否则改一下 URL 或直接构造请求即可绕过，§5 伦理约束就没有执行载体。**只改 URL 的提示层级不能让服务端认为已取过提示**。★ **两个值、两个作用域（2026-09-16 写死）**：**`hint_used` = 本次会话**（`user,item,session` 计数；F6 / F14 的权威值）· **`hint_used_total` = 跨会话累计**（`user,item` 计数；错题本"这道题一共提示了多少次"）。**两个都由服务端算**；"重做时重新计"指前者，"历史不清零"指后者 |
| **F7** | 每次作答**留下结构化记录** | MUST | 记录含：题目、作答、对错、知识点、用时、提示次数、判定者、**标准答案快照 + 资产版本号**（与 F20 的可版本化对齐；否则题库改版后历史记录无法解释）。**"用时"（`duration_ms`）是客户端自陈值，不参与任何验收** —— 要可信用时需另建"作答开始"记录，v2 不做（已记账） |
| **F8** | 错题可按**知识点**归类，并支持重做（**跨会话读回**） | MUST | 能列出"贝叶斯类错了 3 道"并可发起重做；**刷新页面后仍能读到**。只写不读等于没记（§3） |
| **F9** | 知识点**依赖关系**可见，错误可沿依赖上溯 | SHOULD | 存在知识点 DAG；**上溯结果可断言**：输入 `(user_id, kc_id)`，输出 = 从该知识点的错误作答出发、沿 `depends_on` **反向可达**的前置知识点集合（同一条路径不得重复计数）；界面必须能把该集合**全量**显示出来，并给出**计数**（"沿依赖上溯到 N 个前置知识点"），**集合为空时显式说"未定位到前置薄弱点"**（不得静默）。★ **不设门槛（2026-09-16，决策 6A）**：**不按错题数或层数截断** —— 门槛属学习者建模（§9 不做），截断会让"上溯到根源"失去判据；落点见 `docs/02 §2.2` 规则 3 |
| **F10** | 解答能**引用教材出处**（章节/片段），可核对 | SHOULD | 检索命中带出处；**无命中时如实说明"未找到出处"**。**出处用 `cite_id`**（不是自由文本）—— 资源**只取开放许可**的、**抓取后缓存到本地**，因此出处位置稳定、可复核。**缓存缺失时如实说明"出处暂不可用"**，不得凭记忆编造章节号 |
| **F11** | 学生能看到**自己学到哪了**（进度 / 薄弱） | SHOULD | 存在进度视图，**数据源必须是作答记录**，不得用"问答条数"冒充学习进度；★ **"薄弱"在 v2 的取值口径已定（2026-09-16）**：本行只承诺 **"总作答数 + 每知识点正确率"** 两个可见量，**不承诺薄弱判定阈值**（阈值属学习者建模，见 `docs/02 §11.2-3`）—— 阈值未定义期间，该视图必须以**统一文案**"**数据不足，暂不显示薄弱点**"显式占位（2026-09-16，决策 7A：**写死为这一句**，不再出现"阈值未定义"这类面向开发者的措辞），**不得自行给一个分数** |
| **F12** | 可视化辅助（分布曲线、交互动画） | MAY | 动画可播放、分布图可交互 |
| **F13** | 多智能体协作过程**可见** | MAY | 协作轨迹可回放 |
| **F14** | **提示使用透明化**：能回答"这次学习用了多少次提示" | SHOULD | 存在自陈视图，**数据源同 F11**（作答记录），仅按 `hint_used` 聚合；**不采集任何额外个人信息**（服务 S3，零合规成本） |
| **F15** | **账号与角色**：学生 / **教师（管理员）**两种身份；学生只见自己的数据 | MUST | **未登录不得访问任何学习类接口（含提问与判定），不设免登录例外** —— 这是与 §1 S1 摩擦预算的正面冲突，v2 选择站在 §5 伦理约束一侧；学生 A 无法读到学生 B 的任何数据（越权测试）；**可见范围**：管理员可见本机范围内全体学生数据（教师视角由管理页承担），学生只见自己；管理员账号**由 CLI 创建**，接口不可创建管理员 |
| **F16** | 学生可**删除自己的数据** | MUST | 调用删除接口后，该用户在学习记录表中的行数为 **0**（读回断言，不是"没报错"），账号被脱敏。**删除范围：四张表**（问答流水 / **提示流水** / 作答记录 / 掌握度）**均为 per-user**（掌握度主键为 `(user_id, topic)`），**不存在独立的匿名聚合层** —— 因此删除 = 明细 + 该用户的聚合记录一并删除，**不保留任何可回溯到该用户的统计**。与 N6 口径一致。**接口形状在 A3 落进契约**。★ **边界（必须如实声明，否则删除权是虚的）**：删除权覆盖**服务器上属于该用户的全部数据**（口径由 **N17** 于 2026-09-16 自"本机全部数据"**改写** —— 单机形态下就是我们自己的机器，远程托管形态下是我们运营的服务器），但**第三方模型服务已经收到的请求删不掉** —— 该边界须与 N15 一并告知用户 |
| **F17** | 题目可**结构化拆解**（已知 / 所求 / 适用模型） | SHOULD | **结构断言**：三段字段**非空且可被脚本校验**；**第三段的候选模型必须来自 F20 的"题型与解法套路"资产**（`model_id` 须命中资产，**不得由模型现编**）；无法判定时**显式标注"未匹配模型"**，不得留空装懂。**语义部分为人工抽样项**：2 名非作者各自复述一次（协议同 F2） |
| **F18** | **公式 / 定理精准检索**（按名称或知识点定位，非泛泛讲解） | SHOULD | 输入定理名，返回唯一条目（`fm_<slug>`）且带**适用条件**；无命中时如实说明。★ **名称唯一性由资产校验保证**：`content/formula.json` 加载时**名称重复即视为资产错误 ⇒ 启动报错退出**（否则"唯一条目"不可断言） |
| **F19** | 作答记录**必带知识点标注**；知识点之间维护**依赖 DAG** | MUST | 每条作答记录的知识点字段**非空**；DAG 可导出且**无环**。这是**数据模型层**需求，与展示层的 F9 分开定级（理由见附录 B 变更记录） |
| **F20** | **内容资产可维护**：题库 / 错因库 / 知识点 DAG / 公式库 / **题型与解法套路** | MUST | **五类资产**各有**固定文件格式**且可**版本化**（**自制资产入库**；**教材原文与抓取到的开放许可资源属 L1，不入仓**，见 F10）；**错因与公式条目必须关联具体知识点 ID**，**题型条目必须关联它所用的知识点 ID**（F17 的"适用模型"吃的就是这一份）；**题库条目必须带 `gradable` 标记与 `answer_kind`**（数值题 / 应用题，未标记视为资产错误）。**首版规模（下界，只许上调）**：题库 **≥ 30 道** · 知识点 DAG **≥ 40 节点且覆盖 ≥ 5 章** · 错因库 **≥ 40 条** · 题型 **≥ 15 类** · **公式库 ≥ 30 条**（★ 2026-09-16 补：原为"无下界"，使 F18 可被一个空库平凡满足，而 `docs/02 §11.2` 却声明"规模问题已定"）。**摊布下限**：每条错因 / 公式 / 题型条目须关联 **≥ 1 个知识点**，且**单一知识点占比 ≤ 30%**（否则可构造"40 条错因全属一个知识点"的合法资产，让 F5 的地板变成白菜关卡）。**无教师访谈时的降级来源**：教材"常见错误"章节 + 自编做题经验 —— 外部依赖**不构成阻塞** |

### 4.2 非功能性需求

| ID | 需求 | 级别 | 为什么对本项目特别重要 |
|---|---|---|---|
| **N1** | **正确性可验证**：错误答案必须被拦住或标注 | MUST | 唯一的技术差异化。学生无法判断对错，**误信的代价由学生承担**。验收必须是**结果指标**：注入 20 道已知**系统错解**（方向颠倒 / 漏完备组 / 单位错 / n 乘错各 5 道）→ 断言被标注存疑或降级者 **≥ 18 道（90%）**。"走了重算流程"是过程指标，**可以被满足而毫无效果**。**该地板只允许上调，下调必须走 ADR**。★ **构成要求（2026-09-16 补，否则地板可被平凡满足）**：§9 的**解答级注入集** 20 道中**至少 8 道必须落在 A / B 级可达的类别上**（即 `sympy_recompute` / `symbolic_equivalence` 可施加），且这些道次的拦截方式**必须是"重算不一致"**（`docs/02 §4.4③` 的第 2、3 行），**不得全部靠"手段缺失 ⇒ 降级到 D"** —— 否则这条地板只约束了"有没有一个降级开关" |
| **N2** | **诚实**：做不到的事必须明说 | MUST | 标注级别与实际验证手段的**一致率必须 100%**；不得笼统说"已校验"。学习工具的信任是一次性的 |
| ~~**N3**~~ | ~~**数据不出校**（可本地 / 局域网运行）~~ | ~~MUST~~ **已废弃** | **2026-09-16 废弃**：v2 改为**用户自持凭据**（N14）—— 学生提问会发给第三方模型服务，**"数据不出校"不再成立**。**残余含义保留**："教材语料不公开分发、不入仓"并入 **F20** 的资产边界。废弃理由与被否决的替代方案见 `ADR-0004` |
| **N4** | **无需安装即用**（免构建、零 npm、打开浏览器就用） | MUST | 决定实际使用率；S1 的耐心 ≈ 0。**口径已收窄（2026-09-16）**：删去原"离线可跑"—— 完整链路依赖网络与凭据（N14），**这条不再声称离线** |
| **N5** | **不篡改第三方平台**（只读，不点击、不注入视觉元素） | MUST（条件） | 仅当将来做注入式探针时生效。**即使探针后置，这条也不得从需求里消失** —— 否则将来会有人"顺手"实现成能操作页面 |
| **N6** | **化名账号、无 PII**：不采集学号/真实姓名；密码只存哈希 | MUST | 既满足 ⑥⑦ 的持久化，又不触发个人信息保护的合规门槛。★ **数据只落「受控托管」下的服务器**（**N17** 口径，2026-09-16 自"只落本地库"改写）：单机形态下就是我们自己的机器，远程托管形态下是我们运营的服务器 |
| **N7** | **响应及时**：单题判定 < 1s，讲解首字 < 2s | SHOULD | 超时就退化成"去问搜索引擎"了。**判定延迟以 F4（MUST）为准，本栏不重复定级** —— 同一指标在两处定级，会让 `docs/06` 的取舍失去依据 |
| **N8** | **可访问、可读** | MUST | **三项分别验收，且自动化程度不同（2026-09-16 修正）**：**点击目标 ≥ 32px**（可自动）· **键盘可达 + 语义标签**（可自动）· **对比度 ≥ 4.5:1**（**只对"声明的色对"可自动**；**渲染结果仅 review** —— 真算渲染对比度必须用无头浏览器，与"零安装"前提冲突）。由前端冒烟测试断言 ①②，色对清单 + 纯 Python WCAG 计算断言 ③。**屏幕阅读器完整支持不在本项验收内**，见 §9 |
| **N9** | **可复现的效果数据**：能说"这一版比上一版好多少" | SHOULD | 对 S4。**没有基线，所有改进都无法证明** |
| **N10** | 多端可用（至少手机能看错题本） | MAY | 复习发生在碎片时间 |
| **N11** | **可寻址与可回退**：URL 能表达界面状态，浏览器返回键有效 | MUST | 这不只是可访问性问题，**是功能缺陷**：刷到第 3 步提示不能刷新、不能分享、不能回退。缺它，各页面会各自发明一套状态管理（v2 交付 **7 个页面 + 1 个未排期的 demo 页**，清单见 `docs/03 §5`） |
| **N12** | **形态可扩展**：上下文来源、身份、事件、提示四者**解耦**，契约中不得写死"仅主站可用"的假设 | MUST | 这是"探针后置"唯一的技术代价控制手段。**后置不等于放弃** —— 若把主站专属假设写进契约，将来做探针就是重写而不是新增 |
| **N13** | **评测资产为一等交付物** | MUST | **拆成两句**（口径澄清，理由与价格见 §6 表后）：① **样例集**（小、公开，3–5 题）与**运行器**入主仓并**纳入门禁**（本地 `scripts/verify.sh`；接远端后该脚本即 CI 入口），缺失即红灯 —— 负责**防退化**；② **冻结正式集**（注入集 20 道 + 冻结验证集 + **期望判据表**）由**非实现者**保管且**不入主仓**，在阶段门 C 由非实现者执行 —— 负责**能力验收**。理由：N1 是 MUST 而其验收**完全依赖**评测资产 —— 使能项比被支撑的需求低一级属自相矛盾（与 F8 提级同一逻辑） |
| **N14** | **用户自持凭据**：允许用户用**自己的**模型凭据接入；系统**不内置、不共享、不外传** | MUST | 取代 N3 的"数据不出校"部分。**凭据只存浏览器本地、随请求携带、服务端不代为持有**；**硬约束：凭据不得落日志、不得进审计 detail、不得进错误信息、不得进 L2 表**。**无凭据 → 答疑不可用**（判定 / 错题本 / 知识图谱仍可用），理由：不给一个会产出假答案的 mock（N2） |
| **N15** | **外传告知**：必须显著告知"提问会被发送到第三方模型服务"，设置页随时可查 | MUST | 它是 **N2 诚实**的组成部分而不是附加项 —— 诚实不只是"不编造答案"，还包括**说清数据去了哪**。**化名账号（N6）、删除权边界（F16）、审计保留三项的正当性都建立在"用户知道数据去了哪"之上**：若用户不知情，这三条就变成在替他做决定。★ **条件化（2026-09-16 新增 · 同日二次修正）**：内容 = **实际去向**（供应商名 + 是否远程）+ **载荷范围**（提问原文 + 上下文）+ **删除权边界句同屏**。★ **判据是"去向"，不是"部署形态"（二次修正）**：`egress = remote`（**P2**）⇒ 文案"**提问会发送到第三方模型服务**" + 供应商名；`egress ≠ remote`（**P1 本机端点 / P3 无凭据**）⇒ 文案"**数据不发往第三方**"。**为什么不能按形态判**：`docs/02 §1.5` 的**单机形态包含 P2**（单机 + 远程端点 + 凭据）—— 按形态判会让"单机 + 远程端点"的学生看到"数据不发往第三方"，而提问正文**正在**发往第三方，这正是 **N2 诚实**要防的事。触发粒度**每账号一次**，`notice_ack_at` **落服务端**（若只存浏览器，**换台机器就不再告知**，而机房换机器是最常见情形），**读写口见 `docs/02 §6.1`** |
| **N16** | **部署形态与端点可控** | MUST | 系统必须支持**两种部署形态**：**单机**（答辩 / 教师自用）与**远程托管**（正式服务，见 N17）。**端点与凭据由用户提供**（N14）。**单机形态下支持本机端点**（loopback）⇒ **断网可完整演示**；**远程托管形态下只有 P2（用户自配远程端点）与 P3（无凭据）两档，loopback 一律拒绝**（学生填的 loopback 是**服务器的** loopback，既无正当性也是 SSRF 面）。四条验收：① 单机 + 本机端点时**断网**走完整链路（含一次答疑）且 `qa_log.egress='loopback'`；② 设置页展示的去向与 `SELECT DISTINCT egress FROM student_qa_log` **一致**（★ **前置：该账号至少完成过一次答疑**（2026-09-16，决策 5A）—— 干净账号该查询为空，断言**既非真也非假**；P3 下设置页展示"当前无凭据、未发生外传"）；③ 远程配置下用**假 provider** 抓载荷，断言不含禁止字段（见 `docs/02 §1.5④` 载荷白名单）；④ **登出后**浏览器本地不再保留凭据与端点配置。**边界（写进格子，防误读）**：本机端点由**用户自备**（我们**不内置、不打包模型权重**）⇒ **N4 不收窄回来**，**N16 不承诺"装上就能用"**。★ **端点信任边界与 `egress` 枚举已于 2026-09-16 落进 `docs/02 §4.2.1` 与 `docs/02 §3.1`**（此前 `ADR-0005` 的 §4 定了六条，而正文零落点） |
| **N18** | **传输加密** | MUST | **远程托管形态下，凭据与令牌每天都在公网上走** —— N14 把模型凭据放在请求头里、F15 把会话令牌放在请求头里，而**没有一条需求要求这条链路是加密的**；N17 已经承诺了学生在我们的服务器上有账号，缺这一条等于只承诺"存"、不承诺"守"。验收四条：① 远程托管形态下令牌与 `X-Model-Key` **只经 HTTPS** 传输，**明文 HTTP 请求被拒**（重定向或 400 并说明）；② 前端**无任何第三方脚本 / CDN**（与"零依赖"同源，可断言 `index.html` 无外部 `<script src>`）⇒ **凭据不存在跨站外发面**；③ 单机形态（P1 + 本机访问）允许 http，但**必须与 P1 的"本机端点"显式勾选共用同一开关**（口径同 N16）；④ 响应头含 `Strict-Transport-Security`。**不做**：证书自动化与 HSTS 预加载（属部署，`docs/06` 记为脚本化部署项） |
| **N19** | **数据备份与恢复** | MUST | **N17 把学生数据放在我们运营的服务器上，就必须承诺它不会因为一次磁盘故障而消失。** F16 给了学生"删掉我的数据"的权利，却**没有任何需求保证"你不删的时候它还在"** —— 而 `.gitignore` 把 `backend/data/` 整目录排除，学生的唯一副本就在那块磁盘上。验收四条：① 备份落**部署主机本地** `backend/data/backups/`（**同机另一个卷 / 目录**，仍在 deny-by-default 内），★ **不构成第三个出网口**（见 `docs/02 §1.5③`）；② 备份内容 = **L2 的 SQLite 库**（`backend/data/*.sqlite`，编号见 `docs/03 §2`；★ **原文写"三库"，而全仓只定义了一组 `*.sqlite`**）+ `content/MANIFEST.json` 的 `rev` 快照，**不含日志**（R-O 已定日志只记 ID 与动作）。★ **取法**：开了 **WAL**（`docs/02 §3.6`）之后**不得文件级 `cp`** —— 必须用 `sqlite3.Connection.backup()` 或 `VACUUM INTO` 取**一致快照**，否则会出现"备份成功但少了最近的作答"；③ **恢复演练**：从备份恢复后 `student_user` / `student_attempt` / `student_mastery` / `student_hint_log` 的行数与备份时刻一致，且**能被登录与错题本读到**（读回断言，与 F16 同型）；④ 备份失败**可观测**（写一条只含 ID 与动作的日志，符合 R-O）；★ **备份新鲜度可查**：`/api/health` 或管理页须显示**最近一次成功备份的时间与结果** —— 只记"失败"会让"备份停了三天"看起来一切正常（health 仍只报本地事实，不构成第三个出网口）。**不做**：异地 / 多副本备份 · 在线热备 · 点对点恢复 —— 放开任意一项都须走 ADR |
| **N20** | **容量与并发** | MUST | **全文此前只有"只允许单 worker"这一句约束，没有任何容量目标** —— 而部署形态已经是"为学生提供在线服务"，于是"够不够用"无法判定、也无法排练。**承诺的规模是"一个班 / 一门课"**：约 **50 名在读学生**、**峰值 10 路并发答疑**；★ **判定 / 列表接口的口径（2026-09-16 补全，原文引"与 F4 同口径"而 F4 只约束判定链）**：**判定链路**（刷题：归一化 → 判定 → 错因匹配）在**并发 50 路**下 **P95 < 1s**，样本 = **每路同一题连续 20 次**，**固定窗口**；**列表接口**（错题本 / 进度 / 知识树）在同一并发下 **P95 < 1s**，样本 = **每路 20 次同参数请求**；两条**只对 `gradable=true` 的题目与已登录账号**统计。★ **边界写死**：**第 11 路起**并发答疑即超限，**如实返回 503**（`docs/02 §8-13g` 的断言依赖"10 路内不得 503、11 路必须 503"这一对）。验收四条：① 10 路并发答疑时 `/api/health` **仍 < 100ms**（与 `docs/02 §8-7` 同口径）；② 其中任意 5 路**中途断流** ⇒ 图被取消、**无残留任务**，剩余 5 路不受影响、`seq` 单调且 run 隔离（**这是"断开即取消"必须成立的验收**）；③ 10 路答疑 + 50 路判定的复合压测下**无 5xx、无写库冲突报错**（WAL + `busy_timeout` 生效）；④ **以上均在 `worker=1` 下通过**（单 worker 约束不放开）。**不做**：多 worker · 外部检查点存储 · 水平扩展 · 限流排队（超限**如实返回 503 并说明**，与 N2 诚实一致） |
| **N17** | **受控托管**：学生数据**只存于我们运营的服务器**，不流向第三方（**除模型调用载荷**） | MUST | ★ **2026-09-16 新增，因为最终部署形态是远程服务器**。它把三条既有需求的口径从"本地库"改写为"**受控托管**"：**N6**（化名账号：从"只落本地库"改为"只落**我们控制的服务器**"）· **F16**（删除权：从"本机全部数据"改为"**服务器上属于该用户的全部数据**"，且**不含日志文件**——日志按 R-O 只记 ID 与动作）· **审计保留裁定**（前提从"`user_id` 映射不落在系统之外"补充为"**映射不落在我们控制的服务器之外**"）。**如实声明边界**：远程托管下 **"不外传"≠"数据不出学生设备"** —— 学生数据**存在我们的服务器上**，只是不发往第三方模型服务。★ **三条改写已于 2026-09-16 落进 N6 / F16 / `docs/02 §3.1` 的正文**（此前本格只是"声明改写了"，而正文未改 —— 那是本轮 P0-2） |

---

## 5. 一条需求层面的伦理约束（不是功能）

**F1–F6 做得越好，工具越接近"作业代写器"。** 这是需求内在的矛盾，必须在产品层面缓解：

| 缓解手段 | 机制 |
|---|---|
| **提示阶梯（F6）** | 默认先给方向，完整解析需学生先尝试 —— 把"抄答案"变成"要点两下"的摩擦 |
| **不做"直接出最终答案"的入口** | 把"结论"放在"过程"之后 |
| **教师可见（F15 的教师角色，经管理页）** | 让 S3 知道工具在干什么，而不是被绕过 —— 落点是**已落地的管理页**（★ **学生明细逐条查看**，2026-09-16 由"班级统计"改写 —— `docs/02 §6.1` 已判定该词与 §9 的"不做跨用户聚合"**意思相反**），不是待建报表；F14 的学生自陈视图是同一目的的补充视角 |
| **如实记入掌握度** | "用了 3 次提示才答对"照实记。数据上不美化 —— **这正是 `hint_used` 字段存在的理由** |

> ★ 推论：**"提示阶梯"不是体验优化，是伦理约束落地的地方。** 它必须在数据模型里有对应字段（用了几次提示），否则约束**无法执行、也无法度量**。
> 这条把 F6 从"交互设计"提升为"必须产生数据的需求"。

---

## 6. 什么算"做成了"——成功标准

| 维度 | 对应需求 | 达标线 | 怎么测 |
|---|---|---|---|
| 解题正确率 | F2 · F17 | 在**冻结验证集**上 ≥ **回归基线**（首次全量测量值，于阶段门 B 结束时录入本表并冻结；**只许上调，下调走 ADR**）。**这是回归门槛，不是质量门槛** —— 它防止退化，不假装我们知道绝对水平 | 固定题集 + 固定评分脚本（测量条件见"判定准确率"行） |
| 判定准确率 | F4 | 在**冻结验证集**上、**单用户本地**、**进程已启动且首次请求已完成**的条件下：**数值题**（`answer_kind=value`）100% 由确定性代码判定；**应用题**（`answer_kind=text`）允许表达误差。★ **统计范围 = `gradable=true` 的题目**（`gradable=false` 的题不进判定链，`correct=NULL`，见 `docs/02 §3.3`） | 对照标准答案。★ **这是六条核心验收里最容易自动化的一条** —— 确定性代码对照标准答案，无需人工门禁 |
| **判定延迟** | F4 · N7 | **纯计算题**（`answer_kind=value` 且 `gradable=true`）端到端 **< 1s**；判定链路中**无 LLM 调用**。**测量样本数：同一题连续 20 次取 P95**。★ **适用范围：只约束判定链**（刷题：归一化 → 判定），**不覆盖答疑链**的生成 / 验证 / 重算 | 计时 + 调用计数断言（延迟定级以 F4 为准，N7 不重复定级） |
| **讲解首字** | N7 | 讲解首字 < 2s | 计时（这一项才需要"缓存已热"的定义：**进程已启动 + 首次同类请求已完成**） |
| **错误拦截率** | N1 · N13 | **解答级注入集**上的已知错解 **≥ 18/20（90%）** 被标注存疑或降级，**且每类（5 道）须 ≥ 4/5** —— 防整体容错掩盖单类崩盘。★ **构成要求（2026-09-16 补）**：20 道中**至少 8 道**须落在 **A / B 级可达**的类别上（`sympy_recompute` / `symbolic_equivalence` 可施加），且这些道次的拦截方式**必须是"重算不一致"**，**不得全部靠"手段缺失 ⇒ 降级到 D"** —— 否则这条地板只约束了"有没有一个降级开关"（构成口径见 N1 与 `docs/02 §9.1`）。**地板只许上调，下调走 ADR** | 冻结正式集，由**非实现者**执行（约束见下）；见 N1 |
| **错因命中率** | F5 · N13 | **答案级注入集**上的命中率 **≥ 90%**，**且每类 ≥ 4/5**。**"命中"定义为**：实际命中的错因 ID **集合 ⊇ 期望集合，且不含未期望条目**（单值比较会漏掉"一答案多错因"这种真实情形）。**必带负样本**：确实无对应错因的错误答案须输出"**未识别错因**"，**编造即红灯** | 在**答案级注入集**上统计，走**错因匹配的函数级入口**（`domain.match_mistake(normalize(answer), item)`，**归一化必须在入口内**）；见 F5 与 §6 表后 |
| **诚实性** | F3 · N2 | 一致率 **100%** | **全量断言**（不是抽查）：对每条验证报告断言 `level ≤ 手段允许的上限`（无 `sympy_recompute` ⇒ A 不可达；无 `symbolic_equivalence` ⇒ B 不可达）**且** `conf ≤ cap(level)`（上限表见附录 A 待落地项） |
| **越权隔离** | F15 | 学生 A 无法读到学生 B 的任何数据（含聚合与导出） | 越权用例：以 A 的令牌访问 B 的资源 |
| **删除读回** | F16 | ★ **统一为四类处置句（2026-09-16）**：**四张数据表**（问答流水 / 提示流水 / 作答 / 掌握度）**硬删**，读回 **0 行** · `student_token` **级联删除**，读回 **0 行** · `student_user` **脱敏**（行保留、不可登录、`notice_ack_at` 清空）· `student_audit` **保留**（**只记 ID 与动作**）。★ **显式声明：不含日志文件** —— 日志按 **R-O** 只记 ID 与动作，内容不得落盘 | 接口测试（**逐表读回**，不是"没报错"） |
| **提示阶梯生效** | F6 | 未提交前**不返回**完整解析；每次提示使 `hint_used` 精确 +1 | 接口测试 |
| 闭环完整性（MUST 部分） | **F2 · F4 · F5 · F7 · F8 · F19** | 全通（含 F8 能跨会话读到 F7 的数据）。★ **F2 是解答链的输入**（"逐步判定"的判定对象就是它），**原写法把它漏在表外** ⇒ 本条是"全通"却测不到解答是否产生 | 端到端走一遍并**刷新页面**（只写不读不算通） |
| 闭环完整性（SHOULD 环节） | F9 · F10 · F11 · F17 · F18 | 若缺失，界面必须**显式降级**（"本次未拆分题目"/"未找到出处"/"未定位薄弱点"），**不得静默** | 端到端走查（专测缺环时的降级话术） |
| **可访问性** | N8 | **三项各自独立验收**：① 点击目标 ≥ 32px（**可自动**）· ② 键盘可达 + 语义标签（**可自动**）· ③ **对比度 ≥ 4.5:1（仅对"声明的色对"可自动；渲染结果只做 review）** | ①② 由前端冒烟断言；③ 由**色对清单 + 纯 Python WCAG 计算**断言，**并且如实声明它检查的是"声明"而不是"渲染结果"**（渲染结果需无头浏览器，与"零安装"前提冲突 ⇒ 不做，已记账） |
| **复习有效性** | F7 · **F8** · F19 | 同一知识点**重做首次正确率**相对首次作答的提升。**只承诺"能从作答记录算出来"，不设百分比门禁**；样本不足时必须显示**统一文案**"**数据不足，暂不显示薄弱点**"（与 F11 同源，2026-09-16）。**首次正确率的单位 = `(user, item)` 的第一次 attempt**，指标实际来自错题重做（`source=wrong_review`），故 F8 也是它的来源 | 从作答记录按知识点分组统计。**观察指标，不是验收项** —— 见下方说明 |
| **可用性 · 单机完整**（含答疑） | **N16 · N14** · N1 | **联网 + 有效凭据**（P2）走完全链路；首次使用出现外传告知，**文案必须含供应商名与载荷范围**（因为 `egress=remote`） | 端到端演示 |
| **可用性 · 单机离线**（含答疑） | **N16** | **P1 本机端点 + 断网**走完整链路（含一次答疑）；`qa_log.egress='loopback'`；告知文案为"**数据不发往第三方**" | 端到端演示（**断网**） |
| **可用性 · 远程完整**（含答疑） | **N14 · N15** · N1 | 远程托管下联网 + 有效凭据；告知文案含**实际去向 + 载荷范围 + 删除权边界** | 端到端演示 |
| **可用性 · 降级**（**不含答疑**） | N16 · F4 · F8 · F9 · F11 | 无凭据跑完**判定 / 错题本 / 进度 / 知识图谱 / DAG 浏览**；答疑**不可用且如实说明**（不是报错，是"未配置凭据"） | 端到端演示（不含答疑）。★ **依据 `docs/02 §1.5` 的能力 × 依赖矩阵**：把"需网络 / 需凭据"两列全为 ❌ 的能力连起来即本行清单。★ **本行与上一行原引 N4，已改（2026-09-16）**：N4 的口径已收窄为"免安装"，其格子明写"删去原'离线可跑'"，**用 N4 支撑一条"断网验收"正是本文自己警告的那种漂移**；离线能力来自 **N16 的 P1 本机端点** |
| **传输加密** | **N18** | 远程托管下**明文 HTTP 请求 0 通过**；前端**0 个外部脚本** | 明文 HTTP 请求被拒（断言状态码 / 重定向）+ 断言 `index.html` 无外部 `<script src>` |
| **备份可恢复** | **N19** | 从备份恢复后四张表的行数与备份时刻一致，且**能被登录与错题本读到** | 备份 → 删库 → 恢复 → **逐表读回**（同 F16 的读回纪律，不是"没报错"） |
| **容量与并发** | **N20** | 10 路并发答疑下 `/api/health` < 100ms（**第 11 路必须显式 503**）；5 路中途断流后**无残留图**且无事件串台；10 路答疑 + 50 路判定的复合压测下**无 5xx、无写库冲突**（判定 / 列表 P95 < 1s，样本与窗口见 N20 格子） | 并发压测 + 断流注入 + 残留任务断言（均在 `worker=1`） |
| 可寻址 | N11 | 任一界面状态可复制 URL 到新标签页还原；返回键有效。**须附"可寻址完备清单"**：每个 feature 的每个持久状态都对应一条路由（含 `#/settings`、题目 / 提示层级 / 筛选组合 / 翻页位置） | **路由冒烟（每 feature ≥ 1 断言，断言 URL ↔ 视图双向一致）+ 手工走查抽查**（原为纯手工走查，是 MUST 里最弱的一格） |

**两条对账规则**：

- **方向 A（防凭空）**：**§6 每个维度必须挂 ≥1 个需求 ID**，且该 ID 必须在 §4 存在 —— 挡住"冒出一个谁也没要求过的指标"。
- **方向 B（防漏项）**：**§6 不承担全覆盖责任**（它是对外证明，不是内部分解）；未进 §6 的 MUST 需求，必须在 [`06-开发计划与阶段门`](06-开发计划与阶段门.md) 有**独立**阶段门动作 —— 与附录 B 已有的规则合流，不新造机制。★ **已落（2026-09-17）**：`docs/06 §2.2` 为**恰好 7 条**（F1 · F20 · N4 · N5 · N6 · N12 · N17）各给了独立动作，并写明它们**为什么**不在 §6（它们是**性质**而不是**指标**）。

**闭环两行的分级依据**（可机械核对 —— 环节的级别决定它落在哪一行）：

| 环节 | ① 题目拆解 | ② 公式检索 | ③ 逐步判定 | ④ 即时批改 | ⑤ 教材出处 | ⑥ 错题归类 | ⑦ 依赖上溯 + 进度 |
|---|---|---|---|---|---|---|---|
| 需求 ID | F17 | F18 | F5 | F4 | F10 | F8 | F9 · F11 |
| 级别 | SHOULD | SHOULD | MUST | MUST | SHOULD | MUST | SHOULD |

**MUST 行** = ③ ④ ⑥ 三个 MUST 环节 + **F7**（记录前提）+ **F19**（数据支撑）。
**SHOULD 行** = ① ② ⑤ ⑦ 四个 SHOULD 环节。
> 规则：**MUST 达标线里不得混入 SHOULD 环节**（那会造成"用 SHOULD 支撑 MUST"，正是 G4 的形态）。级别一变，先看它该落在哪一行。
> ★ **两条限定（2026-09-16 补）**：① **F1 出现在本表但不在七环之内**（七环见 §3），它的级别（MUST）不影响本表任何一行；② **F1 与 F10 在 v2 属"部分"交付**（§1.1），**被后置的那一部分（注入式读屏 / PDF 溯源定位）不计入本表任一行的达标线** —— 否则"全通"会变成一句永远为假的话。

**记法禁令**：**禁止使用需求 ID 区间记法**（原"F1→F8 全通"已改为显式 ID 列）。
理由有二：① 与附录 B"编号是外部引用锚点、只增不改"冲突 —— 将来在区间内插入一条 MUST，会被**静默**算进闭环验收；② 与 §3 的七环定义不符 —— 七环是 ①F17 ②F18 ③F5 ④F4 ⑤F10 ⑥F8 ⑦F9+F11，**F1 并不在七环之内**。
（§3 里的**环节编号区间** ①–⑤ / ⑥⑦ 不受此禁令约束：它们不是需求 ID，见附录 C。）
> 另注：**可用性一行的拆分（2026-09-16）**删去了原"**断网演示**" —— 完整链路现在依赖网络与用户凭据（N14），"断网可完整演示"已**不可能成立**。改为**两条独立验收**，并如实承认降级演示**不覆盖答疑**（§1 代价四）。

**评测资产的三条独立约束（N13 的可执行形式）** —— 防"出题人与被评人是同一批人"：

1. **冻结验证集**：从题库切出，**出题记录不进实现人视野**（执行形式见下表）；
2. **版本号 + 冻结日期**：注入集与验证集都带版本；并设**分类地板**（每类 5 道须 ≥ 4/5），防止整体容错掩盖单类崩盘；
3. **阈值不得在看到结果之后调整测法**；评测资产由**非实现者**执行。

**N13 与"完全盲测"的相容解**（刻意记账，因为它有真实价格）：

| 资产 | 存放 | 谁执行 | 自动化 |
|---|---|---|---|
| **样例集**（3–5 题 + 期望结论） | **入主仓** | 本地门禁（接远端后即 CI） | **自动红灯** —— 保住防退化价值 |
| **冻结正式集**（**解答级注入集** + 冻结验证集） | **不入主仓**，非实现者保管 | 阶段门由非实现者手工执行 | **人工门禁，不能自动红灯** |
| **运行器** | **入主仓**（`scripts/eval_verify.py`，**不含任何答案**） | 任意人（**正式集的执行结果只能由非实现者签署**） | 可 review |
| **期望判据表** | 与正式集同处，**不入主仓** | 非实现者编写 | — |

> 为什么必须这样拆：**只要注入集进了仓库，实现人就能读到**，盲测失效。★ **理由已换锚（2026-09-16）**：原写的是"离线、无密钥基础设施的前提下（ADR-0001），门禁无法从仓库外取题"—— 那个前提**已不成立**（v2 联网、且用户自持凭据）。**结论不变，但理由改成**：为一件**一次用一次**的验收，引入**外部托管位置 + 一套取题凭据**，成本高于收益；而"正式集不进仓"这条隔离要求**与能不能联网无关**。
> **为什么把"评分脚本"拆成"运行器 + 期望判据表"**：运行器本身**必须可 review** —— 否则没法确认它真的在数命中，而不是在数"没报错"。把**机制**留在仓库、把**答案**移出仓库，两个目的同时达成。
> **价格已记账**：N1 的正式验收**不能自动化**；需要**至少两人不可互换**（出题人 ≠ 判定/验证实现人）；若判定引擎只有一人实现，**此人不得参与出题**。若将来排不开人手，退路是降为"最小可行隔离"（出题人与实现人分开），届时须走 ADR 说明为何放弃盲测。
> ★ **两份具名注入物（2026-09-16 定死）** —— 上一版的"注入集"一个名字装了两种东西，导致 F5 的指标在声明的注入点上**测不出来**：
> - **解答级注入集 → 服务 N1**：注入物 = **系统错解**（一道题 + 一个已知错误的**解答**）；注入点 = **验证链的输入**（函数级、确定性、可批量）；断言 = 该解答被标注"存疑 / 不一致"或级别被降。**不走 `/practice/grade`**（那条链路只比对学生的答案，测不到"系统自己的解答对不对"；且任何错答案都会被判错 ⇒ 门禁**平凡通过**）。
> - **答案级注入集 → 服务 F5**：注入物 = **学生错答案**（一道题 + 一个已知错误的**答案** + **期望错因 ID 集合**）；注入点 = **错因匹配的函数级入口**（`domain.match_mistake(normalize(answer), item)` —— **归一化必须在入口内**，否则测的是现实中不存在的路径）；断言 = **实际命中集合 ⊇ 期望集合，且不含未期望条目**。**必带负样本**（确实无对应错因的错误答案）⇒ 断言"**未识别错因**"，防编造。
> - **端到端 3–5 条**放进**样例集层**：只承担**回归保护**（"这条链真的接上了"），**不承担 N1 / F5 的能力验收**。

> **为什么"复习有效性"不设门禁**：它需要**同一个学生反复练同一知识点**才能算出，而样本量取决于真实使用，不由我们决定。
> 因此本文只承诺**能力**（能算出来）与**诚实**（算不出时显示"数据不足"），不承诺**数值**。把它设成门禁只会逼出一个假数字 —— 那正是 N2 要防的事。
> **代价已记账**：它不是承诺，只是能力。

---

## 7. 需求 × 交付形态

本仓 v2 **只做主站**。下表把"需求成立但形态后置"的部分明确标出来 —— 这是本仓与上一个仓库最主要的口径差异。

| 需求 | v2 的交付形态 | 后置的部分 |
|---|---|---|
| F1 带上下文提问 | **主站内上下文**：题目 / 知识点 / 教材片段 | 注入式读屏（宿主页面正文与选区）→ 后置 |
| F17/F18 拆解与检索 | 主站内（吃 §4 F20 的内容资产） | — |
| F19 知识点标注与 DAG | 数据模型层，主站持有 | — |
| F10 教材出处 | 本地语料检索 + 出处标注 | PDF 阅读器与教材溯源定位 → 后置 |
| F12 可视化 | 主站动画库 + 分布可视化 | — |
| F13 协作可见 | 主站轨迹页 | — |
| F20 内容资产 | 自制资产入库并版本化（教材语料除外，见 N3） | — |
| N5 不篡改平台 | **条件生效**（无注入则无篡改面） | 探针落地时立即生效，红线不变 |
| N12 形态可扩展 | 契约层面现在就守住 | — |

**判定规则**：一条需求若"形态后置"，它在 `docs/06` 里**不得**被算作已完成；只能是"未开始（形态后置）"。禁止用主站的一部分满足度去抵充探针的缺失。

---

## 8. 建仓承诺：上一个仓库教给我们的 12 条

> 本节是本仓**从零开始的全部理由**。以下每条都有上一个仓库的实证来源，因此**不是风格偏好，是踩过的坑**。
> 每条都给出"谁来守" —— 只有能进门禁的承诺才算承诺。

| # | 承诺 | 来源（旧仓实证） | 对应需求 | 谁守 |
|---|---|---|---|---|
| 1 | **拦截率是结果指标**；不接受"走了验证流程"作为交付证据 | 验证级别在真实链路上**恒为 D**（分级 A/B/C 实际不可达），代码"走了流程"却一步真验证都没做 | **N1**（**N13** 为使能项） | `docs/06` 阶段门 + N1 拦截率测试 |
| 2 | **作答记录不被任何"合规门槛"挂起** | `attempt` 契约字段设计完备（含知识点/提示次数/用时），却因被归入"需知情的建模线"而整体挂起 —— 而错题本要的正是它。**本地落库、化名、逐题可删的记录本就不触发那个门槛** | **F7 · F8 · F19**（N6 · **N15** 为合规边界） | §4 F7/F8/F19 均为 MUST；契约状态不得为 reserved |
| 3 | **提示必须进数据模型** | `hint_used` 字段被设计出来并标注"最容易漏"，但无任何实现把提示映射到计数 | **F6**（F14 引用其数据） | F6 验收 + 字段-需求对照（附录 A） |
| 4 | **进度数据源是作答记录，不是问答记录** | 统计接口读的是问答流水，于是"问了 100 次"会显示成"学习了 100 次" | **F11**（F14 引用 F11 的数据源） | F11 验收 |
| 5 | **路由必须可寻址** | 前端路由只写 `location.hash`、无人监听 `hashchange`，实测返回键只变 URL 不变视图 | **N11** | N11 + 前端冒烟测试 |
| 6 | **确定性判定走 REST，且必须有出口** | 判定引擎 521 行、16 项测试通过，但全仓 21 个路由**没有一个批改端点** —— "引擎有、通路无" | **F4** | F4 + 契约 `producer/consumer` 零悬空 |
| 7 | **可访问性是一等需求** | 正文对比度实测 **2.56:1**（需 4.5）、7 个点击目标 < 32px、语义标签全仓 5 处 | **N8**（本次已提为 MUST） | N8 写入契约与页面模板 + 前端冒烟断言 |
| 8 | **超长输入与正则回溯按建仓规则处理** | `/api/practice/grade` 曾**冻结整个事件循环**；判定正则对 100k 位数字耗时约 **2 分钟** | —（属**规则 R-A**，非需求） | 建仓规则 R-A |
| 9 | **写库必须显式提交，测试必须读回** | 写函数缺少一次提交，作答记录被静默回滚；测试当时只断言"没报错" | —（属**规则 R-B**，非需求） | 建仓规则 R-B + 测试规范 |
| 10 | **演示叙事不得吃掉产品代码** | 协作可见类页面（轨迹/架构/助手）约 1 400 行，是前端最大代码块；而功能页很薄 | —（约束的是**配额**，F13 仅是 MAY） | **可机检上限：演示类页面总行数 ≤ 产品功能页总行数**（脚本按 feature 分组统计）。**这是 review 门禁，不是硬红灯**。**反诱导声明**：不得为过关把功能页写肥 —— 出现该情形即视为违背承诺 10 的意图 |
| 11 | **一主题一篇文档** | 旧仓分工写了 3 篇、计划写了 3 篇、地图写了 2 篇，而**系统设计一篇都没有** —— 冗的是重复，缺的是核心 | —（属**文档机制**） | `docs/07` 单一权威登记 —— ★ **已产出（阶段 A4）**：见 `docs/07 §1` 文档清单 · `docs/07 §4` 主题索引 · `docs/07 §6` 与 `docs/03 §11` 的分工 |
| 12 | **契约零悬空、缺口补齐** | 最热的问答接口、错误体、分页、作答、掌握度**全无契约**；每端各写各的形状 | **N13** + 全部契约（附录 A 的待落地表） | 契约校验（`docs/04`） |

> **"对应需求"为空的行不是漏填**：第 8–11 条约束的是**怎么做**（建仓规则 / 配额 / 文档机制），不是**做什么**。
> 给它们硬安一个需求 ID 才是错的 —— 那会造成"同一件事两处定级"的漂移（正是本次评审 G4 指出的问题）。
> 本列的作用只有一个：**让"承诺"与"需求清单"能互相核对，谁的条目落空一眼可见。**

**两条建仓规则**（承诺 8、9 的落地形式，进 `scripts/verify.sh` 自动扫描）：

- **R-A**：所有请求体中的字符串字段**必须**显式设上界（`max_length`）；判定类正则**禁止嵌套量词**。
- **R-B**：任何写库函数**必须**显式提交；对应测试**必须**读回断言，不得只断言"HTTP 200"。

**一条人力规则**（**脚本查不了，只能靠人** —— 正因如此更要写下来）：

- **R-C**：内容资产（**F20**）与编排层（`graph/`）**各须有明确责任人**，`docs/05` 的责任人表**不得留空位** —— **空缺即该需求不得计为已立项**。★ **五类内容资产须至少两人分担（2026-09-16，决策 9A）**，且**资产产出必须进 `docs/06` 阶段门 B**（F5 的地板要靠它）。★ **已落（2026-09-17）**：责任人见 `docs/05 §2.2`（五类落在**三个域**，F5/F17/F18 三条流水不共用瓶颈），门与判据见 `docs/06 §7`。**为什么把"至少两人"也写进规则**：五类资产的下界是题库 ≥30 / DAG ≥40 节点 / 错因库 ≥40 / 题型 ≥15 / 公式库 ≥30，而 **F5 / F17 / F18 分别吃错因库 / 题型库 / 公式库** —— 三条需求对应三条独立的产出流水，压在一个人身上会让它们**同时**挂。
  为什么它值得单列：旧仓把这五类资产明确写为"需要自制，工作量可控，但**没人能替你写**"，而当时的任务书里**没有任何一处**为它们指定人。结果就是"要求它存在，但没人造它"。

---

## 9. 本仓不做（明确边界）

**v2 永久不做**：npm / 打包器 · ORM · Postgres · Docker 化生产部署 · i18n · 多端适配（N10 为 MAY）。
**v2 不做（运维面已立项，但边界写死）**：**多 worker 与外部检查点存储**（单 worker 保留不放，放开须走 ADR）· **异地 / 多副本备份**（N19 只做同机备份，目标是同机另一个卷 / 目录）· **限流排队与高可用**（N20 超限**如实 503**）· **证书自动化与 HSTS 预加载**（N18 只要求加密传输与拒绝明文）。
**v2 本阶段不做（形态后置）**：注入式探针（读屏） · 划词交互 · 驱动宿主动画 · PDF 阅读器与教材溯源定位。
**v2 不做（数据门槛仍在）**：**跨用户聚合**与**教师侧建模分析**（成绩分析报表 / 掌握度热力图 / 跨班对比）—— 这些依赖真实学生数据与知情同意，**与"作答记录"是两件事**（见承诺 2，别再混为一谈）。
**本条的边界（必须写死，否则会重演旧仓的失败模式）**：本条**仅**指跨用户聚合与教师侧建模；**仅本人可见**的作答记录、掌握度与进度视图属 **F11 / F14 / F19，不受本条约束**。本机范围内的学生明细查看由管理页承担（F15 的教师角色）。
> 为什么这条边界必须写出来：旧仓的作答记录契约正是"因被归入需知情的建模线而**整体挂起**"，而错题本要的正是它。下一个接手者完全可以说"F11 的薄弱视图就是学习者建模"，然后整条线停摆 —— 这句话就是防这个读法的。
**v2 不做（无验收项）**：屏幕阅读器完整支持 —— 语言层输出无法机检，写进 MUST 会变成一扇永远红的假门禁，写 SHOULD 又假装将来会做。**不做就是不做**（见 N8）。

---

## 附录 A · 需求与数据的对照规则（防"由界面反推数据模型"）

数据模型**不是**"页面要显示什么就加什么字段"。
**规则**：字段-需求对照表放在 [`02-系统设计`](02-系统设计.md)，且必须满足两条：

1. **每个字段能指出它支撑哪个需求 ID** —— 说不出的**删掉**；
2. **每条 MUST 需求至少被一个字段或一个接口支撑** —— 说不出的**补上**。

> 上一个仓库的 6 处需求缺口，正是"先画界面、后凑字段"的产物。这条规则的成本接近于零，但它是唯一能挡住那种漏法的机制。

**必须在 A3（`docs/03` + `contracts/`）指定数据侧落地位置的需求** —— 避免需求里写"写入契约"而契约里查不到：

| 需求 | 要落进的**位置**（**数据侧 → `contracts/` · 传输与运维侧 → `docs/03`**） |
|---|---|
| **F15** | 账号与角色的字段定义、越权语义（学生不得读到他人数据） |
| **F16** | 删除接口的请求 / 响应形状，以及"删除范围"口径（**四张表**：问答流水 + 提示流水 + 作答 + 掌握度） |
| **F3** | 验证报告的字段与**级别标签的渲染口径**（后端发了前端不看等于没发） |
| **F6** | 提示阶梯的请求 / 响应形状、**`student_hint_log` 的字段**与 `hint_used` 的服务端计数口径 |
| **F7** | 作答记录的字段、**标准答案快照**与**资产版本号**（`content_rev` 及其反解链） |
| **F19** | 作答记录的知识点字段、知识点的标识形式（DAG 节点 ID 口径） |
| **F20** | 五类资产的**文件格式**（含题库的 `gradable` 标记）与 `content/MANIFEST.json` 的形状 |
| **F10** | L1 语料的 `cite_id` 形状与检索响应里出处的表达 |
| **N13** | 评测资产（**样例集** / **冻结正式集** / **运行器** / **期望判据表**）的格式、**存放位置**与门禁接入点 |
| **F3 · N2** | **置信度上限表** `cap(level)`：旧仓 schema **只定义了 A / C / D**（A 通过 1.0 · C ≤ 0.85 · D ≤ 0.4），**B 级没有定义** —— 本仓须补齐，否则 §6 的 `conf ≤ cap(level)` 断言无法全量执行。★ **口径已在 `docs/02 §4.4` 定下**（含 B 级），A3 只负责落成契约 |
| **N18** | 传输加密的落点：**HTTPS-only 中间件 / 重定向**、`Strict-Transport-Security` 头、以及"单机允许 http"的**显式开关**（与 P1 的本机端点勾选共用）。★ **落点（2026-09-17 核）**：**响应头与中间件不是对象形状，契约里没有也不该有这一项**（`Strict-Transport-Security` 全仓契约 0 命中）⇒ 真实落点 = `docs/02 §8-13e`（HSTS 必须存在）· `docs/03 §8.1`（TLS 行：远程强制 HTTPS / 单机共用开关）· `docs/01 §6` 的"传输加密"验收行 |
| **N19** | 备份产物的**路径与内容清单**（L2 的 `*.sqlite` + `content/MANIFEST.json` 的 `rev` 快照；★ **取法 = `sqlite3.Connection.backup()` 的一致快照，不得文件级 `cp`**）、以及恢复读回的**断言清单**（四张表）。★ **落点（2026-09-17 核）**：**它是目录与脚本，不是跨端对象**（`backup` 全仓契约 0 命中）⇒ 真实落点 = `docs/03 §8.3`（位置 / 内容 / 取法 / 恢复演练）· `docs/03 §8.2`（`backup.py`）· `docs/06 §6` 演练 ②③ |
| **N20** | 并发上限的**超限响应形状**（503 + `code`）与 `worker=1` 的**启动检测**（`docs/02 §8-15`）；★ **判据：第 11 路起即超限** |
| **F4 · F20** | 题库条目的 **`answer_kind`**（枚举 `value` / `text`）**与它同 `gradable` 的关系**（`gradable=false` ⇒ 不进判定链，两条判定类验收只对 `gradable=true` 统计，口径见 `docs/02 §3.3`）—— 它是 `docs/01 §6` 的**判定准确率**与**判定延迟**两条验收的分类依据，此前只在本文出现、`docs/02` 全篇没有 |
| **N16** | `student_qa_log.egress` 的**枚举取值**（`loopback` / `remote`）与"**P3 不写 `qa_log`**"的口径 —— N16 验收 ①② 都读它 |
| **N14** | **R-M 的七面**（日志 · 审计 `detail` · 错误信息 · L2 表 · URL/query · 前端持久化以外的地方 · 出网载荷本体）。★ **落点（2026-09-17 核）**：★ **只有第 ⑦ 面有对象形状** —— `chat.schema.json` 的 `x-invariants` 末条写着"请求体里**没有任何字段可以承载凭据**（凭据只能在请求头）—— 这是 **R-M 第 ⑦ 面**的结构化形式"；**其余六面是代码纪律**（`docs/02 §1.6` 的 R-J / R-M：grep + 用例 + review），**JSON Schema 表达不了** |
| **F15** | `student_user.must_change_pw` 的字段定义与"**只放行改密接口**"的 403 `must_change_pw` 语义（`docs/02 §5.3`） |
| **N1 · N2** | **验证判定表**（`docs/02 §4.4`）：手段 × 结果 → 级别 / 是否重算 / 是否降 D，**含 B 级 `cap`** |
| **架构侧（`docs/03`）** | **25 个端点的层归属**（`docs/02 §6.1` 的全部 path，脚本数出）· **N12 四解耦**（上下文来源 / 身份 / 事件 / 提示各在哪一层、换来源要改哪几个文件）· **目录骨架**（含 `backend/data/{logs,backups}/`）· **依赖方向的检查方式**（每条可机检或明确标 review）· **R-Q 的施加点** |

> 这张表是**待办登记，不是契约**。A3 完成后，本表每一项都应在 **`contracts/` 或 `docs/03`** 里找到落点 —— ★ **按层分派**：**数据侧（字段 / 枚举 / 对象形状）→ `contracts/`**，**传输与运维侧（响应头 / 目录 / 脚本 / 部署形态）→ `docs/03`**；**两处都找不到才是 A3 漏项**。
> ★ **2026-09-17 修正本节收尾句**：原文写"本表每一项都应能在 `contracts/` 里找到对应定义"，**与本表抬头（"A3 = `docs/03` + `contracts/`"）不一致，且与事实不符** —— N18 / N19 在契约里 0 命中、N14 只落一面（见上三行）。按原文，今天的读者会判定"A3 漏了两项"。

## 附录 B · 需求 ID 的变更规则

- **只增不改**：已有 ID 的含义不得改变；需求变了就新增 ID，旧 ID 标注"已被 Fxx 取代"并注明日期。
- **不重编号**：编号是外部引用锚点（`docs/06` 阶段门、测试用例名、提交信息都会引用）。
- **删除需求**要写明理由与日期，不许静默消失。
- 每条 **MUST** 需求必须在 [`06-开发计划与阶段门`](06-开发计划与阶段门.md) 里有对应的验收动作；没有的就是计划漏了，不是需求多余。★ **已落（2026-09-17）**：`docs/06 §2` 覆盖全部 **28** 条 —— **21** 条引 `docs/01 §6` 的达标线（只补"在哪一步过门"），**7** 条（F1 · F20 · N4 · N5 · N6 · N12 · N17）在此有**独立动作**。

### B.1 · MUST 需求清单（28 条）—— `docs/06` 的机械对照源

| 类别 | ID | 条数 |
|---|---|---|
| 功能 | **F1 · F2 · F3 · F4 · F5 · F6 · F7 · F8 · F15 · F16 · F19 · F20** | 12 |
| 非功能 | **N1 · N2 · N4 · N5（条件）· N6 · N8 · N11 · N12 · N13 · N14 · N15 · N16 · N17 · N18 · N19 · N20** | 16 |

**三条口径提醒**（否则这张清单会被误读成"**28 项**都要做完"）：

1. **N5 是条件 MUST** —— 仅在注入式探针落地时生效。探针后置期间，`docs/06` 里记为"未开始（形态后置）"，**不得计为已完成**。
2. **按 §7 的判定规则，MUST 满足率的上限不是 100%** —— F1（读屏部分）与 F10（PDF 溯源部分，本身为 SHOULD）在 v2 属形态后置。
   这是**明账**，不是遗漏：禁止用主站的一部分满足度去抵充探针的缺失。
3. ★ **N3 已不在本清单内** —— 它于 2026-09-16 **废弃**（"数据不出校"由 **N14** 取代；残余的"教材语料不公开分发"并入 **F20**）。★ **计数的完整变化链：22 → 23 → 25 → 28**：`22 → 23` = 去掉 N3（−1）· 新增 N14、N15（+2）；`23 → 25` = 新增 **N16**、**N17**（+2，`ADR-0005`）；`25 → 28` = 新增 **N18**、**N19**、**N20**（+3，本轮运维面立项）。★ **原写法只写到"22 → 23"，而同节标题写的是 25** —— 那正是 R-P1 要挡的"正文数字没跟表体走"。

> 这张清单是 `docs/06` 阶段门的**输入**，也是唯一的机械对照源 —— 有了它，"某条 MUST 在计划里没有验收动作"就变成一条可勾选的检查，而不是靠人记。

### B.2 · ID 变更记录

| 日期 | 类型 | ID | 变更 | 来源 |
|---|---|---|---|---|
| 2026-09-16 | **新增** | F17 | 题目可结构化拆解（已知 / 所求 / 适用模型），SHOULD | 评审 G2（§3 的 ① 原先无落点） |
| 2026-09-16 | **新增** | F18 | 公式 / 定理精准检索（带适用条件），SHOULD | 评审 G2（§3 的 ② 原先无落点） |
| 2026-09-16 | **新增** | F19 | 作答记录必带知识点标注 + 依赖 DAG（无环），**MUST** | 评审 G3（数据模型层原先只由展示级的 F9 承载） |
| 2026-09-16 | **新增** | F20 | 内容资产可维护（题库 / 错因库 / DAG / 公式库），**MUST** | 评审 G6（原先"要求它存在，但没人造它"） |
| 2026-09-16 | **新增** | N13 | 评测资产为一等交付物并纳入 CI，**MUST** | 评审 G6（N1 的使能项） |
| 2026-09-16 | 定级 | **F8** | SHOULD → **MUST** | 评审 G1（只记不读，与 §8 承诺 2 的失败模式同构） |
| 2026-09-16 | 定级 | **N8** | SHOULD → **MUST**，验收限定为三项可机检指标 | 评审 G4（承诺 7 称"一等需求"却无 MUST 支撑） |
| 2026-09-16 | 验收收紧 | F5 · F8 · F14 · F16 · N1 · N7 | 补命中率指标 / 补跨会话读回 / 改引用 F11 / 定删除范围 / 填 90% 地板 / 声明判定延迟以 F4 为准 | 评审 G4 · G5 · G8 · G9 · G12 |
| 2026-09-16 | 移出验收 | N8 的"屏幕阅读器完整支持" | 移入 §9 不做（无验收项 → 不为它承诺） | 评审 G4 |
| 2026-09-16 | **口径澄清** | **F15** | 角色口径定为"学生 / **教师（管理员）**"，并补可见范围（管理员见本机全体，学生只见自己）与"管理员账号由 CLI 创建" | 评审 R2（§5 声称的缓解手段原先无落点） |
| 2026-09-16 | 验收收紧 | **F15** | 写明"未登录不得访问**任何**学习类接口（含提问与判定），**不设免登录例外**" | 评审 R1（与 §1 S1 摩擦预算正面冲突，裁决为站在 §5 伦理一侧） |
| 2026-09-16 | **记账变更** | §1（非需求 ID） | 明账由"一处代价"扩为**两处**：形态（探针后置）+ **门槛**（零摩擦诉求不被服务，v2 的 S1 实为已注册学生） | 评审 R1 |
| 2026-09-16 | **口径澄清** | **N13** | 拆成两句：样例集入主仓进 CI（防退化）· 冻结正式集由非实现者保管且不入主仓（能力验收） | 评审 R7（"完全盲测"与"纳入 CI"不能同时按字面成立） |
| 2026-09-16 | 验收收紧 | **F2 · F17** | "可被复述"（人工判读）换成**结构断言**；语义部分补判读协议并标注为人工抽样项 | 评审 R8（本文自己的标准是"验收必须可断言"） |
| 2026-09-16 | **记账变更** | §9（非需求 ID） | "数据门槛"加边界句：仅指跨用户聚合与教师侧建模；F11 / F14 / F19 不受其约束 | 评审 R3（原措辞复现了旧仓"记录被门槛整体挂起"的读法空间） |
| 2026-09-16 | 口径 | §6（非需求 ID） | 加"对应需求"列并补四行（延迟 / 越权 / 删除 / 提示）· 闭环拆两行 · **禁用需求 ID 区间记法** · 新增两条对账规则 | 评审 R5 · R6 |
| 2026-09-16 | 验收收紧 | §6 解题正确率（非需求 ID） | 定为**回归基线**（首次全量测量值冻结，只许上调，下调走 ADR），并明写"这是回归门槛，不是质量门槛" | 评审 R4（原"必须先有基线"是与 G5 同类的空阈值） |
| 2026-09-16 | 验收收紧 | **F7** | 补"**标准答案快照 + 资产版本号**" | 评审 R11（题库改版后历史记录无法解释） |
| 2026-09-16 | 口径 | 附录 C（非需求 ID） | 新增术语：**回归基线** · **样例集 / 冻结正式集（盲测）** · **①–⑦（环节编号）** | 评审 R4 · R7 · R6 |
| 2026-09-16 | 新增 | §1.1 | **需求 × 角色覆盖矩阵**（33 行）+ 汇总计数 · **§8 承诺 10 补可机检上限与反诱导声明** | 评审 R12 · R10 |
| 2026-09-16 | **补缺** | **F20 · F17** | F20 的资产由**四类扩为五类**，补入"**题型与解法套路**"（首版 ≥ 15 类，须关联知识点 ID）；F17 验收相应收紧为"第三段的候选模型**必须命中该资产**（`model_id`），不得由模型现编" | 自查发现（A1 定稿前）：F17 的"适用模型"原先无资产支撑，会退化成模型现编。**来源**：旧仓把"题型与解法套路 15–30 类"单列为一份自制资产 |
| 2026-09-16 | **口径变更** | **F6** | `hint_used` 改由**服务端**计数（提示流水），客户端上报值一律忽略；补"只改 URL 的提示层级不能让服务端认为已取过提示" | 评审 A2（原设计下 §5 伦理约束只靠前端声明，改 URL 或直接构造请求即可绕过） |
| 2026-09-16 | **口径变更** | **F16** | 删除范围 **三张表 → 四张表**（新增提示流水 `student_hint_log`） | 评审 A2 连带 |
| 2026-09-16 | **口径变更** | **F20** | 补"题库条目必须带 `gradable` 标记"，并给出**四条首版规模下界**（题库 ≥ 30 道 · DAG ≥ 40 节点且覆盖 ≥ 5 章 · 错因库 ≥ 40 条 · 题型 ≥ 15 类） | 评审 A5（不可判定题目没有出口）· C7 与 `docs/02 §11.2-1`（DAG 只要无环，5 个节点也能过） |
| 2026-09-16 | 验收收紧 | **F7** | "用时"（`duration_ms`）明确为**客户端自陈值，不参与任何验收** | 评审 A2 连带（服务端无"作答开始"记录，为它再加一张表不划算） |
| 2026-09-16 | 验收收紧 | **F2** | 结构断言**限定适用范围**：只约束 **solve 类**解答，`explain`/`lookup` 不受"步骤数 ≥ 3"约束 | 评审 C9（否则脚本会把讲解类回答当解答判） |
| 2026-09-16 | 口径澄清 | **N13** | "评分脚本"拆为**运行器（入主仓，不含答案）**与**期望判据表（不入主仓）**；"纳入 CI"改为"**纳入门禁**（本地 `scripts/verify.sh`；接远端后该脚本即 CI 入口）" | 评审 B1（远端推迟 ⇒ "纳入 CI"在 v2 无载体）· `docs/02 §9`（运行器必须可 review） |
| 2026-09-16 | 口径 | **§6** | 判定准确率的测量条件由"缓存已热"改为"**进程已启动且首次请求已完成**"；**新增"讲解首字 < 2s"一行**（N7）；复习有效性行的需求 ID 补 **F8** | 评审 B7（确定性判定没有"缓存"这个对象）· C8 · B4 |
| 2026-09-16 | **口径新增** | **N1 · N13** | **注入集口径定死**：注入物 = **系统错解**（不是学生错解）· 注入点 = **验证链的输入**（函数级）· 断言 = 被标注存疑或级别被降 · **不走 `/practice/grade`**（否则门禁平凡通过）。分两层：函数级 20 道（盲测）+ 端到端 3–5 道（样例集） | 评审 A4（原口径下"错解"指代不明，若走纯判定链路则任何错答案本来就会被判错 —— 门禁会**平凡通过**） |
| 2026-09-16 | 新增 | §1.1 · §8 · 附录 C | 覆盖矩阵 · 承诺 10 的可机检上限 · 术语表（**回归基线** / **样例集与冻结正式集** / **①–⑦ 环节编号**） | 评审 R12 · R10 · R4 · R7 · R6 |
| **2026-09-16** | ★ **废弃** | **N3** | **「数据不出校」作废** —— v2 改为**用户自持凭据**（N14），学生提问会发给第三方模型服务。残余含义"教材语料不公开分发"并入 **F20**。**MUST 计数 22 → 23**（−N3 +N14 +N15） | **负责人需求变更（2026-09-16）**：产品口径由"离线 / 局域网可跑"改为"允许用户自接 key 的 agent 助手 + 部分学习资源联网可达"。替代方案与否决理由见 `ADR-0004` |
| 2026-09-16 | 新增 | **N14** | **用户自持凭据**，MUST —— 凭据只存浏览器、随请求携带、**不得落日志 / 审计 / 错误信息 / L2**；**无凭据则答疑不可用** | 同上 |
| 2026-09-16 | 新增 | **N15** | **外传告知**，MUST —— 必须显著告知"提问会发送到第三方模型服务"，设置页可查；是 **N2** 的组成部分 | 同上（化名、删除权、审计保留三项的正当性都建立在"用户知道数据去了哪"之上） |
| 2026-09-16 | **口径收窄** | **N4** | 删去"**离线可跑**"，保留"无需安装即用（免构建、零 npm、零安装）" | 同上（完整链路依赖网络与凭据） |
| 2026-09-16 | 口径变更 | **F10** | 出处明确用 **`cite_id`**；资源**只取开放许可**并**抓取后缓存**；缓存缺失时如实说明"出处暂不可用" | 同上（避免"引用不可复核"与评测不稳定） |
| 2026-09-16 | 验收收紧 | **F16** | 补**边界句**：删除权覆盖本机全部数据，但**第三方已收到的请求删不掉**（须与 N15 一并告知） | 同上（否则删除权是虚的） |
| 2026-09-16 | 口径变更 | **§6 可用性** | 拆为**两条**：完整演示（联网 + 有效凭据）· 降级演示（无凭据跑完判定 / 错题本 / 进度 / 知识图谱）。**删去"断网演示"** | 同上（原验收动作测的是错的东西） |
| 2026-09-16 | **记账变更** | §1（非需求 ID） | 明账由**两处扩为四处**：形态 · 门槛 · **过程（环节③ 只能部分兑现）** · **现场风险（完整演示依赖网络与凭据）** | 评审 P1-6 + 负责人需求变更 |
| 2026-09-16 | **口径新增** | **N1 · F5** | **注入集拆为两份具名注入物**：**解答级注入集 → N1**（验证链函数级入口）· **答案级注入集 → F5**（错因匹配函数级入口，期望值写**错因 ID 集合**，断言"⊇ 期望且不含未期望"，**必带负样本**）。端到端 3–5 条只作回归保护 | 评审 P0-2（原"注入集"一个名字装了两种东西 ⇒ F5 的指标在声明的注入点上**测不出来**：`docs/02 §9.1` 禁止走判分链，而错因匹配只存在于那条链里） |
| 2026-09-16 | **口径变更** | **F5** | 补"错因标签**必须落库**（`mistake_id` + `mistake_matched`）并可由错题本跨会话读回" | 评审 P0-1（展示层有、数据层没有 —— 对 F19 用的那把尺子没对自己用） |
| 2026-09-16 | 口径 | **F4 · §6** | 补"纯计算题 / 数值题 / 应用题"定义（由题库 `answer_kind` 承载）；"冻结题集"统一为**冻结验证集**；判定延迟补**测量样本数**（20 次取 P95）与**适用范围**（只约束判定链） | 评审 P0-3 · P0-4（未定义术语出现在三条验收里；"重算"语义二义） |
| 2026-09-16 | 口径 | **F6 · F14** | `hint_used` 明确定义**两个值**：`hint_used`（本次会话，F6/F14 权威值）· `hint_used_total`（跨会话累计，错题本展示） | 评审 P1-1（原字段未写作用域，而"重做重新计"与"历史不清零"各自预设了一个不同的作用域） |
| 2026-09-16 | 验收收紧 | **N8** | 三项**分别标注**：点击目标 / 键盘可达 = 可自动；**对比度 = 声明的色对可自动，渲染结果仅 review** | 评审 P1-8（原"可机检"与"无 npm"互斥：算对比度必须渲染，而无头浏览器与零安装前提冲突） |
| 2026-09-16 | 验收收紧 | **N11** | 由"手工走查"改为"**路由冒烟（每 feature ≥ 1 断言）+ 抽查**"，并要求附**可寻址完备清单** | 评审 P2-8（MUST 的验收方式曾是全表最弱的一格） |
| 2026-09-16 | 口径 | §1.1 | 覆盖矩阵由 **33 行 → 35 行**，汇总计数改为**由脚本从表体导出**（本行曾因手工统计出错） | 评审 P0-6 · P1-7 |
| 2026-09-16 | **新增** | **N18 · N19 · N20** | **运维面立项**：**传输加密** · **数据备份与恢复** · **容量与并发**（约 50 名学生 / 峰值 10 路并发答疑 / 单 worker）。**MUST 计数 25 → 28** | 本轮评审 **P1-11**：N17 把学生数据放在我们运营的服务器上，却**没有任何需求覆盖"守、不丢、能扛"** —— 缺 HTTPS（凭据与令牌明文过网）、缺备份（`.gitignore` 把 `backend/data/` 整目录排除，学生数据的唯一副本在那块磁盘上）、缺容量目标 |
| 2026-09-16 | **口径改写** | **N6 · F16** | N6"只落本地库" → "只落**受控托管**下的服务器"；F16"本机全部数据" → "**服务器上属于该用户的全部数据**" | 本轮评审 **P0-2**：N17 在 2026-09-16 **声明**改写了三条口径，而正文**一条都没改**（含 `docs/02 §3.1` 审计裁定的前提句）—— 声明式改写必须落进正文，否则实现按正文写就得到旧口径 |
| 2026-09-16 | **口径修正** | **N15** | 条件化的判据由"**部署形态**"改为"**去向（`egress`）**" | 本轮评审 **P0-4**：`docs/02 §1.5` 的**单机形态包含 P2**（单机 + 远程端点）⇒ 按形态判会让"单机 + 远程端点"的学生看到"数据不发往第三方"，而提问正文**正在**发往第三方 —— 这是 N15/N2 的正面违反 |
| 2026-09-16 | **口径修正** | §5（非需求 ID） | 删去"**班级统计**"，改为"学生明细逐条查看" | 本轮评审 **P0-3**：`docs/02 §6.1` 已判定"班级统计"与 §9 的"不做跨用户聚合"**意思相反**并改了措辞，而 `docs/01 §5` 仍留着这个词 —— 同一个词在两侧意思相反，边界就没有执行载体 |
| 2026-09-16 | **验收收紧** | **F5 · F20** | F5 格子写上命中率地板与"**类 = 错因库一级分类（首版 ≥ 4 类）**"；F20 补 **公式库 ≥ 30 条**（并适用摊布下限） | 本轮评审 **P1-10 · P2-9**：答案级注入集的"每类 ≥ 4/5"此前**没有类的定义**，指标不可执行；公式库无下界使 F18 可被一个空库平凡满足，而 `docs/02 §11.2` 却声明"规模问题已定" |
| **2026-09-16** | ★ **验收收紧（第三轮）** | **N1** | 补**构成要求**：解答级注入集 20 道中**至少 8 道**须落在 A / B 级可达类别上，且这些道次的拦截方式**必须是"重算不一致"**，不得全部靠"手段缺失 ⇒ 降级到 D" | 第三轮自洽性核对：`docs/02 §4.4③` 的"手段缺失 ⇒ 只能到 C / D"与 `docs/02 §4.2-4` 的"降级"叠加后，**"被拦住或被标注 ≥ 18/20"可被"一律降级"平凡满足** —— 而 N1 是唯一的技术差异化 |
| **2026-09-16** | ★ **口径补全（第三轮）** | **N20** | 补"判定 / 列表接口"的完整口径：并发 50 路 · 每路 20 次取样 · 固定窗口 · 仅 `gradable=true` 与已登录账号；★ **写死"第 11 路起即超限并 503"** | 第三轮自洽性核对：原文引"**与 F4 同口径**"，而 F4 / §6 的口径**只覆盖判定链、不覆盖列表接口**；且 `docs/02 §8-13g` 的压测断言缺一个明确的超限边界 |
| **2026-09-16** | ★ **口径补全（第三轮）** | **F4 · §6** | F4 补术语对齐（"纯计算题" = §6"数值题" = `answer_kind=value` **且** `gradable=true`）；§6"判定准确率"补"**统计范围 = `gradable=true`**" | 第三轮自洽性核对：同一分类在 F4 / §6 / F20 / `docs/02 §3.3` **四处用了三种名词**，"判定准确率 100%"读不出约束的是哪一类题 |
| **2026-09-16** | ★ **验收收紧（第三轮）** | **F9 · F11 · F18** | F9 补"上溯结果 = 沿 `depends_on` 反向可达的前置集合（不重复计数）+ 空集显式降级"；F11 把 v2 取值收窄为"总数 + 每知识点正确率"并**显式声明不承诺薄弱阈值**；F18 补"**公式名称唯一性由资产校验保证，重名 ⇒ 启动报错退出**" | 第三轮自洽性核对：三条 SHOULD 的验收栏此前**没有可断言的对象**（"错误可上溯"没有输出定义、"薄弱"的阈值 `docs/02 §11.2-3` 明写未定义、"唯一条目"没有唯一性来源），而 `docs/02 §11.2` 的缺口清单里也没有它们（已补 3b / 3c） |
| **2026-09-16** | ★ **口径修正（第三轮）** | **§6 · §1.1** | 闭环 MUST 行补 **F2**（解答链的输入，原先漏在表外 ⇒ 本条"全通"测不到解答是否产生）；表后补两条限定（F1 不在七环内；F1 / F10 的"部分"不计入达标线）；§1.1 档次说明把"未排期"并进"后置"档（**同义词，不是新档**），汇总行改为 **未排期 2（F12 · F13）· 后置 2（N5 · N10）· 部分 2（F1 · F10）**（已用脚本逐行核对） | 第三轮自洽性核对：F12 / F13 写成"未排期"而档次说明**没有这一档**、汇总行漏计它们（R-P1 的脚本会先撞这里）；★ **同一轮的第一版修法（写"未排期 3 · 后置 1"）是错的** —— 它把 N10（表体写"后置"）算进了"未排期"，属"凭印象改数字"，与本节反复记的失败模式同型，故如实记账 |
| **2026-09-16** | ★ **口径修正（第三轮）** | **N19 · F16 相关** | N19 的"备份内容 = SQLite **三库**"改为"**L2 的 `*.sqlite`**（编号见 `docs/03 §2`）"，并补**取法**：WAL 之下不得文件级 `cp`，须用 `sqlite3.Connection.backup()` / `VACUUM INTO` 取一致快照；验收④补"**备份新鲜度可查**（最近一次成功备份时间与结果）" | 第三轮自洽性核对：全仓只定义了一组 `*.sqlite`，"三库"无出处；而开了 WAL 之后**文件级备份会让 N19 平凡通过**（文件在、内容缺），与"写库漏提交"同类 |
| 2026-09-16 | **计数对齐** | §1.1 · 附录 A · B.1（非需求 ID） | 覆盖矩阵 **35 → 40 行**；汇总 **S1=20 · S2=26 · S3=15 · S4=14**；B.1 **25 → 28**；计数变化链补齐；附录 A 补 6 项 A3 未落地登记 | 本轮评审 **P1-13**：§1.1 曾同时写着"37 条"与"35 行"；B.1 标题写 25 而同节写"22 → 23" |
| 2026-09-16 | **记账变更** | §9（非需求 ID） | 补运维面的明确边界：不做多 worker · 不做异地 / 多副本备份 · 不做限流排队与高可用 · 不做证书自动化 | 本轮评审 P1-11（把"承诺什么"与"不承诺什么"分开写，避免运维面立项后边界含糊） |
| 2026-09-16 | **口径修正** | **N1** | 验收句里的"已知错解"改为"已知**系统错解**" | 本轮评审 P3：与 §6 表后的两份**具名注入物**统一（解答级注入集的注入物是**系统**错解，不是学生错解） |
| 2026-09-16 | **措辞修正** | §3（非需求 ID） | "七个环节与需求 ID **一一对应**" → "逐环都有需求落点" | 本轮评审 P3：⑦ 映射到 **F9 与 F11 两条**（8 个 ID / 7 个环节），"一一对应"不成立 |
| 2026-09-16 | **验收收紧** | **N16** | 验收② 补**前置**"该账号至少完成过一次答疑"（干净账号该查询为空，断言既非真也非假）；P3 下展示"当前无凭据、未发生外传" | 第四轮裁定（`docs/02 §11.1-7` · 决策 5A） |
| 2026-09-16 | **验收收紧** | **F9** | **不设门槛：全量 + 计数**，不按错题数或层数截断 | 第四轮裁定（`docs/02 §11.1-8` · 决策 6A）：门槛属学习者建模（§9 不做） |
| 2026-09-16 | **口径澄清** | **F11** | 阈值未定义期间的占位文案统一写死为"**数据不足，暂不显示薄弱点**" | 第四轮裁定（`docs/02 §11.1-9` · 决策 7A）：原写"数据不足 / 阈值未定义"两个可选，会让每个实现者各写一句 |
| 2026-09-16 | **口径新增** | §8 的 **R-C**（非需求 ID） | 五类内容资产**须至少两人分担**，产出进 `docs/06` 阶段门 B | 第四轮裁定（`docs/02 §11.1-11` · 决策 9A）：F5 / F17 / F18 分别吃错因库 / 题型库 / 公式库，压在一个人身上会同时挂 |
| 2026-09-17 | **口径修正** | 附录 A（非需求 ID） | "**24 个端点**的层归属" → **25 个** | `docs/04 §9` 缺口 1 裁定"`qa_log` **学生自己可见**"后补入 `GET /api/me/qa` ⇒ 路由清单由 24 增至 25；`docs/03 §6.1` · `docs/04 §3` 与本节同步 |
| 2026-09-17 | **口径修正** | 附录 **B.1**（非需求 ID） | "误读成 **23 项**" → "**28 项**" | 同节标题与表体都是 28 —— 上一轮把变化链补成了 `22 → 23 → 25 → 28`，**却漏了这一句**；★ 它正是 **R-P1** 自陈的那个样例位置，而 B.1 是 `docs/06` 阶段门的**唯一机械对照源** |

**关于 F9 没有被拆成 F9a/F9b**：评审建议如此，但子编号会引入一种**新的编号方式**，与本文"不重编号、已有 ID 的含义不得改变"冲突。
F9 的**展示级**定位本来就没错，错的是**数据模型层没有独立 ID** —— 因此用新增的 F19 承载 MUST 层，F9 原义保留。

> **评审条目编号（G1–G12）与本表的关系**：G 编号是**评审侧**编号，与需求 ID 是两套体系。
> 本文只在**本表**引用它们，**不写入正文** —— 否则同一件事就会有"需求 ID"与"评审编号"两处索引，又是一处分级漂移源。

---

## 附录 C · 术语表

> 本文多处出现的术语，在这里**一次定清**。本表只定义**含义**；凡涉及字段、枚举、取值的**字段级定义**，一律以 `contracts/` 为唯一真相，冲突以契约为准（见 [`07-文档地图`](07-文档地图.md)）。

| 术语 | 含义 | 权威在哪 |
|---|---|---|
| **主站** | 我们自己的站点。v2 的**唯一**交付形态，所有功能都在这里 | 本文 §7 · `docs/03` |
| **探针**（注入式探针） | 注入到第三方页面（如课程平台）里、只读屏幕上已有内容并提问的形态。**v2 后置**，但接口不得写死（N12） | 本文 §7 · §9 · ADR-0003 |
| **注入式读屏** | 探针读取宿主页面正文 / 标题层级 / 公式 / 表格 / 选区的能力。**v2 不做** | 本文 §9 |
| **形态后置** | 需求成立、但交付形态推迟。**判定规则**：后置的需求在 `docs/06` 里不得计为已完成，只能是"未开始（形态后置）" | 本文 §7 |
| **验证级别 A–D** | 对一条解答"验证到什么程度"的分级。**A** = 纯数值，可完全验证（需数值重算手段）；**B** = 符号可强验证（需符号等价手段）；**C** = 应用题，可交叉验证；**D** = 证明题 / 开放题，**不做承诺** | 含义见本表；**字段级定义以 `contracts/verification.schema.json` 为准** |
| **手段封顶** | **级别不得超过 `checks` 里实际出现的手段所允许的上限** —— 没有数值重算就不可能到 A 级，没有符号等价就不可能到 B 级。**这是 N2「一致率 100%」可被机器断言的机制**：对每条验证报告断言 `level ≤ 手段允许的上限`。置信度口径：A 级通过可达 1.0；C 级即使全通过也不应高于 0.85；D 级 ≤ 0.4 | 本表定义规则；取值以 `contracts/verification.schema.json` 为准 |
| **地板阈值** | 验收下界（如拦截率 ≥ 18/20）。**只允许上调，下调必须走 ADR** —— 防止标准被最后一个改代码的人悄悄降低 | 本文 §6 · §8 承诺 1 |
| **注入集** | **已拆为两份具名注入物（2026-09-16）**：**解答级注入集**（喂验证链，服务 **N1**）· **答案级注入集**（喂错因匹配，服务 **F5**）。原写法是一个名字装两种东西，导致 F5 的指标在声明的注入点上**测不出来**。各 **20 道** | 本文 §4 N1 · §6 |
| **内容资产** | 题库 · 错因库 · 知识点 DAG · 公式库 · **题型与解法套路**（F17 的"适用模型"吃最后一份）。自制、入库、可版本化；**教材原文与抓取的开放许可资源属 L1，不入仓** | 本文 §4 F20（边界见 F10） |
| **评测资产** | 题库 + **两份注入集** + 运行器 + **期望判据表**。**与内容资产有交集但不等同** —— 前者回答"教什么"，后者回答"怎么证明做对了" | 本文 §4 N13 |
| **判分引擎** | 做确定性判定的那部分代码。**不依赖大模型往返**（F4），走 REST 出口 | 本文 §4 F4 · `docs/02` |
| **回归基线** | **防退化**口径：以"首次全量测量值"为基线并冻结，只许上调。与**地板阈值**的区别 —— 地板阈值是**能力下界**（我们认定低于它产品就不成立），回归基线是**历史参照**（我们只是不允许变差） | 本文 §6 |
| **①–⑦（环节编号）** | §3 里"一次挫败"的七个环节编号。**不是需求 ID**，不得当作需求引用；环节到需求的映射见 §3 | 本文 §3 |
| **样例集 / 冻结正式集（盲测）** | 评测资产的两半：**样例集**小且公开、入主仓进门禁（负责防退化）；**冻结正式集** = 两份注入集 + 冻结验证集 + 期望判据表，由**非实现者**保管且**不入主仓**（负责能力验收）。"**盲测**" = 出题记录不进实现人视野 | 本文 §6 · §4 N13 |
| **用户自持凭据** | 用户用**自己的**模型凭据接入；系统不内置、不共享、不外传。**只存浏览器本地、随请求携带**；服务端**不代为持有**，且凭据**不得落日志 / 审计 / 错误信息 / L2**。**无凭据则答疑不可用** | 本文 §4 N14 · `ADR-0004` |
| **外传告知** | 必须显著告知"提问会被发送到第三方模型服务"，设置页随时可查。它是 **N2 诚实**的组成部分，不是附加项 | 本文 §4 N15 |
| **数值题 / 应用题** | 按**标准答案的类型**分：数值或闭式表达式 ⇒ **数值题**；含单位 / 文字结论 ⇒ **应用题**。**由题库条目的 `answer_kind` 字段承载** ⇒ 分类是**资产属性**而不是人的判断，可被脚本读 | 本文 §4 F4 · F20（字段级定义在 `contracts/`） |
| **cite_id** | 教材出处的**机器可读标识**（`<教材>-<章>-<节>`）。资源**只取开放许可**并**抓取后缓存到本地**，因此 `cite_id` 长期可复核；缓存缺失时如实说明"出处暂不可用" | 本文 §4 F10 · `docs/02 §1.3` |
| **部署形态** | **两档**：**单机**（答辩 / 教师自用，跑在教师本人的机器上）· **远程托管**（正式服务，跑在我们运营的服务器上）。★ **形态决定"数据存在谁的机器上"，不决定"是否外传"** —— 后者由运行配置档决定 | 本文 §4 N16 · `docs/02 §1.5①` |
| **P1 / P2 / P3（运行配置档）** | **P1** = 本机端点（loopback，用户自备，**仅单机形态**，断网可完整演示）· **P2** = 远程端点 + 用户凭据（**会外传**）· **P3** = 无凭据（**答疑不可用**，其余照常）。★ **远程托管形态下 P1 一律拒绝** —— 学生填的 loopback 是**服务器**的 loopback，既无正当性也是 SSRF 面 | 本文 §4 N16 · `docs/02 §1.5①` |
| **egress（去向）** | `student_qa_log` 的字段，枚举 **`loopback`**（本机端点，不外传）/ **`remote`**（远程端点，**会外传**）。★ 它同时是 **N16 验收②** 的读对象与 **N15 告知文案**的判据（**判据是去向，不是形态**） | 本文 §4 N15 · N16；字段级定义以 `contracts/` 为准 |
| **出网面（egress manifest）** | 全仓**运行期恰好两个出网口**：**(A)** `backend/app/tools/llm.py`（服务运行期的模型调用）· **(B)** `scripts/fetch_corpus.py`（**离线**预抓，不在服务运行期）。由 **R-K** 守；`GET /api/health` 与备份**都不在清单内** | 本文 §4 N16 · `docs/02 §1.5③` |
| **受控托管** | 学生数据**只存于我们运营的服务器**，不流向第三方（**除模型调用载荷**）。★ 由此 **"不外传" ≠ "数据不出学生设备"** —— **单机形态下两者重合，远程托管下不重合** | 本文 §4 **N17** · `ADR-0005` |
| **传输加密** | 远程托管形态下令牌与模型凭据**只经 HTTPS** 传输；明文 HTTP 被拒；前端无任何第三方脚本 / CDN | 本文 §4 **N18** |
