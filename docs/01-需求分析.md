# 01 · 需求分析

> | | |
> |---|---|
> | 性质 | **需求层的唯一权威**。本仓所有"应该有什么"的结论以本文为准 |
> | 回答什么 | **为谁**做 · 解决**什么**问题 · **什么算做成了** |
> | **不**回答什么 | 怎么做 → [`02-系统设计`](02-系统设计.md) · 怎么分层 → [`03-架构总览`](03-架构总览.md) · 字段长什么样 → [`contracts/`](../contracts/) · 谁做 → [`05-分工与协作规范`](05-分工与协作规范.md) · 何时做 → [`06-开发计划与阶段门`](06-开发计划与阶段门.md) |
> | 方法 | §1–§6 **脱离实现**推导（假设今天才拿到题目）；§7 映射交付形态；§8 是**建仓承诺**，把上一个仓库踩过的坑变成这里的硬性做法 |
> | 需求可追溯性 | 每条需求都带**可断言的验收方式**。写不出验收方式的不进本文 —— 那是愿望，不是需求 |
> | 快照 | ProbCrew v2 · 建仓文档（阶段 A） |

---

## 1. 用户不是一个人，是四个

学生项目最常见的失败是**把"学生"当成一个人**。实际上有四类人的成功标准**互相冲突**，一个界面同时讨好必然撕裂。

| 角色 | 真实处境 | 他要的 | 他**不**关心 |
|---|---|---|---|
| **S1 正在写作业的学生** | 有 deadline，现在就要这道题怎么算 | 快、准、步骤能抄 | 不许要注册、不许读完说明书 |
| **S2 备考的学生** | 要的是"我哪里不会"，以及反复练 | 薄弱点定位、能重做、有进度 | 单题的漂亮讲解 |
| **S3 任课教师** | 关心学生是否**真在学**、会不会被代做 | 能否被我看见、能否证明有效 | 架构多先进 |
| **S4 项目组 / 评委** | 关心这事有没有技术含量、能不能讲清 | 可验证、可复现的数字 | 界面好不好看 |

**关键推论**：**S1 与 S2 的诉求方向相反** —— S1 要"最快拿到答案"，S2 要"让我自己想出来"。

> ⚠️ **本仓必须承认的一处代价**：上一个仓库用**两种形态**化解这个冲突（注入式探针服务 S1，主站服务 S2）。本仓决定**先只做主站**，探针后置。因此 **S1 在 v2 里没有被完全服务** —— 他能得到快速问答与快捷入口，但"就着屏幕上那段话提问"这件事做不了。
> 这不是"以后再说"的推脱，而是一笔**明账**：需求 F1 成立，交付形态后置（见 §7），并由 **N12** 保证主站的设计不会把探针的路堵死。

---

## 2. 概率论这门课特殊在哪

换一门课（比如英语单词），需求会完全不同。四条性质直接决定产品形态：

| 性质 | 后果 |
|---|---|
| **答案几乎总是一个可验证的数值或表达式** | ★ **最大的机会**：正确性可以被机器证明。英语作文不行，历史论述不行，概率论行 |
| **但"过程"比"答案"重要** | 学生错的往往不是算错，而是**建模错**（把 P(A\|B) 与 P(B\|A) 搞反、漏掉完备事件组）。只给答案没用 |
| **错误高度模式化** | 贝叶斯方向颠倒、独立与互斥混淆、忽略 n 次试验的独立性……**错误种类有限且可枚举**（约 50–100 条） |
| **知识点依赖关系强** | 不会条件概率就学不会贝叶斯；不会期望就学不会方差。**知识点是有向图，不是列表** |

**推论 1**：因为答案可验证，"**告诉你这个答案经过了几层验证**"能成为真卖点 —— 文科类工具做不到。
**推论 2**：因为错误模式化且有限，"**告诉你踩了哪个坑**"比"再讲一遍知识点"有效得多，而且要**从教材与教师经验整理即可，不需要任何学生数据**。
**推论 3**：因为依赖关系强，错题不能只记"这道题错了"，必须记"**这道题考了哪几个知识点**"，才能沿依赖图往上找根源。**这一条直接决定数据模型**。

---

## 3. 从"学生的一次挫败"倒推功能

不要列功能清单。跟着一次真实的挫败走一遍，功能会自己长出来。

```
场景：学生做到一道贝叶斯题，卡住了。

① 他不知道这题在问什么            → 需求：题目能看懂 / 能拆解
② 他隐约记得有个公式但想不起来     → 需求：精准的公式/定理查找（不是泛泛讲解）
③ 他写了一个式子，不确定对不对     → 需求：逐步判定，指出第一处错在哪
④ 他算完了，但答案是 0.99         → 需求：即时批改 + 诊断"你把方向搞反了"
⑤ 他改对了，但不知道为什么对       → 需求：解析 + 这一步的依据（教材出处）
⑥ 一周后他又错了同一类题           → 需求：错题按知识点归类 + 能重做
⑦ 他连着错了三道贝叶斯题           → 需求：沿知识依赖往上找，是不是条件概率就没懂
                                            → 需求：给出复习路径，而不是"再练 10 道"
```

**七个环节，缺一环闭环就断。** 其中 ⑥⑦ 与 ①–⑤ 是**两种不同类型的需求**：

- ①–⑤ 是**单次会话内**的需求 → 无状态、可随时丢弃
- ⑥⑦ 是**跨会话**的需求 → **必须有持久化的作答记录**

> ★ **这是全篇最重要的推论**：任何声称"伴学"的工具，如果没有持久化作答记录，它其实只是个"答疑工具"。
> **"学习"这个词承诺的是时间维度上的变化，而没有记录就没有变化可谈。**

---

## 4. 需求清单

级别：**MUST**（缺了产品不成立）· **SHOULD**（缺了明显变差）· **MAY**（锦上添花）。
**"验收方式"一栏必须是可断言的动作**，不是形容词。

### 4.1 功能性需求

| ID | 需求 | 级别 | 验收方式（可断言） |
|---|---|---|---|
| **F1** | 学生能**带着上下文**提问（当前在看的题目 / 知识点 / 教材片段） | MUST | 提问请求必须携带至少一个上下文标识；无上下文时接口**显式降级**而非静默 |
| **F2** | 系统能解题并给出**可读的逐步过程** | MUST | 给定题目，输出步骤数 ≥ 3，且每步可被复述 |
| **F3** | 每条解答带**正确性声明**（这次验证到什么程度） | MUST | 每条解答都携带级别标签；**无标签不出结果**；前端渲染该标签（后端发了前端不看等于没发） |
| **F4** | 提交作答后**即时判定**，且**不依赖大模型往返** | MUST | 纯计算题端到端 < 1s；判定链路中无 LLM 调用（以调用计数断言） |
| **F5** | 判定错误时给出**针对性错因**，而非"答错了" | MUST | 命中预定错误模式时输出对应解释；未命中时**如实说"未识别错因"** |
| **F6** | **分级提示**（先方向 → 再步骤 → 最后解析），且**每次提示计入记录** | MUST | 未提交前**不得**返回完整解析；每次提示使 `hint_used` 精确 +1，并能从错题本如实显示"这道题用了 2 次提示" |
| **F7** | 每次作答**留下结构化记录** | MUST | 记录含：题目、作答、对错、知识点、用时、提示次数、判定者、当时的标准答案 |
| **F8** | 错题可按**知识点**归类，并支持重做 | SHOULD | 能列出"贝叶斯类错了 3 道"，并可发起重做 |
| **F9** | 知识点**依赖关系**可见，错误可沿依赖上溯 | SHOULD | 存在知识点 DAG；错误可上溯到前置知识点 |
| **F10** | 解答能**引用教材出处**（章节/片段），可核对 | SHOULD | 检索命中带出处；**无命中时如实说明"未找到出处"** |
| **F11** | 学生能看到**自己学到哪了**（进度 / 薄弱） | SHOULD | 存在进度视图，**数据源必须是作答记录**，不得用"问答条数"冒充学习进度 |
| **F12** | 可视化辅助（分布曲线、交互动画） | MAY | 动画可播放、分布图可交互 |
| **F13** | 多智能体协作过程**可见** | MAY | 协作轨迹可回放 |
| **F14** | **提示使用透明化**：能回答"这次学习用了多少次提示" | SHOULD | 存在自陈视图，数据源为 `hint_used` 聚合；**该视图不采集任何额外个人信息**（服务 S3，零合规成本） |
| **F15** | **账号与角色**：学生 / 管理员两种身份；学生只见自己的数据 | MUST | 未登录不得访问学习类接口；学生 A 无法读到学生 B 的任何数据（越权测试）；管理员接口不可创建管理员 |
| **F16** | 学生可**删除自己的数据** | MUST | 调用删除接口后，该用户在学习记录表中的行数为 **0**（读回断言，不是"没报错"），账号被脱敏 |

### 4.2 非功能性需求

| ID | 需求 | 级别 | 为什么对本项目特别重要 |
|---|---|---|---|
| **N1** | **正确性可验证**：错误答案必须被拦住或标注 | MUST | 唯一的技术差异化。学生无法判断对错，**误信的代价由学生承担**。验收必须是**结果指标**：**注入 20 道已知错解（方向颠倒 / 漏完备组 / 单位错 / n 乘错各 5 道）→ 断言被标注存疑或降级者 ≥ 阈值**。"走了重算流程"是过程指标，**可以被满足而毫无效果** |
| **N2** | **诚实**：做不到的事必须明说 | MUST | 标注级别与实际验证手段的**一致率必须 100%**；不得笼统说"已校验"。学习工具的信任是一次性的 |
| **N3** | **数据不出校**（可本地 / 局域网运行） | MUST | 教材版权 + 学生数据合规；也是"能否给老师看"的前提 |
| **N4** | **无需安装即用** | MUST | 决定实际使用率；S1 的耐心 ≈ 0。免构建、零 npm、离线可跑 |
| **N5** | **不篡改第三方平台**（只读，不点击、不注入视觉元素） | MUST（条件） | 仅当将来做注入式探针时生效。**即使探针后置，这条也不得从需求里消失** —— 否则将来会有人"顺手"实现成能操作页面 |
| **N6** | **化名账号、无 PII**：不采集学号/真实姓名；密码只存哈希 | MUST | 既满足 ⑥⑦ 的持久化，又不触发个人信息保护的合规门槛。数据只落本地库 |
| **N7** | **响应及时**：单题判定 < 1s，讲解首字 < 2s | SHOULD | 超时就退化成"去问搜索引擎"了 |
| **N8** | **可访问、可读** | SHOULD | 对比度 ≥ 4.5:1；点击目标 ≥ 32px；键盘可达；语义标签。学习工具的可及性，也是评奖加分项 |
| **N9** | **可复现的效果数据**：能说"这一版比上一版好多少" | SHOULD | 对 S4。**没有基线，所有改进都无法证明** |
| **N10** | 多端可用（至少手机能看错题本） | MAY | 复习发生在碎片时间 |
| **N11** | **可寻址与可回退**：URL 能表达界面状态，浏览器返回键有效 | MUST | 这不只是可访问性问题，**是功能缺陷**：刷到第 3 步提示不能刷新、不能分享、不能回退。缺它，四个页面会各自发明一套状态管理 |
| **N12** | **形态可扩展**：上下文来源、身份、事件、提示四者**解耦**，契约中不得写死"仅主站可用"的假设 | MUST | 这是"探针后置"唯一的技术代价控制手段。**后置不等于放弃** —— 若把主站专属假设写进契约，将来做探针就是重写而不是新增 |

---

## 5. 一条需求层面的伦理约束（不是功能）

**F1–F6 做得越好，工具越接近"作业代写器"。** 这是需求内在的矛盾，必须在产品层面缓解：

| 缓解手段 | 机制 |
|---|---|
| **提示阶梯（F6）** | 默认先给方向，完整解析需学生先尝试 —— 把"抄答案"变成"要点两下"的摩擦 |
| **不做"直接出最终答案"的入口** | 把"结论"放在"过程"之后 |
| **教师可见（F14）** | 让 S3 知道工具在干什么，而不是被绕过 |
| **如实记入掌握度** | "用了 3 次提示才答对"照实记。数据上不美化 —— **这正是 `hint_used` 字段存在的理由** |

> ★ 推论：**"提示阶梯"不是体验优化，是伦理约束落地的地方。** 它必须在数据模型里有对应字段（用了几次提示），否则约束**无法执行、也无法度量**。
> 这条把 F6 从"交互设计"提升为"必须产生数据的需求"。

---

## 6. 什么算"做成了"——成功标准

| 维度 | 达标线 | 怎么测 |
|---|---|---|
| 解题正确率 | 在自建题库上 ≥ 基线水平（**必须先有基线**） | 固定题集 + 固定评分脚本 |
| **错误拦截率** | 注入的已知错解**必须被发现或降级标注** | 注入 20 道已知错解，统计拦截率（见 N1） |
| **诚实性** | 标注级别与实际验证手段**一致率 100%** | 抽查：标 A 的是否真做了重算 |
| 判定准确率 | 数值题 100%（确定性代码）；应用题允许表达误差 | 对照标准答案 |
| 闭环完整性 | F1→F7 全通；F8 能跨会话读到 F7 的数据 | 端到端走一遍并**刷新页面**（验证持久化） |
| 可用性 | 无 Key、无网、无安装可完整演示 | 断网演示 |
| 可寻址 | 任一界面状态可复制 URL 到新标签页还原；返回键有效 | 手工走查（N11） |

---

## 7. 需求 × 交付形态

本仓 v2 **只做主站**。下表把"需求成立但形态后置"的部分明确标出来 —— 这是本仓与上一个仓库最主要的口径差异。

| 需求 | v2 的交付形态 | 后置的部分 |
|---|---|---|
| F1 带上下文提问 | **主站内上下文**：题目 / 知识点 / 教材片段 | 注入式读屏（宿主页面正文与选区）→ 后置 |
| F10 教材出处 | 本地语料检索 + 出处标注 | PDF 阅读器与教材溯源定位 → 后置 |
| F12 可视化 | 主站动画库 + 分布可视化 | — |
| F13 协作可见 | 主站轨迹页 | — |
| N5 不篡改平台 | **条件生效**（无注入则无篡改面） | 探针落地时立即生效，红线不变 |
| N12 形态可扩展 | 契约层面现在就守住 | — |

**判定规则**：一条需求若"形态后置"，它在 `docs/06` 里**不得**被算作已完成；只能是"未开始（形态后置）"。禁止用主站的一部分满足度去抵充探针的缺失。

---

## 8. 建仓承诺：上一个仓库教给我们的 12 条

> 本节是本仓**从零开始的全部理由**。以下每条都有上一个仓库的实证来源，因此**不是风格偏好，是踩过的坑**。
> 每条都给出"谁来守" —— 只有能进门禁的承诺才算承诺。

| # | 承诺 | 来源（旧仓实证） | 谁守 |
|---|---|---|---|
| 1 | **拦截率是结果指标**；不接受"走了验证流程"作为交付证据 | 验证级别在真实链路上**恒为 D**（分级 A/B/C 实际不可达），代码"走了流程"却一步真验证都没做 | `docs/06` 阶段门 + N1 拦截率测试 |
| 2 | **作答记录不被任何"合规门槛"挂起** | `attempt` 契约字段设计完备（含知识点/提示次数/用时），却因被归入"需知情的建模线"而整体挂起 —— 而错题本要的正是它。**本地、化名、不出校的记录本就不触发那个门槛** | §4 F7 为 MUST；契约状态不得为 reserved |
| 3 | **提示必须进数据模型** | `hint_used` 字段被设计出来并标注"最容易漏"，但无任何实现把提示映射到计数 | F6 验收 + 字段-需求对照（附录） |
| 4 | **进度数据源是作答记录，不是问答记录** | 统计接口读的是问答流水，于是"问了 100 次"会显示成"学习了 100 次" | F11 验收 |
| 5 | **路由必须可寻址** | 前端路由只写 `location.hash`、无人监听 `hashchange`，实测返回键只变 URL 不变视图 | N11 + 前端冒烟测试 |
| 6 | **确定性判定走 REST，且必须有出口** | 判定引擎 521 行、16 项测试通过，但全仓 21 个路由**没有一个批改端点** —— "引擎有、通路无" | F4 + 契约 `producer/consumer` 零悬空 |
| 7 | **可访问性是一等需求** | 正文对比度实测 **2.56:1**（需 4.5）、7 个点击目标 < 32px、语义标签全仓 5 处 | N8 写入契约与页面模板 |
| 8 | **超长输入与正则回溯按建仓规则处理** | `/api/practice/grade` 曾**冻结整个事件循环**；判定正则对 100k 位数字耗时约 **2 分钟** | 建仓规则（见下） |
| 9 | **写库必须显式提交，测试必须读回** | 写函数缺少一次提交，作答记录被静默回滚；测试当时只断言"没报错" | 建仓规则 + 测试规范 |
| 10 | **演示叙事不得吃掉产品代码** | 协作可见类页面（轨迹/架构/助手）约 1 400 行，是前端最大代码块；而功能页很薄 | `docs/03` 技术债清单 + `docs/06` 配额 |
| 11 | **一主题一篇文档** | 旧仓分工写了 3 篇、计划写了 3 篇、地图写了 2 篇，而**系统设计一篇都没有** —— 冗的是重复，缺的是核心 | `docs/07` 单一权威登记 |
| 12 | **契约零悬空、缺口补齐** | 最热的问答接口、错误体、分页、作答、掌握度**全无契约**；每端各写各的形状 | 契约校验（`docs/04`） |

**两条建仓规则**（承诺 8、9 的落地形式，进 `scripts/verify.sh` 自动扫描）：

- **R-A**：所有请求体中的字符串字段**必须**显式设上界（`max_length`）；判定类正则**禁止嵌套量词**。
- **R-B**：任何写库函数**必须**显式提交；对应测试**必须**读回断言，不得只断言"HTTP 200"。

---

## 9. 本仓不做（明确边界）

**v2 永久不做**：npm / 打包器 · ORM · Postgres · Docker 化生产部署 · i18n · 多端适配（N10 为 MAY）。
**v2 本阶段不做（形态后置）**：注入式探针（读屏） · 划词交互 · 驱动宿主动画 · PDF 阅读器与教材溯源定位。
**v2 不做（数据门槛仍在）**：学习者建模 / 教师端报表 / 掌握度热力图 —— 这些依赖真实学生数据与知情同意，**与"作答记录"是两件事**（见承诺 2，别再混为一谈）。

---

## 附录 A · 需求与数据的对照规则（防"由界面反推数据模型"）

数据模型**不是**"页面要显示什么就加什么字段"。
**规则**：字段-需求对照表放在 [`02-系统设计`](02-系统设计.md)，且必须满足两条：

1. **每个字段能指出它支撑哪个需求 ID** —— 说不出的**删掉**；
2. **每条 MUST 需求至少被一个字段或一个接口支撑** —— 说不出的**补上**。

> 上一个仓库的 6 处需求缺口，正是"先画界面、后凑字段"的产物。这条规则的成本接近于零，但它是唯一能挡住那种漏法的机制。

## 附录 B · 需求 ID 的变更规则

- **只增不改**：已有 ID 的含义不得改变；需求变了就新增 ID，旧 ID 标注"已被 Fxx 取代"并注明日期。
- **不重编号**：编号是外部引用锚点（`docs/06` 阶段门、测试用例名、提交信息都会引用）。
- **删除需求**要写明理由与日期，不许静默消失。
- 每条 **MUST** 需求必须在 [`06-开发计划与阶段门`](06-开发计划与阶段门.md) 里有对应的验收动作；没有的就是计划漏了，不是需求多余。
