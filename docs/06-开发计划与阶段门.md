# 06 · 开发计划与阶段门

> | | |
> |---|---|
> | 性质 | **"何时做、做到哪一步算过门"的唯一权威**。阶段划分 · **28 条 MUST 的验收动作** · 三个阶段门 · 四类运维演练 · 内容资产产出 |
> | 回答什么 | **分几个阶段 · 每个阶段的出口条件是什么 · 每条 MUST 在哪一步被验收 · 运维与资产什么时候练/产出** |
> | **不**回答什么 | 达标线本身（拦多少、延迟多少、命中率多少）→ [`01-需求分析`](01-需求分析.md) 的 `docs/01 §6`（**本篇只引用，不复制**）· 谁负责 → [`05-分工与协作规范`](05-分工与协作规范.md) · 规则内容（R-A…R-Q）→ `docs/02 §1.6` · 契约校验口径 → `docs/04 §7` · 代码放在哪 → [`03-架构总览`](03-架构总览.md) |
> | 上游输入 | `docs/01` 附录 **B.1**（28 条 MUST，**唯一机械对照源**）· `docs/01 §6`（达标线与测法）· `docs/01 §7`（交付形态判定规则）· `docs/01 §8`（12 条承诺与 R-A/R-B/R-C）· `docs/02 §1.6`（规则总表）· `docs/02 §11.1` 决策 10/11 · `docs/03 §8.2`（脚本清单）· `docs/04 §7`（契约校验口径） |
> | 下游产出 | `scripts/verify.sh` 的门禁接线 · `.github/workflows/ci.yml` 的步骤 · `docs/01 §6` 的**回归基线**（阶段门 B 结束时录入并冻结） |
> | 快照 | ProbCrew v2 · 阶段 A4 |

**旧仓把这件事写成三篇**（`05-开发计划` · `12-基础开发计划` · `17-模块化任务书`），而三篇都没能回答一个问题：**某条 MUST 由谁、在哪一步、用什么动作验收**。于是"计划"变成日期表，"验收"落到最后一周临时凑。v2 合成一篇（`ADR-0003` 决策 6）。

★ **本文的核心不是日程，是三个门**。`docs/01` 附录 B 已经写死了这条规则：

> 每条 **MUST** 需求必须在 `docs/06` 里有对应的验收动作；没有的就是计划漏了，不是需求多余。

**因此本文 §2 是那条规则的执行载体** —— **28 条 MUST 一条不落**，且**每条都指得出验收动作与它所在的门**。

---

## 1. 阶段与阶段门（★ 本文的骨架）

| 阶段 | 做什么 | 出口条件（**阶段门**） | 谁必须到场 |
|---|---|---|---|
| **阶段 A · 文档** | A1 需求 · A2 系统设计 · A3 架构与 **14 份契约** · A4 本篇与 `docs/05`/`docs/07`（**已完成**） | **阶段门 A：文档自洽** —— R-P1/P2/P3 全绿 · 契约零悬空 · 14 份 schema 的 `examples` 自校验 | **架构与集成**域 |
| **阶段 B · 实现** | 三条主链路（答疑 / 判定 / 学习留痕）· 前端外壳与 7 个页面 · 门禁脚本 `scripts/verify.sh` · ★ **五类内容资产产出** | **阶段门 B** —— 见 §4：闭环全通 · R-A…R-Q 门禁绿 · 资产达下界 · **回归基线录入并冻结** | 全部四域 |
| **阶段 C · 真实链路与盲测** | 真实链路上测定 HITL 超时取值 · ★ **冻结正式集盲测**（非实现者执行）· 四条可用性演示 · 运维演练 | **阶段门 C** —— 见 §5：盲测有签署结论 · 四条演示过 · 运维演练留档 | 全部四域 + **非实现者** |

### 1.1 为什么恰好三档、不设第四档

**依据是既有断言，不是新划的**：`docs/02 §11.1-5` 把 HITL 取值推到"**阶段 C 用真实链路测定**"；`docs/01 §6` 把解题正确率的**回归基线**定在"**阶段门 B 结束时**录入并冻结"；`docs/03 §8.2` 把 `eval_verify.py --frozen-dir` 的执行时机写作"**阶段门 C**，由非实现者执行"。**三个阶段是被这三条反推出来的**，因此不再增设第四档 —— 增设会让上面三句里的"阶段 C"变成需要重新解释的词。

> ★ **阶段 A 的三个小阶段（A1–A4）不设独立门**：它们是**同一篇文档流水线的四批**，中间批次不需要签字；`docs/02` 表头写的"阶段 A2"、`docs/03` 写的"阶段 A3"就是这个用法（**它们标的是快照，不是门**）。

### 1.2 三个门的关系

```
阶段门 A ──▶ 阶段 B（实现）──▶ 阶段门 B ──▶ 阶段 C（真链）──▶ 阶段门 C（交付）
  文档自洽                        闭环 + 资产 + 基线              盲测 + 演示 + 演练
```

★ **门只增不改**：**进了后面的门不许让前面的门变红**。`scripts/verify.sh` 是**唯一**的门禁入口（本地跑它就是本地门禁，接上远端它就是 CI 入口 —— `docs/03 §8.2`），因此"阶段门 A 的检查"在 B、C 阶段**照旧每次都跑**。

---

## 2. ★ 28 条 MUST 的验收动作（`docs/01` 附录 B.1 的机械对照源）

**本节与 B.1 的关系**：B.1 给**清单与条数**，本节给**动作与门**。★ **两边条数必须相等（28）**，任何一边增删都必须同步另一边 —— 这就是 `docs/01` 附录 B 那条规则的机械形式。

### 2.1 已在 `docs/01 §6` 有达标线的（**21 条**）

**下表不复制达标线**（`docs/01 §6` 是它的唯一权威），只回答"**在哪一步、用什么方式过门**"：

| MUST | `docs/01 §6` 的维度 | 门 |
|---|---|---|
| **N20** | 容量与并发 | **B** + 运维演练④（§6） |
| **N19** | 备份可恢复 | **B** + 运维演练②③（§6） |
| **N18** | 传输加密 | **C**（远程托管形态）+ 运维演练① |
| **N16** | 可用性四条 | **C** |
| **N15** | 可用性 · 远程完整 | **C** |
| **N14** | 可用性 · 单机完整 | **C** |
| **N13** | 错误拦截率 / 错因命中率（使能项） | **A**（样例集）+ **C**（正式集） |
| **N11** | 可寻址 | **B** |
| **N8** | 可访问性 | **B** |
| **N2** | 诚实性 | **B** |
| **N1** | 错误拦截率 | **C** |
| **F19** | 闭环完整性（MUST）· 复习有效性 | **B** |
| **F16** | 删除读回 | **B** |
| **F15** | 越权隔离 | **B** |
| **F8** | 闭环完整性（MUST） | **B** |
| **F7** | 闭环完整性（MUST）· F16 | **B** |
| **F6** | 提示阶梯生效 | **B** |
| **F5** | 错因命中率 | **C** |
| **F4** | 判定准确率 · 判定延迟 · 闭环完整性 | **B** |
| **F3** | 诚实性 | **B** |
| **F2** | 解题正确率 · 闭环完整性 | **B** |

> ★ **本节只回答"在哪一步过门"** —— **达标线与测法在 `docs/01 §6`（唯一权威）**，**自动化程度也记在它的"怎么测"列**。★ 本节**曾多写一列"执行方式"**（如 N20 行的"可自动：并发压测（11 路断言 503）"）—— 而"11 路 / 503"正是 §6 **达标线**里的数，**违反了本节 §10 自己立的判定方法**（"任何一格删掉日期与门名之后仍然是一个验收标准，它就越位了"）。2026-09-17 删除该列。
> ★ **§2.2 的 7 条与本节不同**：它们**没有 §6 行**，所以**动作必须写在那里** —— 那不是复制，是唯一记载处。
> ★ **人工门禁的排期不在这里**：N1 / F5 的正式集是"**不入主仓、由非实现者手工执行**"，它们的具体动作排在 **§5 阶段门 C 第 2 项**（"不能自动红灯"的代价已记在 `docs/01 §6`）。

### 2.2 ★ 未进 `docs/01 §6` 的 7 条 —— **必须在这里有独立动作**

`docs/01 §6` 的**方向 B** 原文：**"§6 不承担全覆盖责任**（它是对外证明，不是内部分解）；未进 §6 的 MUST 需求，必须在 `docs/06` 有**独立**阶段门动作"。逐条核对后**恰好 7 条**未进 §6：

| MUST | 它要求什么 | ★ **独立阶段门动作** | 门 | 判据 |
|---|---|---|---|---|
| **F1** | 带上下文提问（主站内上下文） | 在答疑链路用例里**显式构造**三种上下文来源（题目 / 知识点 / 教材片段）各一次，并断言**无上下文时显式降级**（`context` 缺省不等于报错） | **B** | 三条用例 + 一条降级用例；★ **注入式读屏部分不计入**（`docs/01 §7`：形态后置） |
| **F20** | 内容资产可维护 + 可版本化 | **五类资产达下界**（§7）+ `content/MANIFEST.json` 可由门禁**复算 `content_rev`**（`docs/04 §7` 校验⑥）+ 启动校验（`docs/02 §8-14`）过 | **B** | 五类下界逐项断言；复算值与文件内一致 |
| **N4** | 免安装（免构建外壳） | **零依赖门禁**：断言 `frontend/` 无 npm 产物、无外部 CDN、`index.html` 直接可开；且 `frontend/vendor/katex/` 已本地化 | **B** | `docs/04 §7` 校验⑦ + 目录断言 |
| **N5** | 不篡改平台（**条件 MUST**） | ★ **不排动作** —— 探针后置期间本需求**不生效**。`docs/01` 附录 B.1 的规定是：在本文里记为"**未开始（形态后置）**"，**不得计为已完成** | **—** | 见 §9 口径① |
| **N6** | 化名账号（不采集学号 / 姓名） | **接口与库两层断言**：`adminImportRequest` 不含学号 / 姓名（契约 `additionalProperties: false` 已钉死）+ 库中无对应列；注册只收 `username` / `display_name` | **B** | 两条断言（请求体被拒 + `PRAGMA table_info` 无学号列） |
| **N12** | 形态可扩展（四解耦） | **架构断言**：`docs/03 §5.3` 的四解耦表逐行核 —— 换上下文来源 / 身份 / 事件 / 提示**只改表里列出的文件**；外加一句 grep：契约里不出现"仅主站可用"这类假设 | **B** | 四行逐行核 + 一条 review 记录 |
| **N17** | 受控托管（数据只存我方服务器） | **出网面断言**：全仓运行期出网调用只出现在 `tools/llm.py`（**R-K** grep）+ 假 provider 抓载荷断言**不含** `user_id` / `username` / 令牌 / `attempt` / `mastery` / `qa_log` / 审计（**R-L**） | **B** | R-K 与 R-L 的门禁 + 一条 N16 验收③ 的用例 |

> ★ **这 7 条为什么会被漏掉**：`docs/01 §6` 是**对外证明**（"我们做成了什么"），它天然只覆盖**可展示的维度**；而 F1 的上下文、N4 的零依赖、N6 的采集边界、N12 的解耦、N17 的出网面是**性质**而不是**指标** —— 它们**没有"达标线"可言，只有"有没有"**。**"有没有"必须靠动作，不能靠指标** —— 这正是方向 B 存在的理由。
> ★ **N5 是唯一不排动作的一条**，因为它是**条件 MUST**：`docs/01 §7` 明写"探针落地时立即生效，红线不变"。**把它排成动作才是错的** —— 那会让"形态后置"变成一句谎。

---

## 3. 阶段门 A · 文档自洽（**当前阶段，A4 收口时执行**）

| # | 检查 | 判据 | 自动化 |
|---|---|---|---|
| 1 | ★ **R-P1 计数由表体导出** | `docs/01 §1.1` 行数与 S1–S4 计数 · 附录 B.1 的 MUST 条数 · `docs/02 §10` 的落点数，**由脚本从各自表体算出**并断言与正文数字一致 | 可自动 |
| 2 | ★ **R-P2 相对引用解析** | 三种引用形式（本篇 `§N.M` · 跨篇 `docs/0X §N.M` · `ADR-00NN §N`）全部命中；**裸 `§N.M` 一律按本篇判**；未产出目标须在 `docs/07` 登记 | 可自动 |
| 3 | ★ **R-P3 落点反向闭合** | `docs/02 §10` 每行的落点能在被指章节 grep 到关键词（N15 → `noticeStatus.message` / `must_render`）；`docs/01` 附录 A 每项在 `contracts/` 找得到定义 | 可自动 |
| 4 | **契约零悬空**（承诺 12） | `docs/02 §6.1` 的 path 集合 = `docs/03 §6.1` = `docs/04 §3`（**当前 25 条**），**三处双向 diff 为空** | 可自动 |
| 5 | **14 份 schema 自洽** | `examples` 全部自校验 · 跨文件 `$ref`（含 JSON Pointer）全部可解析 · `$schema`/`$id`/`version` 齐 | 可自动 |
| 6 | **`code` 枚举对账** | `error.schema.json` 的 `enum` = `x-http-status-by-code` = `docs/02 §6.3` 表体（**当前 16 条**），且**状态码逐项一致** | 可自动 |
| 7 | **事件词汇表** | `events.schema.json` 的 `x-type-count` = 枚举项数（**当前 14 类**） | 可自动 |
| 8 | **R-C 自检**（唯一人工项） | `docs/05 §2.4` 的五条清单 | **人工** |
| 9 | ★ **表内自计数与表体一致**（2026-09-17 补） | `docs/03 §2` 的"**四条**规则" ↔ 表体 4 条 · `docs/05 §2.4` 的"**五条**清单" ↔ 5 行 · §6 的"**四类**" ↔ 4 行 · §7 的"**五类**" ↔ 5 行 · §5 引用的门号 ↔ §4 的项号。★ **本轮补第 9 项的原因**：`docs/03 §2` 加了第 4 条规则却**没改"三条"这个数** —— 与 **R-P1** 同类，但对象是**文档自己的表**（R-P1 只管 `docs/01 §1.1` / B.1 / §10 那三处） | 可自动 |

> ★ **与 `docs/04 §7` 的七节校验的对应关系**（不写清会被读成"漏了两节"）：**§3 第 4 项 = 校验④**（path 覆盖完整）· **第 5 项 = 校验①②**（schema 自洽 + `examples` 自校验）· **第 6 项 = 校验③**（`code` 枚举一致）· **第 7 项 = 校验⑤**（事件枚举一致）。★ **校验⑥（`MANIFEST` 与 `content_rev` 复算）与校验⑦（部署无关化 + 零依赖）不在阶段门 A** —— 它们要的是阶段 B 的产物（`content/MANIFEST.json` 与前端外壳），分别落在 **§4 第 5 项** 与 **§2.2 的 N4 行**。

> ★ **第 1–3 项就是 R-P 本体**（`docs/02 §1.6` 要求"`docs/06`（A4）必须把 R-P 列为门禁项"）—— 本节即那条要求的落点。**R-P 是"文档不会自己漂回去"的唯一保证**：本轮 A5 查出并修掉的三处失同步（path `24→25` · `code` `14→16` · MUST `23→28`）**全部是 R-P1/P3 能机械发现的形态**。

---

## 4. 阶段门 B · 实现层

**入口条件（两条并列，缺一不可）**：

1. `docs/05 §2.4` 的 **R-C 五条清单**通过（**资产产出与编排层都有责任人** —— R-C 原文："空缺即该需求不得为已立项"）。
2. ★ **骨架就绪**（2026-09-17 补）：**FastAPI 入口 `backend/app/main.py`**（`docs/03 §2` · §8.4）+ **`frontend/index.html` 与 `frontend/core/` 外壳** + **`backend/tests/`** + **`requirements.txt`** + **`scripts/verify.sh` v0 跑绿一次** —— 责任域 = **架构与集成**（`docs/05 §3` 已把这几条路径判给它）。
   ★ **为什么它必须写成入口条件**：本节下面 **11 项出口条件全部假定骨架已存在**，而此前**没有任何一处排过"把它建起来"这个动作** —— **关键路径的起点没有定义**。它同时就是**工作包 W0**。

| # | 出口条件 | 判据 | 责任人（`docs/05`） |
|---|---|---|---|
| 1 | **三条主链路的 MUST 闭环全通** | `docs/01 §6` 的闭环行：**F2 · F4 · F5 · F7 · F8 · F19 全通**，且**端到端走一遍并刷新页面**（只写不读不算通） | 后端与数据 · 编排与工具 · 前端页面与内容资产 |
| 2 | **SHOULD 环节缺环时显式降级** | F9 / F10 / F11 / F17 / F18 若缺失，界面必须显示统一降级话术，**不得静默** | 前端页面与内容资产 |
| 3 | **R-A…R-Q 门禁绿** | §8 的门禁项总表（`scripts/verify.sh` + 用例） | 架构与集成（门禁）· 各域（被查项） |
| 4 | ★ **五类内容资产达下界** | §7 的五条 | §7 的五个主责域 |
| 5 | ★ **`content_rev` 可复算** | 门禁用 `docs/02 §3.3` 规则 2 的算法**重算**，与 `MANIFEST.json` 内的值一致（`docs/04 §7` 校验⑥） | 架构与集成 |
| 6 | ★ **回归基线录入并冻结** | 首次全量测量值写入 `docs/01 §6` 的"解题正确率 / 判定准确率"格并**冻结**（只许上调，下调走 ADR） | 后端与数据录入 · **非实现者**核 |
| 7 | **单 worker 启动检测** | `>1` 即拒绝启动并说明原因（`docs/02 §8-15`） | 架构与集成 |
| 8 | **启动校验清单全过** | `docs/02 §8-14` 的七条（DAG 有环 / 缺 `kc_ids` / 缺 `gradable` / 缺 `answer_kind` / 公式重名 / `rev` 不一致 / 语料缺许可字段）—— **坏就起不来** | 架构与集成 |
| 9 | **运维演练 ② ③ ④** | 备份调度 · **恢复演练（首次）** · 压测（§6） | 架构与集成 |
| 10 | **CI 接线生效** | `.github/workflows/ci.yml`（本篇 §8 的步骤表）+ `scripts/verify.sh` 存在且绿 | 架构与集成 |
| 11 | ★ **验证级别 A/B 可达**（`docs/03 §9.2` 第 10 行的技术债） | **实现 `sympy_recompute` 与 `symbolic_equivalence` 两个手段** —— 否则级别照旧**恒为 D**（旧仓形态："代码走了流程，却一步真验证都没做"，承诺 1）。★ **它与 N1 的地板直接耦合**：N1 要求"20 道中**至少 8 道**须落在 A/B 级可达类别上，且拦截方式必须是**重算不一致**" —— A/B 不可达时这条地板**因构造不可能而无法满足** | 编排与工具 |

---

## 5. 阶段门 C · 真实链路与盲测（交付门）

| # | 出口条件 | 判据 | 执行人 |
|---|---|---|---|
| 1 | ★ **HITL 超时取值测定** | 在真实链路上测出取值并写回配置默认值（决策 5：**此前该值必须来自配置，不得硬编码**） | 编排与工具 |
| 2 | ★ **冻结正式集盲测** | 解答级注入集（N1）与答案级注入集（F5）跑完，**结果由非实现者签署**，结论写进本文 | **非实现者** |
| 3 | **N13 的三层资产齐备** | 样例集入仓进门禁 · 冻结正式集**不入仓** · 运行器入仓（`scripts/eval_verify.py`，**不含任何答案**） | 编排与工具 + 非实现者 |
| 4 | **四条可用性演示** | 单机完整 · 单机离线（**断网**）· 远程完整 · 降级（**不含答疑**）—— 四条各走一遍，**并如实承认降级演示不覆盖答疑** | 全部四域 |
| 5 | **N16 验收② 的前置** | 该账号**至少完成过一次答疑**（干净账号该查询为空，断言既非真也非假） | 后端与数据 |
| 6 | **运维演练 ① ③** | 证书续期 · 恢复演练（§6） | 架构与集成 |
| 7 | **阶段门 A 与 B 仍绿** | 重跑一遍（§1.2：门只增不改） | 架构与集成 |

---

## 6. ★ 四类运维演练（`docs/02 §11.1-10` 决策 10 的落点）

**决策 10 原文**：四类"**各绑一个 `docs/06` 阶段门动作**"。**否决"只把恢复演练进 `docs/06`"的理由是**：另外三项同样会变成"**到验收那天才发现从没排过练**"。本节即那四个动作：

| # | 类 | ★ **绑定的阶段门动作** | 判据（引用既有验收，不新定） | 责任人 |
|---|---|---|---|---|
| ① | **证书续期**（N18 连带） | **阶段门 C 第 6 项**：在远程托管形态下，用**距到期 ≤ 30 天**的证书走一次续期流程，续期后**立即**重跑 N18 的两条断言（明文 HTTP 0 通过 · `Strict-Transport-Security` 存在） | `docs/01 §6` 的"传输加密"行 | 架构与集成 |
| ② | **备份调度**（N19 连带） | **阶段门 B 第 9 项**：`scripts/backup.py` 就绪 + **调度生效**（每日一次）+ **一次真实执行**留下产物；备份失败**必须可观测**（写一条只含 ID 与动作的日志，符合 R-O） | `docs/03 §8.3` 的备份位置与取法（**禁止文件级 `cp`**，须 `sqlite3.Connection.backup()` 或 `VACUUM INTO`） | 架构与集成 |
| ③ | ★ **恢复演练频率**（N19 连带） | **阶段门 B 第 9 项**做**首次**演练；**阶段门 C 第 6 项**做**第二次**并留档。★ **频率首次取值（2026-09-17）**：**每两周一次**，且**每次数据库 schema 变更或 `content_rev` 变更后追加一次**、**交付前至少一次** | `docs/03 §8.3` 的恢复演练：恢复后四张表**逐表读回**且能被登录与错题本读到 | 架构与集成 + **非实现者**核 |
| ④ | **压测脚本**（N20 连带） | **阶段门 B 第 9 项**：`scripts/load_test.py` 就绪并跑出 N20 的判据（10 路并发答疑下 `/api/health` < 100ms、**第 11 路显式 503**；5 路中途断流后**无残留图**；10 路答疑 + 50 路判定的复合压测**无 5xx、无写库冲突**） | `docs/01 §6` 的"容量与并发"行（**均在 `worker=1` 下**） | 架构与集成 |

> ★ **频率与取值是本表首次写死**（2026-09-17）：**"每两周一次"是首次取值，不是从需求推出来的数** —— 依据是四人小组在小学期内的**可执行**节奏。它与 `docs/03 §7①` 的"`api/` 单函数 ≤ 40 行"同类：**宁可先定一个能被执行的数，也不要一个谁都能解释的形容词**；调整须在本文改并说明理由，**不得取消**（取消即 N19 退回"没排过练"）。
> ★ **`scripts/backup.py` 与 `scripts/load_test.py` 是本节首次指定的脚本名** —— 它们必须同步进 `docs/03 §8.2` 的**脚本清单**（那里的唯一权威），否则会出现"仓里有脚本，但清单不承认它"。

---

## 7. ★ 内容资产产出（`docs/02 §11.1-11` 决策 11 / 决策 9A 的落点）

**决策 11 原文**：`docs/05` 的 **R-C 表须至少两人分担**，**产出进 `docs/06` 阶段门 B**。责任人表见 `docs/05 §2.2`；本节给**门与判据**：

| 资产 | 下界（MUST 级地板） | **门** | 判据 | 主责域 |
|---|---|---|---|---|
| 题库 `problems.json` | **≥ 30 道**（长期 ≥ 100） | **B** | 启动校验过 + 每条含 `kc_ids` / `pt_id` / `gradable` / `answer_kind` | 前端页面与内容资产 |
| 错因库 `mistakes.json` | **≥ 40** | **B** | 每条含 `category` / `kc_ids`；**F5 的地板只能在此之后测** | 后端与数据 |
| 知识点 DAG `dag.json` | **≥ 40 节点 · ≥ 5 章** | **B** | **无环** + `depends_on` 单向（F19） | 后端与数据 |
| 题型库 `types.json` | **≥ 15** | **B** | 每条含 `framework` / `kc_ids`（F17 拆解的内容依据） | 前端页面与内容资产 |
| 公式库 `formula.json` | **≥ 30 条** | **B** | **名称唯一**（重名 ⇒ 启动报错退出，否则 F18 的"唯一条目"不可断言） | 编排与工具 |

★ **五条的共同出口**：`content/MANIFEST.json` 记录**五类各自的 `rev`** 并派生出 `content_rev`；门禁**重算**并与文件内值比对（§4 第 5 项）。**下界只许上调，下调走 ADR**（改 MUST 级地板，决策 9A 已否决"缩小下界"）。

---

## 8. 门禁项总表（R-A…R-Q 与校验口径的接线）

**本表是"规则 → 脚本/用例 → 门"的映射**。★ **规则内容不在这里**（唯一权威是 `docs/02 §1.6`），契约校验的**七节口径**也不在这里（唯一权威是 `docs/04 §7`）—— 本节只回答"**什么时候必须绿**"。

| 规则 / 校验 | 检查方式（`docs/02 §1.6` 的口径） | 落在哪 | 必须绿的门 |
|---|---|---|---|
| **R-A** 字符串字段上界 · 禁嵌套量词 | 可自动：`verify.sh` 静态扫描（扫 Pydantic 字段声明断言 `max_length`）+ 长度/长数字串用例 | `scripts/verify.sh` | A（文档面）· **B** |
| **R-B** 写库显式 `commit()` · 测试读回 | 可自动：扫描 + 读回用例 | `scripts/verify.sh` + `backend/tests/` | **B** |
| **R-C** 资产与编排层责任人 | ★ **仅 review**（脚本查不了） | `docs/05 §2.4` 清单 | **A**（入口）· **B**（入口条件） |
| **R-D** 禁硬编码色值/圆角/阴影 | 可自动：grep（**排除 `styles/tokens.css`**）；颜色关键字靠 review 兜底 | `scripts/verify.sh` | **B** |
| **R-E** 禁 `innerHTML` 拼用户内容 | 可自动：grep `innerHTML` 赋值 | `scripts/verify.sh` | **B** |
| **R-F** 可访问性三项分开 | 可自动：①② 前端冒烟；③ 色对清单 + 纯 Python WCAG（**只对"声明的色对"**） | 前端冒烟 + `verify.sh` | **B** |
| **R-G** URL 可寻址 + 监听 `hashchange` | 可自动：路由冒烟（N11） | 前端冒烟 | **B** |
| **R-H** 演示类页面 ≤ 产品页 | 可自动（review 级）：按 `feature.json` 的 `class` 分组统计行数 | `scripts/verify.sh` | **B** |
| **R-I** `must_render` 字段必须渲染 | 可自动：前端冒烟断言该字段的**显示字符串**出现在 DOM | 前端冒烟 | **B** |
| **R-J** 凭据不进日志/审计/错误/L2 | 可自动：grep 写入点 + "带凭据请求不产生含凭据日志"用例 | `verify.sh` + 用例 | **B** |
| **R-K** 出网收口（唯一运行期出网口） | 可自动：grep 出网调用出现在 `tools/llm.py` 之外即红灯 | `scripts/verify.sh` | **B** |
| **R-L** 载荷白名单 | 可自动：**假 provider 抓载荷** + 断言不含禁止字段（N16 验收③） | 用例 | **B** |
| **R-M** 凭据七面不落 | 可自动：①②③④⑦ grep + 假 provider；⑤ 路由冒烟；**⑥ 仅 review** | `verify.sh` + 用例 + review | **B** |
| **R-N** "去向可查" | 可自动：接口用例（**前置：该账号至少完成过一次答疑**） | 用例 | **C** |
| **R-O** 日志只记 ID 与动作 | 可自动：grep 写入点 + "跑完一次答疑后日志不含提问原文"用例 | `verify.sh` + 用例 | **B** |
| **R-P** 文档自洽（P1/P2/P3） | 可自动：`verify.sh` 从表体算计数、解析引用、落点反向闭合 | `scripts/verify.sh` | **A**（← `docs/02 §1.6` 点名要求）· 之后每次 |
| **R-Q** 工具调用边界（①②③） | 可自动：grep `allowed=None` / 硬编码成功标记 + 失败可见性用例 | `scripts/verify.sh` + 用例 | **B** |

★ **其中三条的"落在哪"不是 `verify.sh`，必须说清**：**R-C**（人）、**R-M 的 ⑥**（无埋点、无导出路径）、**R-I / R-F / R-G 的前端冒烟**（需要真跑前端）。**把它们混算进 `verify.sh` 会让门禁看起来比实际更强。**

---

## 9. 五条口径（否则本节会被读成"28 条都要做完"）

① ★ **N5 是条件 MUST** —— 探针后置期间它**不生效**。本文§2.2 明确记为"**未开始（形态后置）**"，**不得计为已完成**（`docs/01` 附录 B.1 口径 1）。

② ★ **MUST 满足率的上限不是 100%** —— `docs/01 §6` 的两条限定：**F1 的注入式读屏部分**与 **F10 的 PDF 溯源部分**（F10 本身是 SHOULD）在 v2 属**形态后置**，**不计入任何达标线**。这是**明账，不是遗漏**。

③ ★ **禁止用主站的满足度抵充探针的缺失** —— `docs/01 §7` 的判定规则原文：形态后置的需求"**不得**被算作已完成；只能是'未开始（形态后置）'"。

④ **禁止需求 ID 区间记法** —— `docs/01 §6` 的记法禁令：写"F2 · F4 · F5 · F7 · F8 · F19 全通"，**不写**"F1→F8 全通"。理由：区间记法会让将来插入的 MUST 被**静默**算进验收。

⑤ **观察指标不设门禁** —— 复习有效性（F7 · F8 · F19）只承诺"能从作答记录算出来"，**不承诺数值**；样本不足时显示统一文案"**数据不足，暂不显示薄弱点**"。**把它设成门禁只会逼出一个假数字**（N2 要防的事）。

---

## 10. 与其它文档的分工（防两处记载）

| 问题 | 唯一权威 | 本文只能 |
|---|---|---|
| **达标线**（拦多少 / 延迟多少 / 命中率多少） | `docs/01 §6` | **引用维度名**，不复制数值 |
| **28 条 MUST 的清单与条数** | `docs/01` 附录 B.1 | §2 与之**条数相等**，只补"动作与门" |
| **规则内容**（R-A…R-Q 是什么） | `docs/02 §1.6` | §8 只写"何时必须绿" |
| **契约校验的七节口径** | `docs/04 §7` | §3 只写"哪些在阶段门 A 必须绿" |
| **谁负责** | `docs/05` | 只引用域名 |
| ★ **工作包（顺序与边界）** | **本篇 §11** | `docs/05` 只管"哪块归谁"，**不排顺序** |
| **脚本清单** | `docs/03 §8.2` | §6 新增的两个脚本名**必须同步回去** |
| ★ **域与写作用域互斥** | `docs/05 §3` / §4 | §11 的工作包只写**顺序与边界**，**不写责任人**（一律指回 `docs/05`） |
| **演练的部署前提**（备份位置、取法、不含日志） | `docs/03 §8.3` | §6 引用，不重写 |

> ★ **本文最容易长歪的地方**：把 `docs/01 §6` 的达标线抄一遍"方便查看"。**判定方法**：本文任何一格如果**删掉日期与门名之后仍然是一个验收标准**，那它就抄错了位置 —— 它属于 `docs/01 §6`。

---

## 11. ★ 工作包（可分派单元）—— 第一个"可以直接领活"的粒度

**本节只回答一件事**：把 §4 的 **11 项出口条件**切成几块、按什么顺序、**谁能与谁并行**。判据**一律用指针**（`§2` / `§4` / `§7` / `docs/05`）—— 本节**不复制任何判据、不写任何日期**：日期是小组的事，本节只给**顺序与边界**（那是别处没有的信息）。

★ **切法是「域 × 纵深切片」，不是横切**：每个包是一条**能演示的闭环**。横切（"先把所有 model 写完"）会把四个人的进度**锁在同一层**上，于是同时卡在同一个未知上。

| 包 | 主要交付 | 责任域（`docs/05`） | 前置 | ★ 闭合（**全部为指针**） |
|---|---|---|---|---|
| **W0 骨架** | FastAPI 入口 `backend/app/main.py` · `frontend/index.html` 与 `frontend/core/` 外壳（`dom`/`bus`/`store`/`router`/`api`/`auth`）· `backend/tests/` · `requirements.txt` / `-dev` · **`scripts/verify.sh` v0（先跑 R-P 与契约校验）跑绿一次** | **架构与集成** | 无 | **§4 入口条件 2** · §4 第 3 / 7 / 10 项 · 载体：N11（`router`）· N8（`tokens.css`） |
| **W1 身份闭环** | 登录 → 令牌 → `GET /api/me` → 越权 404 → `must_change_pw` 与两把锁 · `DELETE /api/me/data` 的四类处置 · `#/login` | **后端与数据**（`#/login` 归 **架构与集成**） | W0 | §2.1 的 **F15 · F16 · N6** · §4 第 3 项的**凭据写入点**（R-J / R-M / R-O） |
| **W2 判定闭环** | `GET /api/problems` · `POST /api/practice/grade`（确定性 · `to_thread`）· `POST /api/practice/hint`（提示阶梯）· attempt 落库**读回** · `GET /api/me/wrong` · `GET /api/me/progress` · **题库与错因库达下界**（§7）· `#/practice` · `#/wrong` · `#/home` | **后端与数据** · **前端页面与内容资产** | W0 · W1 | §2.1 的 **F4 · F6 · F7 · F8 · F19** · §4 第 1 项（判定与留痕侧）· 第 6 项（回归基线）· 第 2 项（F11 的降级话术）· §7 的两条资产 |
| **W3 答疑闭环** | `POST /api/chat` 的 **14 类事件**与 SSE 帧 · 验证报告 + `must_render` · `qa_log.egress` · `GET` / `POST /api/me/notice` · 端点信任边界与 `X-Model-Key` · `#/settings` | **编排与工具** · **后端与数据** | W0 · W1 | §2.1 的 **F1 · F2 · F3 · N1 · N2 · N14 · N15 · N16** · **§4 第 11 项**（A/B 级可达 —— 不实现两个手段则 N1 的地板构造不可能） |
| **W4 知识闭环** | 余下三类资产达下界（§7）· `GET /api/knowledge/tree` / `formula` · F17 的拆解走 `hint` · F9 的**前端 BFS** · `#/knowledge` | **编排与工具** · **后端与数据** · **前端页面与内容资产** | W0 · W2（题库格式已定） | §2.1 的 **F17 · F18** · §2.2 的 **F20** · **§4 第 4 / 5 项**（五类齐备 + `content_rev` 复算）· 第 2 项（F9 / F10 / F17 / F18 的降级话术） |
| **W5 门禁与运维** | `scripts/verify.sh` **全量**（R-A…R-Q）· 前端冒烟（R-F①② / R-G / R-I）· `backup.py` + **恢复演练首次** · `load_test.py` + 并发压测 · **CI 生效** | **架构与集成** | W1–W4 全部 | §4 第 3 / 9 / 10 项 · §2.1 的 **N8 · N11 · N18 · N19 · N20** 的**断言面** |

**关键路径与并行度**：

```
W0 ──┬─▶ W1 ──┬─▶ W2 ──▶ W4 ──┐
     │        └─▶ W3 ──────────┼─▶ W5 ──▶ 阶段门 B
     └─（W0 期间：另外三个域**不空等**，见下）
```

★ **W0 期间另外三个域不会空等** —— 这是"契约先冻结"的兑现方式，也是它**不是一句宣言**的证据：契约已有 **14 份 + 38 个 examples 全部自校验**（`docs/04 §4`），因此

- **前端**可以照 `examples` 把页面与渲染先写完（**`examples` 就是它当前的假数据源**）；
- **后端**可以把 `domain/` 的纯函数与用例先写完（它**只依赖 `core/`**，`docs/03 §3.1` 那条"整张表的地基"）；
- **编排域**可以先把图节点与**显式注册表**写完（`docs/03 §7①`）。

> ★ **本节与 `docs/05` 的分工**：`docs/05` 回答"**哪块归谁**"（域与写作用域互斥），本节回答"**谁先谁后、能与谁并行**"（顺序与边界）。**责任人不在这里** —— 一律指回 `docs/05`。
> ★ **本节不得出现具体阈值**：一旦出现"11 路 503"这类数，就是抄了 `docs/01 §6` 或 §4 的位置（§10 的判定方法）。**资产下界一律写"达下界（§7）"。**
> ★ **W0 是唯一"必须先做"的包**，而它只占一个人 —— 这正是把骨架**单列成包**而不是塞进 W1 的理由：塞进去会让另外三域**白等一个人**。
